<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>进程树</title>
    <url>/2026/03/11/OS/%E8%BF%9B%E7%A8%8B%E6%A0%91/</url>
    <content><![CDATA[<h1 id="进程树"><a href="#进程树" class="headerlink" title="进程树"></a>进程树</h1><blockquote>
<p>返回综述: <a href="%E8%BF%9B%E7%A8%8B%E7%AE%A1%E7%90%86.md">进程管理</a></p>
</blockquote>
<h2 id="进程树的数据结构"><a href="#进程树的数据结构" class="headerlink" title="进程树的数据结构"></a>进程树的数据结构</h2><p>在 <code>task_struct</code> 中, 通过以下字段构成一棵<strong>多叉双向树</strong>:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> __<span class="title">rcu</span> *<span class="title">real_parent</span>;</span> <span class="comment">// 生物学父进程: 谁fork了我</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> __<span class="title">rcu</span> *<span class="title">parent</span>;</span>     <span class="comment">// 法律上的父进程: 谁来wait我、收我的SIGCHLD</span></span><br><span class="line"><span class="comment">// 通常 real_parent == parent, 仅在 ptrace 调试时不同</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">children</span>;</span> <span class="comment">// 作为父进程时: 子进程链表的哨兵头(入口)</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">sibling</span>;</span>  <span class="comment">// 作为子进程时: 挂在父进程children链表上的节点</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *<span class="title">group_leader</span>;</span> <span class="comment">// 线程组的主线程(和进程树无关, 是线程组维度的字段)</span></span><br></pre></td></tr></table></figure>

<h3 id="parent-和-real-parent-为什么要有两个-parent"><a href="#parent-和-real-parent-为什么要有两个-parent" class="headerlink" title="parent 和 real_parent, 为什么要有两个 parent?"></a><code>parent</code> 和 <code>real_parent</code>, 为什么要有两个 parent?</h3><p>这个场景主要是为了应对 debug 即 ptrace 场景, 在不使用调试器的时候, 两者指向的都是父进程.<br>但是在使用调试器的场景下, <code>real_parent</code> 仍然是 fork parent, 但是 <code>parent</code> 指向 ptrace 调试器进程, 这个 <code>parent</code> 会接收 <code>wait</code> 和 <code>SIGCHLD</code> 信号</p>
<h3 id="children-和-sibling-是怎么构成一棵多叉树的"><a href="#children-和-sibling-是怎么构成一棵多叉树的" class="headerlink" title="children 和 sibling 是怎么构成一棵多叉树的?"></a><code>children</code> 和 <code>sibling</code> 是怎么构成一棵多叉树的?</h3><p>这是一个经典的 Linux 内核侵入式链表设计. 逻辑上等效于 LCRS(Left-Child Right-Sibling) 树:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">tree_node</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">tree_node</span> *<span class="title">parent</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">tree_node</span> *<span class="title">first_child</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">tree_node</span> *<span class="title">next_sibling</span>;</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>但 LCRS 的写法存在问题:</p>
<ol>
<li><code>task_struct</code> 关联了非常多的链表关系(进程树、全局进程列表、线程组等). LCRS 的遍历逻辑是进程树专用的, 每种链表关系都要单独维护一套增删&#x2F;遍历 API. 实际上链表操作是通用的, 应该和具体的使用场景解耦.</li>
<li>LCRS 通常是单向链表, 删除需要先遍历找前驱, 时间复杂度 O(n)</li>
<li>不能用动态数组存 <code>children</code>, 因为内核关键路径上绝不能有可能失败的内存分配</li>
</ol>
<p>因此 Linux 使用了侵入式链表: 把一个通用的链表节点(<code>list_head</code>)嵌入到数据结构内部:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> *<span class="title">next</span>, *<span class="title">prev</span>;</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>每个 <code>task_struct</code> 同时扮演两个角色:</p>
<ul>
<li><strong>作为父进程</strong>: <code>children</code> 是子进程链表的哨兵头(入口, 不代表任何子进程)</li>
<li><strong>作为子进程</strong>: <code>sibling</code> 是挂在父进程 <code>children</code> 链表上的普通节点</li>
</ul>
<p><code>children.next</code> 指向第一个子进程的 <code>sibling</code> 地址, 每个子进程的 <code>sibling.next</code> 指向下一个兄弟的 <code>sibling</code> 地址, 最后一个兄弟的 <code>sibling.next</code> 回到父进程的 <code>children</code>, 形成双向循环链表:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">父进程.children ←→ 子A.sibling ←→ 子B.sibling ←→ 子C.sibling ←→ (回到父进程.children)</span><br></pre></td></tr></table></figure>

<h3 id="fork-的时候-怎么将新子进程挂到父进程的进程树上"><a href="#fork-的时候-怎么将新子进程挂到父进程的进程树上" class="headerlink" title="fork 的时候, 怎么将新子进程挂到父进程的进程树上?"></a>fork 的时候, 怎么将新子进程挂到父进程的进程树上?</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// kernel/fork.c</span></span><br><span class="line"><span class="comment">// p 是新创建的子进程, p-&gt;real_parent 是执行fork的父进程</span></span><br><span class="line">list_add_tail(&amp;p-&gt;sibling, &amp;p-&gt;real_parent-&gt;children);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 等效于(伪代码):</span></span><br><span class="line"><span class="comment">// 把 p-&gt;sibling 插到 real_parent-&gt;children 链表的尾部</span></span><br><span class="line">tail = parent-&gt;children.prev;</span><br><span class="line">tail-&gt;next = &amp;p-&gt;sibling;</span><br><span class="line">p-&gt;sibling.prev = tail;</span><br><span class="line">p-&gt;sibling.next = &amp;parent-&gt;children;</span><br><span class="line">parent-&gt;children.prev = &amp;p-&gt;sibling;</span><br></pre></td></tr></table></figure>

<h3 id="遍历-怎么找到某个进程的所有子进程"><a href="#遍历-怎么找到某个进程的所有子进程" class="headerlink" title="遍历: 怎么找到某个进程的所有子进程?"></a>遍历: 怎么找到某个进程的所有子进程?</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *<span class="title">child</span>;</span></span><br><span class="line">list_for_each_entry(child, &amp;parent-&gt;children, sibling) &#123;</span><br><span class="line">    <span class="comment">// child 就是每个子进程的 task_struct *</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>list_for_each_entry</code> 展开后等效于:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> *<span class="title">pos</span> =</span> parent-&gt;children.next; <span class="comment">// 从哨兵头的next开始</span></span><br><span class="line"><span class="keyword">while</span> (pos != &amp;parent-&gt;children) &#123;             <span class="comment">// 走回哨兵头就停</span></span><br><span class="line">    child = container_of(pos, <span class="keyword">struct</span> task_struct, sibling);</span><br><span class="line">    <span class="comment">// ... 使用 child ...</span></span><br><span class="line">    pos = pos-&gt;next;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="container-of-怎么从-sibling-的地址反推出-task-struct-的地址"><a href="#container-of-怎么从-sibling-的地址反推出-task-struct-的地址" class="headerlink" title="container_of: 怎么从 sibling 的地址反推出 task_struct 的地址?"></a><code>container_of</code>: 怎么从 <code>sibling</code> 的地址反推出 <code>task_struct</code> 的地址?</h3><p><code>list_head</code> 内部没有 data 字段, 它不知道自己属于哪个 <code>task_struct</code>. 但因为 <code>sibling</code> 是嵌在 <code>task_struct</code> 内部的, 它在结构体中的偏移量是编译时确定的常量, 所以: <strong><code>sibling</code> 的地址 - 偏移量 &#x3D; <code>task_struct</code> 的起始地址</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// include/linux/container_of.h</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> container_of(ptr, type, member) \</span></span><br><span class="line"><span class="meta">    ((type *)((char *)(ptr) - offsetof(type, member)))</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 用法:</span></span><br><span class="line">container_of(&amp;某子进程.sibling, <span class="keyword">struct</span> task_struct, sibling)</span><br><span class="line"><span class="comment">// = &amp;某子进程.sibling 的地址 - sibling在task_struct中的偏移</span></span><br><span class="line"><span class="comment">// = 某子进程 task_struct 的起始地址</span></span><br></pre></td></tr></table></figure>

<p>这就是侵入式链表的核心: <strong>不是链表节点包含数据, 而是数据结构包含链表节点, 通过地址减偏移反推出数据结构地址.</strong></p>
<hr>
<h2 id="进程树为什么存在"><a href="#进程树为什么存在" class="headerlink" title="进程树为什么存在"></a>进程树为什么存在</h2><h3 id="为什么内核必须记录-parent、children、sibling-而不是只维护一个-PID-列表"><a href="#为什么内核必须记录-parent、children、sibling-而不是只维护一个-PID-列表" class="headerlink" title="为什么内核必须记录 parent、children、sibling, 而不是只维护一个 PID 列表?"></a>为什么内核必须记录 <code>parent</code>、<code>children</code>、<code>sibling</code>, 而不是只维护一个 PID 列表?</h3><p>只维护了 PID 列表, 相当于对于内核来说只知道有哪些进程, 而不知道这些进程之间的关系, 则需要亲缘关系的时候(如 <code>wait()</code>, 托孤, 信号传播), 都需要遍历 PID 列表来拿到亲缘关系, 时间复杂度 O(n), 但是维护了亲缘关系以后, 就只用遍历子进程, 变成了 O(子进程数)</p>
<h3 id="这些关系分别支撑了哪些操作-wait、发送信号、回收退出状态、托孤"><a href="#这些关系分别支撑了哪些操作-wait、发送信号、回收退出状态、托孤" class="headerlink" title="这些关系分别支撑了哪些操作: wait、发送信号、回收退出状态、托孤?"></a>这些关系分别支撑了哪些操作: <code>wait</code>、发送信号、回收退出状态、托孤?</h3><ul>
<li><strong>托孤</strong>: 父进程退出的时候, 在 exit 路径中通过 <code>forget_original_parent()</code> → <code>find_new_reaper()</code> 将自己所有的子进程的 <code>real_parent</code> 改成 init&#x2F;subreaper. 由 init 进程来调用 <code>wait()</code>&#x2F;<code>waitpid()</code> 来完成 <code>task_struct</code> 最后的 PID 资源的回收和 <code>exit_state</code> 的回收. 这个机制依赖于父进程通过 <code>children</code> 链表遍历所有子进程</li>
<li><strong>信号传播</strong>:<ol>
<li><code>kill(-pgid, sig)</code> 向整个进程组发信号</li>
<li>会话断开的时候, 会话 leader(常常是 bash&#x2F;zsh…) 进程退出, 发送 <code>SIGHUP</code> 信号, 沿进程树传播要通知所有的前台进程&#x2F;子进程, 子进程收到后退出, 如果没有树结构, 就只能全局扫描</li>
<li><code>parent</code> 支撑 <code>SIGCHLD</code>: 子进程退出的时候(<code>do_exit()</code> → <code>exit_notify()</code>), 内核通过 <code>parent</code> 指针找到父进程, 向他发送 <code>SIGCHLD</code> 信号, 通知父进程有个进程退出了, 调用 <code>wait()</code> 回收资源</li>
</ol>
</li>
<li><strong>回收状态&#x2F;<code>wait()</code></strong>: <code>do_wait()</code> 的实现就是遍历 <code>current-&gt;children</code> 链表, 逐个检查子进程的 <code>exit_state</code>, 找到 <code>EXIT_ZOMBIE</code> 的子进程回收, 没有 <code>children</code> 链表, 就无法找到自己的子进程, 可能会产生永久僵尸</li>
</ul>
<hr>
<h2 id="进程树和任务控制的关系"><a href="#进程树和任务控制的关系" class="headerlink" title="进程树和任务控制的关系"></a>进程树和任务控制的关系</h2><h3 id="进程树、进程组、Session-之间是什么关系"><a href="#进程树、进程组、Session-之间是什么关系" class="headerlink" title="进程树、进程组、Session 之间是什么关系?"></a>进程树、进程组、Session 之间是什么关系?</h3><p>三者是<strong>不同维度的组织方式, 层层嵌套但不相同</strong>:</p>
<ul>
<li><strong>进程组 (Process Group)</strong>: 由 PGID(Process Group ID) 标识, 通过 <code>task_pgrp()</code> 获取. 同一条管道里的进程共享同一个 PGID. 进程组 leader 的 PID &#x3D;&#x3D; PGID. 注意 PGID 和 TGID(Thread Group ID, 线程组ID) 是两个完全不同的概念.</li>
<li><strong>进程树 (Process Tree)</strong>: 维护所有进程之间的亲缘关系, 是 <code>fork()</code> 的产物. <code>fork()</code> 创建的子进程会继承父进程的 SID 和 PGID. <code>execve()</code> 不创建新进程, 只替换程序映像, PID&#x2F;PGID&#x2F;SID 保持不变. 进程树关系是自动的、不可变的; 进程组关系是 shell 按作业逻辑主动设置的、可变的.</li>
<li><strong>Session (会话)</strong>: 一次终端连接, 是最外层的容器, 包含一个或多个进程组. 通过 <code>setsid()</code> 能让一个非 leader 的进程创建一个新的 Session, 并成为这个 Session&#x2F;Group 的 leader. 这也是为什么守护进程要执行 <code>fork()</code> → <code>setsid()</code>: 如果调用者的 PID &#x3D;&#x3D; PGID(即它是进程组 leader), <code>setsid()</code> 会失败, 所以先 fork 出子进程(子进程 PID ≠ 父进程 PGID), 再在子进程中 <code>setsid()</code>. 如果一个 Session leader 终止了, 会发送 <code>SIGHUP</code> 信号到前台进程组.</li>
</ul>
<table>
<thead>
<tr>
<th>维度</th>
<th>进程树</th>
<th>进程组</th>
<th>会话</th>
</tr>
</thead>
<tbody><tr>
<td><strong>组织依据</strong></td>
<td>谁 fork 了谁</td>
<td>谁在做同一个 job</td>
<td>谁共享同一个终端</td>
</tr>
<tr>
<td><strong>标识符</strong></td>
<td>无(通过 <code>parent</code>&#x2F;<code>children</code> 指针)</td>
<td>PGID</td>
<td>SID</td>
</tr>
<tr>
<td><strong>建立时机</strong></td>
<td>fork 时自动建立</td>
<td>shell 启动 job 时 <code>setpgid()</code></td>
<td>登录时 <code>setsid()</code></td>
</tr>
<tr>
<td><strong>可否改变</strong></td>
<td>不可(除 ptrace&#x2F;托孤)</td>
<td>可以 <code>setpgid()</code></td>
<td>可以 <code>setsid()</code></td>
</tr>
<tr>
<td><strong>包含关系</strong></td>
<td>无层级嵌套</td>
<td>一个会话包含多个进程组</td>
<td>最外层容器</td>
</tr>
</tbody></table>
<h3 id="为什么-Ctrl-C-SIGINT-、Ctrl-Z-SIGTSTP-往往不是只作用在单个进程上"><a href="#为什么-Ctrl-C-SIGINT-、Ctrl-Z-SIGTSTP-往往不是只作用在单个进程上" class="headerlink" title="为什么 Ctrl+C (SIGINT)、Ctrl+Z (SIGTSTP) 往往不是只作用在单个进程上?"></a>为什么 <code>Ctrl+C</code> (<code>SIGINT</code>)、<code>Ctrl+Z</code> (<code>SIGTSTP</code>) 往往不是只作用在单个进程上?</h3><p>实际上是作用在一整个前台进程组上的. 在最经典的管道场景中, <code>cat file | grep foo | wc -l</code>, 如果只发给一个前台的进程, 就会导致 <code>cat</code> 被终止了, 但是 <code>grep</code>&#x2F;<code>wc</code> 还在运行, 还在往管道里面写数据, 行为就乱了</p>
<h3 id="这和-shell、前台任务、后台任务有什么关系"><a href="#这和-shell、前台任务、后台任务有什么关系" class="headerlink" title="这和 shell、前台任务、后台任务有什么关系?"></a>这和 shell、前台任务、后台任务有什么关系?</h3><ul>
<li><strong>shell</strong>: 是作业控制的实现者, 通过进程组和会话机制来管理前台&#x2F;后台任务, 启动时会通过 <code>setsid()</code> 成为 Session leader &#x2F; 进程组 leader</li>
<li><strong>前台任务</strong>: 是会接受终端输入的一组进程. 如 <code>cat file | grep foo</code>, shell 首先通过 <code>fork()</code> 创建出两个子进程, 然后调用 <code>setpgid()</code> 将这两个进程放到一个进程组里面, 调用 <code>tcsetpgrp()</code> 将这个进程组变成前台进程组. 终端的键盘信号都会发送给前台进程组, shell 自己调用 <code>waitpid()</code> 阻塞等待前台任务完成</li>
<li><strong>后台任务</strong>: 没有调用 <code>tcsetpgrp()</code> 这一个设置成前台进程组的步骤, 后台进程组不持有前台, 所以 <code>Ctrl+C</code> 不影响它</li>
</ul>
<table>
<thead>
<tr>
<th>操作</th>
<th>本质</th>
</tr>
</thead>
<tbody><tr>
<td><code>Ctrl+Z</code></td>
<td>终端驱动向前台进程组发 <code>SIGTSTP</code> → 整组暂停</td>
</tr>
<tr>
<td><code>jobs</code></td>
<td>shell 遍历自己管理的进程组列表, 显示状态</td>
</tr>
<tr>
<td><code>fg %1</code></td>
<td>shell 调用 <code>tcsetpgrp()</code> 把目标进程组设为前台 + 发 <code>SIGCONT</code> 恢复</td>
</tr>
<tr>
<td><code>bg %1</code></td>
<td>shell 向目标进程组发 <code>SIGCONT</code>, 但<strong>不</strong>设为前台 → 后台继续运行</td>
</tr>
<tr>
<td><code>kill %1</code></td>
<td>shell 向目标进程组发 <code>SIGTERM</code></td>
</tr>
</tbody></table>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul>
<li>进程树: linux kernel里面为进程之间的亲子关系维护的一颗双向多叉树, 使用linux侵入式链表设计, 进程树对于托孤机制(僵尸清理), 沿亲缘关系的传播的信号来说是不可获取的机制, 比如wait()操作本身就是遍历进程的所有的children, 挨个回收PID和退出状态.</li>
<li>shell任务控制: 任务分成前台任务和后台任务, 拿管道的运用举例子, 创建任务的步骤是shell fork出来一系列的子进程, 然后setpgid(), 将这些进程放到一个进程组里面. 对于前台进程(比如我们输入的命令), 还有一个额外的设置成前台进程的步骤(tcsetpgrp()), 前台进程会接受键盘信号(Ctrl + C &#x2F; Ctrl +Z). 这些信号传播的最小单元是一个进程组. 无论是前台任务还是后台任务, 都会共享一个session id来代表他们是经由这个session代表的shell进程创建的. shell会执行waitpid()来阻塞式等待, 并且完成最后的资源的清理. shell退出的时候, 会沿进程树的所有子进程的所在的进程组发送SIGHUP.</li>
<li>守护进程: daemon进程就是一个后台进程, 创建的核心就是要和原来的session解绑(创立新的pgid, sid), 然后不再是shell进程的子进程, 所有流程是fork(leader进程不能setsid) - 父进程不wait(成为孤儿进程, 挂在init下面) - setsid(重制pgid&#x2F;sid) - fork</li>
<li>僵尸进程: 由父进程不wait()&#x2F;waitpid(), 导致没有执行进程的最后的PID&#x2F;exit_state的回收导致的中间态死亡进程, 大量的僵尸进程会导致PID空间不足</li>
<li>孤儿进程: 父进程死亡, 子进程就变成了孤儿进程. 父进程exit的时候会调用exit() -&gt; forget_original_parent() -&gt; find_new_reaper()流程将子进程托孤给init&#x2F;subreaper, 这两类进程会定期执行wait()清理僵尸进程.</li>
</ul>
]]></content>
      <categories>
        <category>OS</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>进程管理</tag>
        <tag>进程树</tag>
        <tag>task_struct</tag>
        <tag>侵入式链表</tag>
      </tags>
  </entry>
  <entry>
    <title>进程管理</title>
    <url>/2026/03/08/OS/%E8%BF%9B%E7%A8%8B%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<h1 id="进程管理"><a href="#进程管理" class="headerlink" title="进程管理"></a>进程管理</h1><h2 id="进程数据结构"><a href="#进程数据结构" class="headerlink" title="进程数据结构"></a>进程数据结构</h2><p>进程的数据结构是task struct</p>
<ol>
<li>cpu资源:<ol>
<li>调度优先级</li>
</ol>
</li>
<li>内存地址空间资源:<ol>
<li>mm_struct</li>
</ol>
</li>
<li>打开的文件资源:<ol>
<li>file_struct files (一个数组, 存的就是打开的文件的地址, 索引即是文件描符 fd)</li>
</ol>
</li>
</ol>
<p>进程自己的信息与状态</p>
<ol>
<li>进程状态: 存储在task-&gt;state, task-&gt;exit_state两个字段中<ol>
<li>如TASK_RUNNING, TASK_INTERRUPTIBLE, TASK_UNINTERRUPTIBLE, __TASK_STOPED…</li>
</ol>
</li>
<li>唯一ID<ol>
<li>pid: 线程级别的id</li>
<li>gtid: 进程级别的id</li>
</ol>
</li>
<li>文件系统信息 <code>struct fs_struct *fs</code></li>
<li>namespace</li>
<li>进程树关系: 该进程在整个进程树里面的位置</li>
</ol>
<h3 id="进程的状态"><a href="#进程的状态" class="headerlink" title="进程的状态"></a>进程的状态</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* Used in tsk-&gt;state: */</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_RUNNING            0x00000000</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_INTERRUPTIBLE      0x00000001</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_UNINTERRUPTIBLE    0x00000002</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> __TASK_STOPPED          0x00000004</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> __TASK_TRACED           0x00000008</span></span><br><span class="line"><span class="comment">/* Used in tsk-&gt;exit_state: */</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> EXIT_DEAD               0x00000010</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> EXIT_ZOMBIE             0x00000020</span></span><br><span class="line"><span class="comment">/* Used in tsk-&gt;state again: */</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_DEAD               0x00000080</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* 组合状态（便利宏）: */</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_KILLABLE           (TASK_WAKEKILL | TASK_UNINTERRUPTIBLE)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_STOPPED            (TASK_WAKEKILL | __TASK_STOPPED)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_IDLE               (TASK_UNINTERRUPTIBLE | TASK_NOLOAD)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_NORMAL			(TASK_INTERRUPTIBLE | TASK_UNINTERRUPTIBLE)</span></span><br><span class="line"><span class="comment">// ...</span></span><br></pre></td></tr></table></figure>

<p>常见状态语义说明:</p>
<p><strong>存储在task-&gt;state的</strong></p>
<ul>
<li>TASK_RUNNING: 进程正在运行队列中, 准备运行&#x2F;正在运行</li>
<li>TASK_NORAML&#x2F;TASK_INTERRUPTIBLE&#x2F;TASK_UNINTERRUPTIBLE:<ul>
<li>TASK_NORMAL &#x3D; <code>(TASK_INTERRUPTIBLE | TASK_UNINTERRUPTIBLE)</code> 是这两个状态的综合, 表明进程正在睡眠</li>
<li>TASK_INTERRUPTIBLE: 进程处于可打断的睡眠状态, 正在等待某个条件满足, 被wake_up唤醒, 或者被信号唤醒. 不会被计入load average</li>
<li>TASK_UNINTERRUPTIBLE: 进程处于不可中断的睡眠状态, 只能在条件满足时, 被wake_up唤醒, 不能被信号唤醒, 常用于(如磁盘 I&#x2F;O)等不能被中途打断的SLEEP, 这也代表, 在这个状态的进程的数量能一定程度反应当前计算机的物理负载, 这个状态会被计入load average</li>
<li>TASK_KILLABLE &#x3D; <code>(TASK_WAKEKILL | TASK_UNINTERRUPTIBLE)</code> 可以被致命信号杀死(SIGKILL)的不可中断睡眠</li>
<li>__TASK_STOPPED &#x3D; 进程被暂停执行了, 不会被调度. 1. 收到SIGSTOP &#x2F; SIGSTP &#x2F; SIGTTIN &#x2F; SIGTTOU 信号 2. ptrace attach 后发送 SIGSTOP. 常见的进入情景: 1. Ctrl + z 2. debug. 退出的时机: 收到 SIGCONT 信号</li>
<li>__TASK_TRACED &#x3D; 进程正在被调试器跟踪</li>
<li>TASK_IDLE (TASK_UNINTERRUPTIBLE | TASK_NOLOAD):<ul>
<li>语义: TASK_UNINTERRUPTIBLE类似的不可中断睡眠, 但<strong>不计入 load average</strong></li>
<li>内核中空闲时会运行的 idle线程, 内核中某些无关紧要的等待</li>
<li>如果 idle线程用TASK_UNINTERRUPTIBLE而包含TASK_NOLOAD会导致load average空长, 不能反应真实物理资源负载</li>
</ul>
</li>
</ul>
</li>
</ul>
<p><strong>存储在task-&gt;exit_code的</strong></p>
<ul>
<li>EXIT_ZOMBIE: 僵尸状态<ul>
<li>语义: 代码已经执行完毕</li>
<li>进入的时机: 进程调用exit() -&gt; do_exit() -&gt; exit_notify() 中设置 tsk-&gt;exit_state &#x3D; EXIT_ZOMBIE</li>
<li>退出的时机: 父进程调用 <code>wait</code> &#x2F; <code>waitpid()</code> 读取退出状态 -&gt; <code>wait_task_zombie()</code> -&gt; <code>exit_state</code>中改为 <code>EXIT_DEAD</code> -&gt; <code>task_struct</code> 被释放</li>
</ul>
</li>
<li>EXIT_DEAD (0x10) : 彻底死亡<ul>
<li>语义: 进程的task_struct正在被回收或者已经被回收</li>
<li>进入的时机: 父进程 <code>wait()</code> 成功后, 状态从 <code>EXIT_ZOMBIE</code> 变成 <code>EXIT_DEAD</code></li>
<li>退出的时机: 这是一个瞬态, 存在的时间极短, 之后 <code>task_struct</code> 被回收</li>
</ul>
</li>
</ul>
<p><strong>常见的进程类型</strong></p>
<ul>
<li><p><strong>僵尸进程</strong>: 进程(task_struct)已经被释放, 但是还保留PID和exit_state为EXIT_ZOMBIE</p>
<ul>
<li>为什么需要这个状态: 父进程可能需要在进程被关闭以后, 获取进程的PID并检查它的退出码来衡量进程是不是正常退出的</li>
<li>资源泄漏: 状态为EXIT_ZOMBIE的进程占用一个PID和少量的内核内存. 如果大量堆积, 会导致内核内存泄漏, 或PID耗尽</li>
<li>怎么产生的: 父进程没有调用wait()或者waitpid()</li>
<li>解决方式:<ul>
<li>父进程调用 wait() &#x2F; waitpid()</li>
<li>设置 signal(SIGCHLD, SIG_IGN) 让内核自动回收 (do_notigy_parent返回true的时候直接设置状态为EXIT_DEAD)</li>
<li>杀死父进程 -&gt; 僵尸被init(PID&#x3D;1)收养 -&gt; init会自动 <code>wait()</code>清理</li>
</ul>
</li>
</ul>
</li>
<li><p><strong>孤儿进程</strong>: 进程的父进程先于子进程被杀死了, 子进程就成为了孤儿进程</p>
<ul>
<li>内核会将孤儿进程的父进程指向init, 或者当前 PID namespace 中的 subreaper 进程, init进程会定期调用 <code>wait()</code>来清理</li>
<li>本身是正常的, 不会造成问题</li>
</ul>
</li>
<li><p><strong>守护进程</strong>: 在后台运行的, 不再与任何终端关联的长期运行的进程. 如 <code>sshd, nginx</code>等</p>
<ul>
<li>不是一个内核概念, 而是一个用户空间的编程的概念</li>
<li>经典的创建过程: double-fork<ul>
<li><code>fork()</code> - 创建子进程</li>
<li>父进程退出 - 子进程被托孤给init</li>
<li>setsid - 创建新的session (和原终端解绑)</li>
<li>再次 <code>fork()</code> - 确保不会重新获得中断控制能力</li>
<li>chdir(“&#x2F;) - 避免占用可卸载的文件系统</li>
<li>关闭&#x2F;重定向 stdin&#x2F;stdout&#x2F;stderr</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="进程的唯一ID"><a href="#进程的唯一ID" class="headerlink" title="进程的唯一ID"></a>进程的唯一ID</h3><p><strong>tgid和pid, 为什么要有两个ID</strong><br>经典的用途是区分进程和线程, 本身是进程组概念, 父进程和他fork出来的子进程组成一个进程组, 共享一个tgid.<br>他们的id (pid, tgid)会是 (1, 1), (2, 1), (3, 1). 对于操作系统来说, 只有进程这个概念, 线程实际上是一个轻量级进程, 对于操作系统的调度来说, 进程和线程是同等地位的实体, 唯一通过PID来作为唯一标识, 而tgid是标识”进程”而非”线程”的唯一标识.</p>
<h3 id="进程树"><a href="#进程树" class="headerlink" title="进程树"></a>进程树</h3><p>通过 <code>task_struct</code> 中的 <code>parent</code>&#x2F;<code>real_parent</code>、<code>children</code>&#x2F;<code>sibling</code> 字段, 所有进程构成一棵<strong>多叉双向树</strong>. Linux 使用侵入式链表(<code>list_head</code>)而非传统多叉树来实现, 使得增删 O(1)、零内存分配、API 全内核复用.</p>
<p>进程树支撑了 <code>wait()</code> 回收、托孤、<code>SIGCHLD</code> 通知等核心操作. 进程树与进程组(PGID)、会话(Session) 共同构成了完整的作业控制体系.</p>
<p><strong>详细内容</strong>: <a href="%E8%BF%9B%E7%A8%8B%E6%A0%91.md">进程树</a></p>
<h2 id="进程调度"><a href="#进程调度" class="headerlink" title="进程调度"></a>进程调度</h2><h3 id="运行队列-struct-rq"><a href="#运行队列-struct-rq" class="headerlink" title="运行队列 struct rq"></a>运行队列 <code>struct rq</code></h3><p>内核声明了一个per-CPU变量: <code>runqueues</code>, 每个CPU都有自己的rq变量, 通过<code>cpu_rq(cpu)</code>等宏能执行拿到第N号CPU的运行队列指针等操作.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">DECLARE_PER_CPU_SHARED_ALIGNED(<span class="keyword">struct</span> rq, runqueues);</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> cpu_rq(cpu)		(&amp;per_cpu(runqueues, (cpu)))</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> this_rq()		this_cpu_ptr(&amp;runqueues)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> task_rq(p)		cpu_rq(task_cpu(p))</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> cpu_curr(cpu)		(cpu_rq(cpu)-&gt;curr)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> raw_rq()		raw_cpu_ptr(&amp;runqueues)</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>这个数据结构嵌入了<code>cfs_rq</code>, <code>rt_rq</code>, <code>dl_rq</code>三个内部运行队列(注意这里是嵌入, 这三个字段不是指针). 这意味着 rq本身就包含了所有调度类各自的运行队列数据. 每个调度类管自己的调度队列.</p>
</blockquote>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rq</span> &#123;</span></span><br><span class="line">	<span class="type">raw_spinlock_t</span>		__lock; <span class="comment">// 自旋锁, 保护整个队列的并发访问</span></span><br><span class="line"></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> __<span class="title">rcu</span>	*<span class="title">curr</span>;</span> <span class="comment">// 当前正在运行的进程</span></span><br><span class="line"></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">cfs_rq</span>		<span class="title">cfs</span>;</span> <span class="comment">// cfs rq, 完全公平调度队列, 直接嵌入</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">rt_rq</span>		<span class="title">rt</span>;</span> <span class="comment">// real time rq, 实时调度队列, 直接嵌入</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">dl_rq</span>		<span class="title">dl</span>;</span> <span class="comment">// deadline rq, 强实时调度队列, 有完成截止时间, 直接嵌入</span></span><br><span class="line"></span><br><span class="line">	<span class="type">unsigned</span> <span class="type">int</span>		nr_running; <span class="comment">// 一个CPU上最多有多少进程可以是running状态</span></span><br><span class="line">	u64			nr_switches; <span class="comment">// 上下文切换的次数</span></span><br><span class="line">  <span class="comment">// 其他...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="调度类-struct-sched-class"><a href="#调度类-struct-sched-class" class="headerlink" title="调度类 - struct sched_class"></a>调度类 - <code>struct sched_class</code></h3><p>调度类本身是一个虚函数表, 里面定义了一系列的虚函数(接口), 里面的各个函数实际上是函数指针. 可以把调度类就看作是一个interface&#x2F;vitrual class.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sched_class</span> &#123;</span></span><br><span class="line">  <span class="comment">// 把一个进程加入到该调度类的子队列</span></span><br><span class="line">	<span class="type">void</span> (*enqueue_task) (<span class="keyword">struct</span> rq *rq, <span class="keyword">struct</span> task_struct *p, <span class="type">int</span> flags);</span><br><span class="line">  <span class="comment">// 新进程唤醒的时候, 检查是否应该要抢占当前进程</span></span><br><span class="line">	<span class="type">void</span> (*check_preempt_curr)(<span class="keyword">struct</span> rq *rq, <span class="keyword">struct</span> task_struct *p, <span class="type">int</span> flags);</span><br><span class="line">  <span class="comment">// 把一个进程移除到该调度类的子队列</span></span><br><span class="line">	<span class="type">void</span> (*dequeue_task) (<span class="keyword">struct</span> rq *rq, <span class="keyword">struct</span> task_struct *p, <span class="type">int</span> flags);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 从该调度类中选出下一个要运行的进程</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *(*<span class="title">pick_next_task</span>)(<span class="keyword">struct</span> <span class="title">rq</span> *<span class="title">rq</span>);</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 当前进程被换下前的清理工作, 比如对于cfs调度器来说, 就会重新计算vruntime, 然后重新放回cfs.timeline(红黑树)里面</span></span><br><span class="line">	<span class="type">void</span> (*put_prev_task)(<span class="keyword">struct</span> rq *rq, <span class="keyword">struct</span> task_struct *p);</span><br><span class="line">  <span class="comment">// 新进程被换上前的初始化工作</span></span><br><span class="line">	<span class="type">void</span> (*set_next_task)(<span class="keyword">struct</span> rq *rq, <span class="keyword">struct</span> task_struct *p, <span class="type">bool</span> first);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 每个时钟tick时被调用, 检查时间片/vruntime</span></span><br><span class="line">	<span class="type">void</span> (*task_tick)(<span class="keyword">struct</span> rq *rq, <span class="keyword">struct</span> task_struct *p, <span class="type">int</span> queued);</span><br><span class="line">  <span class="comment">// 更新当前进程的运行时间</span></span><br><span class="line">  <span class="type">void</span> (*update_curr)(<span class="keyword">struct</span> rq *rq);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>调度类的实现类有五个</p>
<ol>
<li>stop_sched_class</li>
<li>dl_sched_class</li>
<li>rt_sched_class</li>
<li>fair_sched_class</li>
<li>idle_sched_class</li>
</ol>
<p>这五个类实现了上面的各个函数, 使用不同的策略和子队列.<br>每个task_struct里面都一个sched_class字段, 指向这个进程对应的调度类全局实例(看成是静态类即可)</p>
<p>在rq挑选下一个任务的时候, 会按照上面的顺序遍历所有的调度类, 尝试调用他们各自的pick_next_task.<br>如果调度到的不是NULL, 返回找到的任务.</p>
<h3 id="调度的整体运行流程"><a href="#调度的整体运行流程" class="headerlink" title="调度的整体运行流程"></a>调度的整体运行流程</h3><blockquote>
<p><code>rq</code>是容器, <code>sched_class</code>是行为, 子队列 (cfs_rq, rt_rq, dl_rq) 是各调度类在容器内的私有存储队列. 三者通过<code>task_struct.sched_class</code>桥接</p>
</blockquote>
<p><strong>1. 进程入队: 从唤醒到进入到子队列里面</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">try_wake_up(task_struct* p)</span><br><span class="line">  -&gt; activate_task(rq, p, flags)</span><br><span class="line">    -&gt; enqueue_task(rq, p, flags)</span><br><span class="line">      -&gt; p-&gt;sched_class-&gt;enqueue_task</span><br></pre></td></tr></table></figure>
<ul>
<li>如果这里的进程是普通的进程, 这里的p-&gt;sched_class就是fair_sched_class,  p-&gt;sched_class-&gt;enqueue_task最后调用的就是enqueue_task_fair(), 这个函数会将p插入到cfs rq中的红黑树子队列中</li>
<li>如果p是RT进程, 实际调用的就是enqueue_task_rt()</li>
</ul>
<p><strong>2. 进程出队: 睡眠或退出</strong></p>
<p>__schedule()<br>  -&gt; deactivate_task(rq, prev, DEQUEUE_SLEEP)<br>    -&gt; dequeue_task(rq, prev, flags)<br>      -&gt; p-&gt;sched_class-&gt;dequeue_task(rq, p, flags)</p>
<p><strong>3. 选择下一个进程: pick_next_task的两条路径</strong></p>
<p><em>这里的pick_next_task并不是调度类里面的这个函数, 而是sched.h里面的一个文件</em></p>
<p>__scheduler()<br>  -&gt; pick_next_task(rq, prev, &amp;rf)<br>    -&gt; __pick_next_task(rq, prev, rf)</p>
<p><strong>快速路径</strong></p>
<p>rq-&gt;nr_running &#x3D;&#x3D; rq-&gt;cfs.h_nr_running ? (全部进程都是normal&#x2F;CFS进程)<br>  -&gt; pick_next_task_fair(rq, prev, rf)<br>    -&gt; 从rq-&gt;cfs.tasks_timeline 取出来最左边的节点</p>
<p><strong>慢速路径</strong></p>
<p>for_each_class(class)<br>  class-&gt;pick_next_task(rq)<br>  -&gt; stop_sched_class.pick_next_task(rq) -&gt; 检查 rq-&gt;stop<br>  -&gt; dl_sched_class.pick_next_task(rq)   -&gt; 检查 rq-&gt;dl 红黑树<br>  -&gt; rt_sched_class.pick_next_task(rq)   -&gt; 检查 rq-&gt;rt 优先级数组<br>  -&gt; fair_sched_class.pick_next_task(rq) -&gt; 检查 rq-&gt;cfs 红黑树<br>  -&gt; idle_sched_class.pick_next_task(rq) -&gt; 返回 rq-&gt;idle（兜底）</p>
<p>for_each_class是一个宏, 会遍历所有的sched_class, 尝试调用每个调度类自己实现的pick_next_task, 直到返回的不是NULL, 返回. 这样就构造出来了不同rq&#x2F;调度器之间的优先级问题.</p>
<h3 id="将task-struct挂载在运行队列上"><a href="#将task-struct挂载在运行队列上" class="headerlink" title="将task_struct挂载在运行队列上"></a>将task_struct挂载在运行队列上</h3><p><em>这里主要聚焦的是, rq中的子队列, 比如cfs, 是把task_struct直接存在里面的吗, 是怎么挂载上去的?</em></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span>&#123;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">sched_entity</span>		<span class="title">se</span>;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">sched_rt_entity</span>		<span class="title">rt</span>;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">sched_dl_entity</span>		<span class="title">dl</span>;</span></span><br><span class="line">	<span class="type">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">sched_class</span>	*<span class="title">sched_class</span>;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>同样是侵入式数据结构设计, 通过将这几个嵌入式的字段挂载在task_struct中, 通过container_of来取出task_struct.<br>同时rb_node嵌入到sched_entity中, 来将这个数据结构挂在红黑树上</p>
<p><em>为什么不直接操作task_struct呢?</em></p>
<p>在cgroup分组调度下, CFS需要把“一组进程”当成一个整体来调度, sched_entity可以代表一个进程, 也可以代表一个进程组.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sched_entity</span> &#123;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">load_weight</span>		<span class="title">load</span>;</span> <span class="comment">// 该实体的权重, 通过nice值转化出来的, CFS通过这个计算出来应该分到多少CPU时间</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">rb_node</span>			<span class="title">run_node</span>;</span> <span class="comment">//  红黑树节点, 挂在rq.cfs_rq-&gt;task_timeline上</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">list_head</span>		<span class="title">group_node</span>;</span> <span class="comment">// 挂在rq-&gt;cfs_tasks链表上, 用于SMP均衡负载</span></span><br><span class="line">	<span class="type">unsigned</span> <span class="type">int</span>			on_rq; <span class="comment">// 是否在运行队列里面</span></span><br><span class="line"></span><br><span class="line">	u64				exec_start; <span class="comment">// 当前这一轮开始执行的时间戳, 每次tick或调度的时候更新</span></span><br><span class="line">	u64				sum_exec_runtime; <span class="comment">// 累计实际的运行时间</span></span><br><span class="line">	u64				vruntime; <span class="comment">// 虚拟运行时间, 值越小越优先被选中</span></span><br><span class="line">	u64				prev_sum_exec_runtime; <span class="comment">// 上次调度时候实际运行时间的快照, 用来算, 这次用了多少的时间片</span></span><br><span class="line">	u64				nr_migrations; <span class="comment">// 这个entity被迁移到其他的cpu上的次数</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>sched_entity 是 CFS 调度器的操作单元，嵌入在 task_struct.se 中。它通过 run_node 挂在 cfs_rq 的红黑树上，以 vruntime 为排序键。CFS 选人就是取 vruntime 最小的 sched_entity，再通过 container_of 反推到 task_struct。分组调度下，一个 sched_entity 可以代表整个 task_group，通过 parent&#x2F;cfs_rq&#x2F;my_q 构成层级树。</p>
</blockquote>
]]></content>
      <categories>
        <category>OS</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>进程管理</tag>
        <tag>task_struct</tag>
      </tags>
  </entry>
  <entry>
    <title>算法刷题目录</title>
    <url>/2026/03/30/algorithm/Algorithm-Index/</url>
    <content><![CDATA[<h2 id="题目统计"><a href="#题目统计" class="headerlink" title="题目统计"></a>题目统计</h2><table>
<thead>
<tr>
<th>分类</th>
<th>数量</th>
<th>占比</th>
</tr>
</thead>
<tbody><tr>
<td><strong>array</strong></td>
<td>13</td>
<td>15.12%</td>
</tr>
<tr>
<td><strong>bfs</strong></td>
<td>2</td>
<td>2.33%</td>
</tr>
<tr>
<td><strong>binary_search</strong></td>
<td>3</td>
<td>3.49%</td>
</tr>
<tr>
<td><strong>dfs</strong></td>
<td>7</td>
<td>8.14%</td>
</tr>
<tr>
<td><strong>dp</strong></td>
<td>17</td>
<td>19.77%</td>
</tr>
<tr>
<td><strong>graph</strong></td>
<td>1</td>
<td>1.16%</td>
</tr>
<tr>
<td><strong>greedy</strong></td>
<td>1</td>
<td>1.16%</td>
</tr>
<tr>
<td><strong>linkedlist</strong></td>
<td>14</td>
<td>16.28%</td>
</tr>
<tr>
<td><strong>math</strong></td>
<td>1</td>
<td>1.16%</td>
</tr>
<tr>
<td><strong>sliding_window</strong></td>
<td>4</td>
<td>4.65%</td>
</tr>
<tr>
<td><strong>stack</strong></td>
<td>1</td>
<td>1.16%</td>
</tr>
<tr>
<td><strong>string</strong></td>
<td>10</td>
<td>11.63%</td>
</tr>
<tr>
<td><strong>structure</strong></td>
<td>5</td>
<td>5.81%</td>
</tr>
<tr>
<td><strong>tree</strong></td>
<td>4</td>
<td>4.65%</td>
</tr>
<tr>
<td><strong>two_pointers</strong></td>
<td>3</td>
<td>3.49%</td>
</tr>
</tbody></table>
<h2 id="array"><a href="#array" class="headerlink" title="array"></a>array</h2><ul>
<li><a href="/algorithm/121.%E4%B9%B0%E5%8D%96%E8%82%A1%E7%A5%A8%E7%9A%84%E6%9C%80%E4%BD%B3%E6%97%B6%E6%9C%BA/">121.买卖股票的最佳时机</a></li>
<li><a href="/algorithm/1262.%E5%8F%AF%E8%A2%AB%E4%B8%89%E6%95%B4%E9%99%A4%E7%9A%84%E6%9C%80%E5%A4%A7%E5%92%8C/">1262.可被三整除的最大和</a></li>
<li><a href="/algorithm/16.%E6%9C%80%E6%8E%A5%E8%BF%91%E7%9A%84%E4%B8%89%E6%95%B0%E4%B9%8B%E5%92%8C/">16.最接近的三数之和</a></li>
<li><a href="/algorithm/215.%E6%95%B0%E7%BB%84%E4%B8%AD%E7%9A%84%E7%AC%ACk%E4%B8%AA%E6%9C%80%E5%A4%A7%E5%85%83%E7%B4%A0/">215.数组中的第k个最大元素</a></li>
<li><a href="/algorithm/2946.%E5%BE%AA%E7%8E%AF%E7%A7%BB%E4%BD%8D%E5%90%8E%E7%9A%84%E7%9F%A9%E9%98%B5%E7%9B%B8%E4%BC%BC%E6%A3%80%E6%9F%A5/">2946.循环移位后的矩阵相似检查</a></li>
<li><a href="/algorithm/31.%E4%B8%8B%E4%B8%80%E4%B8%AA%E6%8E%92%E5%88%97/">31.下一个排列</a></li>
<li><a href="/algorithm/41.%E7%BC%BA%E5%A4%B1%E7%9A%84%E7%AC%AC%E4%B8%80%E4%B8%AA%E6%AD%A3%E6%95%B0/">41.缺失的第一个正数</a></li>
<li><a href="/algorithm/54.%E8%9E%BA%E6%97%8B%E7%9F%A9%E9%98%B5/">54.螺旋矩阵</a></li>
<li><a href="/algorithm/56.%E5%90%88%E5%B9%B6%E5%8C%BA%E9%97%B4/">56.合并区间</a></li>
<li><a href="/algorithm/560.%E5%92%8C%E4%B8%BA-k-%E7%9A%84%E5%AD%90%E6%95%B0%E7%BB%84/">560.和为-k-的子数组</a></li>
<li><a href="/algorithm/704.%E4%BA%8C%E5%88%86%E6%9F%A5%E6%89%BE/">704.二分查找</a></li>
<li><a href="/algorithm/88.%E5%90%88%E5%B9%B6%E4%B8%A4%E4%B8%AA%E6%9C%89%E5%BA%8F%E6%95%B0%E7%BB%84/">88.合并两个有序数组</a></li>
<li><a href="/algorithm/912.%E6%8E%92%E5%BA%8F%E6%95%B0%E7%BB%84/">912.排序数组</a></li>
</ul>
<h2 id="bfs"><a href="#bfs" class="headerlink" title="bfs"></a>bfs</h2><ul>
<li><a href="/algorithm/102.%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E5%B1%82%E5%BA%8F%E9%81%8D%E5%8E%86/">102.二叉树的层序遍历</a></li>
<li><a href="/algorithm/103.%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E9%94%AF%E9%BD%BF%E5%BD%A2%E5%B1%82%E5%BA%8F%E9%81%8D%E5%8E%86/">103.二叉树的锯齿形层序遍历</a></li>
</ul>
<h2 id="binary-search"><a href="#binary-search" class="headerlink" title="binary_search"></a>binary_search</h2><ul>
<li><a href="/algorithm/34.%E5%9C%A8%E6%8E%92%E5%BA%8F%E6%95%B0%E7%BB%84%E4%B8%AD%E6%9F%A5%E6%89%BE%E5%85%83%E7%B4%A0%E7%9A%84%E7%AC%AC%E4%B8%80%E4%B8%AA%E5%92%8C%E6%9C%80%E5%90%8E%E4%B8%80%E4%B8%AA%E4%BD%8D%E7%BD%AE/">34.在排序数组中查找元素的第一个和最后一个位置</a></li>
<li><a href="/algorithm/4.%E5%AF%BB%E6%89%BE%E4%B8%A4%E4%B8%AA%E6%AD%A3%E5%BA%8F%E6%95%B0%E7%BB%84%E7%9A%84%E4%B8%AD%E4%BD%8D%E6%95%B0/">4.寻找两个正序数组的中位数</a></li>
<li><a href="/algorithm/69.x-%E7%9A%84%E5%B9%B3%E6%96%B9%E6%A0%B9/">69.x-的平方根</a></li>
</ul>
<h2 id="dfs"><a href="#dfs" class="headerlink" title="dfs"></a>dfs</h2><ul>
<li><a href="/algorithm/200.%E5%B2%9B%E5%B1%BF%E6%95%B0%E9%87%8F/">200.岛屿数量</a></li>
<li><a href="/algorithm/22.%E6%8B%AC%E5%8F%B7%E7%94%9F%E6%88%90/">22.括号生成</a></li>
<li><a href="/algorithm/39.%E7%BB%84%E5%90%88%E6%80%BB%E5%92%8C/">39.组合总和</a></li>
<li><a href="/algorithm/40.%E7%BB%84%E5%90%88%E6%80%BB%E5%92%8C-ii/">40.组合总和-ii</a></li>
<li><a href="/algorithm/46.%E5%85%A8%E6%8E%92%E5%88%97/">46.全排列</a></li>
<li><a href="/algorithm/695.%E5%B2%9B%E5%B1%BF%E7%9A%84%E6%9C%80%E5%A4%A7%E9%9D%A2%E7%A7%AF/">695.岛屿的最大面积</a></li>
<li><a href="/algorithm/79.%E5%8D%95%E8%AF%8D%E6%90%9C%E7%B4%A2/">79.单词搜索</a></li>
</ul>
<h2 id="dp"><a href="#dp" class="headerlink" title="dp"></a>dp</h2><ul>
<li><a href="/algorithm/1143.%E6%9C%80%E9%95%BF%E5%85%AC%E5%85%B1%E5%AD%90%E5%BA%8F%E5%88%97/">1143.最长公共子序列</a></li>
<li><a href="/algorithm/122.%E4%B9%B0%E5%8D%96%E8%82%A1%E7%A5%A8%E7%9A%84%E6%9C%80%E4%BD%B3%E6%97%B6%E6%9C%BA-ii/">122.买卖股票的最佳时机-ii</a></li>
<li><a href="/algorithm/152.%E4%B9%98%E7%A7%AF%E6%9C%80%E5%A4%A7%E5%AD%90%E6%95%B0%E7%BB%84/">152.乘积最大子数组</a></li>
<li><a href="/algorithm/198.%E6%89%93%E5%AE%B6%E5%8A%AB%E8%88%8D/">198.打家劫舍</a></li>
<li><a href="/algorithm/213.%E6%89%93%E5%AE%B6%E5%8A%AB%E8%88%8D-ii/">213.打家劫舍-ii</a></li>
<li><a href="/algorithm/300.%E6%9C%80%E9%95%BF%E9%80%92%E5%A2%9E%E5%AD%90%E5%BA%8F%E5%88%97/">300.最长递增子序列</a></li>
<li><a href="/algorithm/322.%E9%9B%B6%E9%92%B1%E5%85%91%E6%8D%A2(%E9%80%92%E5%BD%92)/">322.零钱兑换(递归)</a></li>
<li><a href="/algorithm/322.%E9%9B%B6%E9%92%B1%E5%85%91%E6%8D%A2(%E9%80%92%E6%8E%A8)/">322.零钱兑换(递推)</a></li>
<li><a href="/algorithm/416.%E5%88%86%E5%89%B2%E7%AD%89%E5%92%8C%E5%AD%90%E9%9B%86/">416.分割等和子集</a></li>
<li><a href="/algorithm/516.%E6%9C%80%E9%95%BF%E5%9B%9E%E6%96%87%E5%AD%90%E5%BA%8F%E5%88%97/">516.最长回文子序列</a></li>
<li><a href="/algorithm/53.%E6%9C%80%E5%A4%A7%E5%AD%90%E6%95%B0%E7%BB%84%E5%92%8C/">53.最大子数组和</a></li>
<li><a href="/algorithm/64.%E6%9C%80%E5%B0%8F%E8%B7%AF%E5%BE%84%E5%92%8C(%E9%80%92%E5%BD%92)/">64.最小路径和(递归)</a></li>
<li><a href="/algorithm/64.%E6%9C%80%E5%B0%8F%E8%B7%AF%E5%BE%84%E5%92%8C(%E9%80%92%E6%8E%A8)/">64.最小路径和(递推)</a></li>
<li><a href="/algorithm/70.%E7%88%AC%E6%A5%BC%E6%A2%AF/">70.爬楼梯</a></li>
<li><a href="/algorithm/72.%E7%BC%96%E8%BE%91%E8%B7%9D%E7%A6%BB(%E9%80%92%E5%BD%92)/">72.编辑距离(递归)</a></li>
<li><a href="/algorithm/72.%E7%BC%96%E8%BE%91%E8%B7%9D%E7%A6%BB(%E9%80%92%E6%8E%A8)/">72.编辑距离(递推)</a></li>
<li><a href="/algorithm/97.%E4%BA%A4%E9%94%99%E5%AD%97%E7%AC%A6%E4%B8%B2/">97.交错字符串</a></li>
</ul>
<h2 id="graph"><a href="#graph" class="headerlink" title="graph"></a>graph</h2><ul>
<li><a href="/algorithm/207.%E8%AF%BE%E7%A8%8B%E8%A1%A8/">207.课程表</a></li>
</ul>
<h2 id="greedy"><a href="#greedy" class="headerlink" title="greedy"></a>greedy</h2><ul>
<li><a href="/algorithm/2573.%E6%89%BE%E5%87%BA%E5%AF%B9%E5%BA%94-lcp-%E7%9F%A9%E9%98%B5%E7%9A%84%E5%AD%97%E7%AC%A6%E4%B8%B2/">2573.找出对应-lcp-矩阵的字符串</a></li>
</ul>
<h2 id="linkedlist"><a href="#linkedlist" class="headerlink" title="linkedlist"></a>linkedlist</h2><ul>
<li><a href="/algorithm/141.%E7%8E%AF%E5%BD%A2%E9%93%BE%E8%A1%A8/">141.环形链表</a></li>
<li><a href="/algorithm/142.%E7%8E%AF%E5%BD%A2%E9%93%BE%E8%A1%A8-ii/">142.环形链表-ii</a></li>
<li><a href="/algorithm/143.%E9%87%8D%E6%8E%92%E9%93%BE%E8%A1%A8/">143.重排链表</a></li>
<li><a href="/algorithm/148.%E6%8E%92%E5%BA%8F%E9%93%BE%E8%A1%A8/">148.排序链表</a></li>
<li><a href="/algorithm/160.%E7%9B%B8%E4%BA%A4%E9%93%BE%E8%A1%A8/">160.相交链表</a></li>
<li><a href="/algorithm/19.%E5%88%A0%E9%99%A4%E9%93%BE%E8%A1%A8%E7%9A%84%E5%80%92%E6%95%B0%E7%AC%AC-n-%E4%B8%AA%E7%BB%93%E7%82%B9/">19.删除链表的倒数第-n-个结点</a></li>
<li><a href="/algorithm/2.%E4%B8%A4%E6%95%B0%E7%9B%B8%E5%8A%A0/">2.两数相加</a></li>
<li><a href="/algorithm/206.%E5%8F%8D%E8%BD%AC%E9%93%BE%E8%A1%A8/">206.反转链表</a></li>
<li><a href="/algorithm/21.%E5%90%88%E5%B9%B6%E4%B8%A4%E4%B8%AA%E6%9C%89%E5%BA%8F%E9%93%BE%E8%A1%A8/">21.合并两个有序链表</a></li>
<li><a href="/algorithm/234.%E5%9B%9E%E6%96%87%E9%93%BE%E8%A1%A8/">234.回文链表</a></li>
<li><a href="/algorithm/24.%E4%B8%A4%E4%B8%A4%E4%BA%A4%E6%8D%A2%E9%93%BE%E8%A1%A8%E4%B8%AD%E7%9A%84%E8%8A%82%E7%82%B9/">24.两两交换链表中的节点</a></li>
<li><a href="/algorithm/25.k-%E4%B8%AA%E4%B8%80%E7%BB%84%E7%BF%BB%E8%BD%AC%E9%93%BE%E8%A1%A8/">25.k-个一组翻转链表</a></li>
<li><a href="/algorithm/82.%E5%88%A0%E9%99%A4%E6%8E%92%E5%BA%8F%E9%93%BE%E8%A1%A8%E4%B8%AD%E7%9A%84%E9%87%8D%E5%A4%8D%E5%85%83%E7%B4%A0-ii/">82.删除排序链表中的重复元素-ii</a></li>
<li><a href="/algorithm/92.%E5%8F%8D%E8%BD%AC%E9%93%BE%E8%A1%A8-ii/">92.反转链表-ii</a></li>
</ul>
<h2 id="math"><a href="#math" class="headerlink" title="math"></a>math</h2><ul>
<li><a href="/algorithm/470.%E7%94%A8-rand-7-%E5%AE%9E%E7%8E%B0-rand-10/">470.用-rand-7-实现-rand-10</a></li>
</ul>
<h2 id="sliding-window"><a href="#sliding-window" class="headerlink" title="sliding_window"></a>sliding_window</h2><ul>
<li><a href="/algorithm/209.%E9%95%BF%E5%BA%A6%E6%9C%80%E5%B0%8F%E7%9A%84%E5%AD%90%E6%95%B0%E7%BB%84/">209.长度最小的子数组</a></li>
<li><a href="/algorithm/239.%E6%BB%91%E5%8A%A8%E7%AA%97%E5%8F%A3%E6%9C%80%E5%A4%A7%E5%80%BC/">239.滑动窗口最大值</a></li>
<li><a href="/algorithm/3.%E6%97%A0%E9%87%8D%E5%A4%8D%E5%AD%97%E7%AC%A6%E7%9A%84%E6%9C%80%E9%95%BF%E5%AD%90%E4%B8%B2/">3.无重复字符的最长子串</a></li>
<li><a href="/algorithm/76.%E6%9C%80%E5%B0%8F%E8%A6%86%E7%9B%96%E5%AD%90%E4%B8%B2/">76.最小覆盖子串</a></li>
</ul>
<h2 id="stack"><a href="#stack" class="headerlink" title="stack"></a>stack</h2><ul>
<li><a href="/algorithm/20.%E6%9C%89%E6%95%88%E7%9A%84%E6%8B%AC%E5%8F%B7/">20.有效的括号</a></li>
</ul>
<h2 id="string"><a href="#string" class="headerlink" title="string"></a>string</h2><ul>
<li><a href="/algorithm/14.%E6%9C%80%E9%95%BF%E5%85%AC%E5%85%B1%E5%89%8D%E7%BC%80/">14.最长公共前缀</a></li>
<li><a href="/algorithm/151.%E5%8F%8D%E8%BD%AC%E5%AD%97%E7%AC%A6%E4%B8%B2%E4%B8%AD%E7%9A%84%E5%8D%95%E8%AF%8D/">151.反转字符串中的单词</a></li>
<li><a href="/algorithm/165.%E6%AF%94%E8%BE%83%E7%89%88%E6%9C%AC%E5%8F%B7/">165.比较版本号</a></li>
<li><a href="/algorithm/179.%E6%9C%80%E5%A4%A7%E6%95%B0/">179.最大数</a></li>
<li><a href="/algorithm/32.%E6%9C%80%E9%95%BF%E6%9C%89%E6%95%88%E6%8B%AC%E5%8F%B7/">32.最长有效括号</a></li>
<li><a href="/algorithm/394.%E5%AD%97%E7%AC%A6%E4%B8%B2%E8%A7%A3%E7%A0%81/">394.字符串解码</a></li>
<li><a href="/algorithm/415.%E5%AD%97%E7%AC%A6%E4%B8%B2%E7%9B%B8%E5%8A%A0/">415.字符串相加</a></li>
<li><a href="/algorithm/43.%E5%AD%97%E7%AC%A6%E4%B8%B2%E7%9B%B8%E4%B9%98/">43.字符串相乘</a></li>
<li><a href="/algorithm/5.%E6%9C%80%E9%95%BF%E5%9B%9E%E6%96%87%E5%AD%90%E4%B8%B2/">5.最长回文子串</a></li>
<li><a href="/algorithm/93.%E5%A4%8D%E5%8E%9F-ip-%E5%9C%B0%E5%9D%80/">93.复原-ip-地址</a></li>
</ul>
<h2 id="structure"><a href="#structure" class="headerlink" title="structure"></a>structure</h2><ul>
<li><a href="/algorithm/1.%E4%B8%A4%E6%95%B0%E4%B9%8B%E5%92%8C/">1.两数之和</a></li>
<li><a href="/algorithm/128.%E6%9C%80%E9%95%BF%E8%BF%9E%E7%BB%AD%E5%BA%8F%E5%88%97/">128.最长连续序列</a></li>
<li><a href="/algorithm/146.lru-%E7%BC%93%E5%AD%98/">146.lru-缓存</a></li>
<li><a href="/algorithm/232.%E7%94%A8%E6%A0%88%E5%AE%9E%E7%8E%B0%E9%98%9F%E5%88%97/">232.用栈实现队列</a></li>
<li><a href="/algorithm/2840.%E5%88%A4%E6%96%AD%E9%80%9A%E8%BF%87%E6%93%8D%E4%BD%9C%E8%83%BD%E5%90%A6%E8%AE%A9%E5%AD%97%E7%AC%A6%E4%B8%B2%E7%9B%B8%E7%AD%89-ii/">2840.判断通过操作能否让字符串相等-ii</a></li>
</ul>
<h2 id="tree"><a href="#tree" class="headerlink" title="tree"></a>tree</h2><ul>
<li><a href="/algorithm/124.%E4%BA%8C%E5%8F%89%E6%A0%91%E4%B8%AD%E7%9A%84%E6%9C%80%E5%A4%A7%E8%B7%AF%E5%BE%84%E5%92%8C/">124.二叉树中的最大路径和</a></li>
<li><a href="/algorithm/199.%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E5%8F%B3%E8%A7%86%E5%9B%BE/">199.二叉树的右视图</a></li>
<li><a href="/algorithm/236.%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E6%9C%80%E8%BF%91%E5%85%AC%E5%85%B1%E7%A5%96%E5%85%88/">236.二叉树的最近公共祖先</a></li>
<li><a href="/algorithm/662.%E4%BA%8C%E5%8F%89%E6%A0%91%E6%9C%80%E5%A4%A7%E5%AE%BD%E5%BA%A6/">662.二叉树最大宽度</a></li>
</ul>
<h2 id="two-pointers"><a href="#two-pointers" class="headerlink" title="two_pointers"></a>two_pointers</h2><ul>
<li><a href="/algorithm/15.%E4%B8%89%E6%95%B0%E4%B9%8B%E5%92%8C/">15.三数之和</a></li>
<li><a href="/algorithm/283.%E7%A7%BB%E5%8A%A8%E9%9B%B6/">283.移动零</a></li>
<li><a href="/algorithm/42.%E6%8E%A5%E9%9B%A8%E6%B0%B4/">42.接雨水</a></li>
</ul>
]]></content>
      <categories>
        <category>算法</category>
      </categories>
      <tags>
        <tag>Index</tag>
      </tags>
  </entry>
  <entry>
    <title>Kafka 学习主干道</title>
    <url>/2026/04/01/kafka/index/</url>
    <content><![CDATA[<h1 id="Kafka-学习主干道"><a href="#Kafka-学习主干道" class="headerlink" title="Kafka 学习主干道"></a>Kafka 学习主干道</h1><h2 id="核心抽象"><a href="#核心抽象" class="headerlink" title="核心抽象"></a>核心抽象</h2><p>Kafka 是一个<strong>持久化的分布式日志系统</strong>，所有设计围绕一个核心约束展开：</p>
<blockquote>
<p><strong>消息是不可变的、有序的、可重放的日志，消费者通过 offset 自主控制读取位置</strong></p>
</blockquote>
<p>从这个约束出发，可以推导出它的所有设计决策：</p>
<table>
<thead>
<tr>
<th>抽象</th>
<th>一句话</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Topic &#x2F; Partition</strong></td>
<td>日志的逻辑分组与物理分片，分片是并发和扩展的基本单位</td>
</tr>
<tr>
<td><strong>Offset</strong></td>
<td>消费者在分区日志中的位置指针，由消费者自己管理</td>
</tr>
<tr>
<td><strong>Consumer Group</strong></td>
<td>一组消费者共同消费一个 topic，每个分区只被组内一个消费者消费</td>
</tr>
<tr>
<td><strong>Replication &#x2F; ISR</strong></td>
<td>分区副本机制，ISR 决定谁有资格成为 leader</td>
</tr>
<tr>
<td><strong>Producer Acks</strong></td>
<td>生产者对”写入成功”的定义，决定可靠性与延迟的取舍</td>
</tr>
<tr>
<td><strong>Log Segment</strong></td>
<td>分区在磁盘上的物理存储单元，append-only，支持顺序写</td>
</tr>
</tbody></table>
<hr>
<h2 id="主干道节点"><a href="#主干道节点" class="headerlink" title="主干道节点"></a>主干道节点</h2><table>
<thead>
<tr>
<th>#</th>
<th>文件</th>
<th>内容</th>
<th>状态</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td><a href="../notes/01-core-concepts.md">01-core-concepts.md</a></td>
<td>Topic &#x2F; Partition &#x2F; Offset &#x2F; Consumer Group &#x2F; Broker</td>
<td>✅ 已完成</td>
</tr>
<tr>
<td>2</td>
<td><a href="../notes/02-log-and-pull.md">02-log-and-pull.md</a></td>
<td>日志抽象、只追加、全局定序、Pull 模式、长轮询</td>
<td>✅ 已完成</td>
</tr>
<tr>
<td>3</td>
<td><a href="../notes/03-storage.md">03-storage.md</a></td>
<td>Segment 结构、Page Cache、零拷贝、Retention、Log Compaction</td>
<td>✅ 已完成</td>
</tr>
<tr>
<td>4</td>
<td><a href="../notes/04-producer-internals.md">04-producer-internals.md</a></td>
<td>key 路由、批量发送、压缩（已合并入 06）</td>
<td>✅ 已合并</td>
</tr>
<tr>
<td>5</td>
<td><a href="../notes/05-replication.md">05-replication.md</a></td>
<td>Replica、ISR、Leader Election、acks、min.insync.replicas</td>
<td>✅ 已完成</td>
</tr>
<tr>
<td>6</td>
<td><a href="../notes/06-producer.md">06-producer.md</a></td>
<td>路由、批量、压缩、幂等、事务、LSO、Exactly-Once</td>
<td>✅ 已完成</td>
</tr>
<tr>
<td>7</td>
<td><a href="../notes/07-consumer.md">07-consumer.md</a></td>
<td>GC、GL、Rebalance、Static Membership、Offset 提交策略</td>
<td>✅ 已完成</td>
</tr>
<tr>
<td>8</td>
<td><a href="../notes/08-kraft.md">08-kraft.md</a></td>
<td>为什么去掉 ZooKeeper，KRaft 元数据管理</td>
<td>✅ 已完成</td>
</tr>
<tr>
<td>9</td>
<td><a href="../notes/09-engineering.md">09-engineering.md</a></td>
<td>Kafka 在真实系统中的定位与工程取舍</td>
<td>✅ 已完成</td>
</tr>
</tbody></table>
<hr>
<h2 id="节点依赖"><a href="#节点依赖" class="headerlink" title="节点依赖"></a>节点依赖</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">01-core-concepts</span><br><span class="line">    ├── 02-storage-design</span><br><span class="line">    ├── 03-replication</span><br><span class="line">    ├── 04-producer</span><br><span class="line">    └── 05-consumer</span><br><span class="line">              └── 06-kraft (可并行)</span><br><span class="line">                      └── 07-engineering</span><br></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>kafka</tag>
        <tag>distributed-systems</tag>
        <tag>messaging</tag>
      </tags>
  </entry>
  <entry>
    <title>LLM 推理优化学习主干道</title>
    <url>/2026/04/01/llm-inference/index/</url>
    <content><![CDATA[<h1 id="LLM-推理优化学习主干道"><a href="#LLM-推理优化学习主干道" class="headerlink" title="LLM 推理优化学习主干道"></a>LLM 推理优化学习主干道</h1><h2 id="前置差距分析"><a href="#前置差距分析" class="headerlink" title="前置差距分析"></a>前置差距分析</h2><blockquote>
<p>分析日期：2026-03-29</p>
</blockquote>
<table>
<thead>
<tr>
<th>前置能力</th>
<th>是否具备</th>
<th>处理方式</th>
</tr>
</thead>
<tbody><tr>
<td>Transformer &#x2F; Attention 基本概念</td>
<td>❌ 缺失</td>
<td>纳入主干道节点 01</td>
</tr>
<tr>
<td>C++ 基础（指针、内存模型、RAII）</td>
<td>❌ 缺失</td>
<td>纳入主干道节点 02-03</td>
</tr>
<tr>
<td>CUDA 执行模型（thread&#x2F;warp&#x2F;block&#x2F;shared memory）</td>
<td>❌ 缺失</td>
<td>纳入主干道节点 04-05</td>
</tr>
<tr>
<td>GPU 内存层级（HBM&#x2F;SRAM&#x2F;带宽瓶颈）</td>
<td>❌ 缺失</td>
<td>纳入主干道节点 06</td>
</tr>
<tr>
<td>LLM 推理系统（KV cache、batching、调度）</td>
<td>❌ 缺失</td>
<td>纳入主干道节点 07-11</td>
</tr>
<tr>
<td>OS 底层（内存管理、进程、调度）</td>
<td>✅ 已具备</td>
<td>节点 06、08 直接复用</td>
</tr>
<tr>
<td>向量化执行引擎经验（Gluten）</td>
<td>⚠️ 部分相关</td>
<td>节点 05、09 作为理解锚点</td>
</tr>
<tr>
<td>Java &#x2F; Spark Infra 经验</td>
<td>✅ 已具备</td>
<td>节点 11 系统设计时复用</td>
</tr>
</tbody></table>
<hr>
<h2 id="核心抽象"><a href="#核心抽象" class="headerlink" title="核心抽象"></a>核心抽象</h2><p>LLM 推理优化是一个 <strong>在有限硬件资源上最大化语言模型服务吞吐与最小化延迟的工程问题</strong>，所有设计围绕一个核心约束展开：</p>
<blockquote>
<p><strong>GPU 显存有限、带宽有限、算力有限——推理优化的本质是在这三个维度上做最优的取舍。</strong></p>
</blockquote>
<p>从这个约束出发，可以推导出它的所有核心设计：</p>
<table>
<thead>
<tr>
<th>抽象</th>
<th>一句话</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Transformer &#x2F; Attention</strong></td>
<td>模型计算的基本单元，推理优化的对象</td>
</tr>
<tr>
<td><strong>C++ &#x2F; CUDA</strong></td>
<td>推理系统的实现语言，所有 kernel 的载体</td>
</tr>
<tr>
<td><strong>KV Cache</strong></td>
<td>用显存换计算，避免重复计算历史 token 的 attention</td>
</tr>
<tr>
<td><strong>Prefill &#x2F; Decode 分离</strong></td>
<td>两个阶段特性不同，需要不同的优化策略</td>
</tr>
<tr>
<td><strong>Batching</strong></td>
<td>把多个请求打包计算，提升 GPU 利用率</td>
</tr>
<tr>
<td><strong>Memory Hierarchy</strong></td>
<td>SRAM &#x2F; HBM &#x2F; DRAM 速度差异决定了 kernel 优化的方向</td>
</tr>
<tr>
<td><strong>Quantization</strong></td>
<td>用精度换显存和速度，是部署侧最常见的压缩手段</td>
</tr>
</tbody></table>
<hr>
<h2 id="主干道节点"><a href="#主干道节点" class="headerlink" title="主干道节点"></a>主干道节点</h2><h3 id="第一层：模型与语言基础（不可跳过的前置）"><a href="#第一层：模型与语言基础（不可跳过的前置）" class="headerlink" title="第一层：模型与语言基础（不可跳过的前置）"></a>第一层：模型与语言基础（不可跳过的前置）</h3><table>
<thead>
<tr>
<th>#</th>
<th>文件</th>
<th>内容</th>
<th>状态</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td><a href="../notes/01-transformer-basics.md">01-transformer-basics.md</a></td>
<td>Transformer 结构与 Attention 计算：模型推理时在算什么</td>
<td>✅ 已完成</td>
</tr>
<tr>
<td>2</td>
<td><a href="../notes/02-cpp-essentials.md">02-cpp-essentials.md</a></td>
<td>C++ 必要基础：指针、内存模型、RAII、智能指针、模板基础</td>
<td>✅ 已完成</td>
</tr>
<tr>
<td>3</td>
<td><a href="../notes/03-cpp-build-system.md">03-cpp-build-system.md</a></td>
<td>C++ 编译模型与 CMake：能独立构建和运行 C++ 项目</td>
<td>✅ 已完成</td>
</tr>
</tbody></table>
<h3 id="第二层：GPU-编程基础"><a href="#第二层：GPU-编程基础" class="headerlink" title="第二层：GPU 编程基础"></a>第二层：GPU 编程基础</h3><table>
<thead>
<tr>
<th>#</th>
<th>文件</th>
<th>内容</th>
<th>状态</th>
</tr>
</thead>
<tbody><tr>
<td>4</td>
<td><a href="../notes/04-cuda-execution-model.md">04-cuda-execution-model.md</a></td>
<td>CUDA 执行模型：thread &#x2F; warp &#x2F; block &#x2F; grid + 第一个 kernel</td>
<td>✅ 已完成</td>
</tr>
<tr>
<td>5</td>
<td><a href="../notes/05-cuda-memory-optimization.md">05-cuda-memory-optimization.md</a></td>
<td>CUDA 内存优化：coalescing &#x2F; shared memory &#x2F; bank conflict &#x2F; GEMM tiling</td>
<td>✅ 已完成</td>
</tr>
<tr>
<td>6</td>
<td><a href="../notes/06-gpu-performance-model.md">06-gpu-performance-model.md</a></td>
<td>GPU 性能模型：HBM&#x2F;SRAM 层级、roofline、用 nsight 分析瓶颈</td>
<td>✅ 已完成</td>
</tr>
</tbody></table>
<h3 id="第三层：推理系统核心"><a href="#第三层：推理系统核心" class="headerlink" title="第三层：推理系统核心"></a>第三层：推理系统核心</h3><table>
<thead>
<tr>
<th>#</th>
<th>文件</th>
<th>内容</th>
<th>状态</th>
</tr>
</thead>
<tbody><tr>
<td>7</td>
<td><a href="../notes/07-autoregressive-generation.md">07-autoregressive-generation.md</a></td>
<td>自回归生成与 KV Cache：token by token 生成机制和显存代价</td>
<td>✅ 已完成</td>
</tr>
<tr>
<td>8</td>
<td><a href="../notes/08-batching-scheduling.md">08-batching-scheduling.md</a></td>
<td>Batching 与调度：static batching vs continuous batching</td>
<td>✅ 已完成</td>
</tr>
<tr>
<td>9</td>
<td><a href="../notes/09-paged-attention.md">09-paged-attention.md</a></td>
<td>PagedAttention 与显存管理：vLLM 的核心设计</td>
<td>⬜ 未开始</td>
</tr>
<tr>
<td>10</td>
<td><a href="../notes/10-flash-attention.md">10-flash-attention.md</a></td>
<td>FlashAttention：IO-aware kernel 优化</td>
<td>⬜ 未开始</td>
</tr>
<tr>
<td>11</td>
<td><a href="../notes/11-quantization.md">11-quantization.md</a></td>
<td>量化：用精度换速度和显存（PTQ &#x2F; INT8 &#x2F; FP8）</td>
<td>⬜ 未开始</td>
</tr>
<tr>
<td>12</td>
<td><a href="../notes/12-inference-system-design.md">12-inference-system-design.md</a></td>
<td>推理系统设计全景：延迟 &#x2F; 吞吐 &#x2F; 成本的工程取舍</td>
<td>⬜ 未开始</td>
</tr>
</tbody></table>
<hr>
<h2 id="节点依赖"><a href="#节点依赖" class="headerlink" title="节点依赖"></a>节点依赖</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">01-transformer-basics（模型基础）</span><br><span class="line">    └── 02-cpp-essentials</span><br><span class="line">            └── 03-cpp-build-system</span><br><span class="line">                    └── 04-cuda-execution-model</span><br><span class="line">                            └── 05-cuda-memory-optimization</span><br><span class="line">                                    └── 06-gpu-performance-model</span><br><span class="line">                                            ├── 07-autoregressive-generation</span><br><span class="line">                                            │       └── 08-batching-scheduling</span><br><span class="line">                                            │               └── 09-paged-attention</span><br><span class="line">                                            └── 10-flash-attention（依赖 05 + 07）</span><br><span class="line">                                                    └── 11-quantization（可并行于 09）</span><br><span class="line">                                                            └── 12-inference-system-design（综合）</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="学习目标"><a href="#学习目标" class="headerlink" title="学习目标"></a>学习目标</h2><p>完成全部节点后，你应该能够：</p>
<ol>
<li>解释 Transformer attention 的完整计算流程</li>
<li>用 C++ 写基本的系统级代码，读懂推理框架的 host 代码</li>
<li>写出正确的 CUDA kernel（GEMM naive → shared memory tiling）</li>
<li>用 nsight 分析 kernel 的 occupancy 和 memory throughput</li>
<li>解释 KV cache 的作用、显存占用和管理策略</li>
<li>解释 continuous batching 为什么比 static batching 高效</li>
<li>解释 PagedAttention 的设计动机和实现思路</li>
<li>解释 FlashAttention 为什么快（IO-aware）</li>
<li>解释量化的基本原理和常见方案</li>
<li>读懂 vLLM 的 scheduler 逻辑和核心 CUDA kernel</li>
</ol>
<hr>
<h2 id="节点详情与实践计划"><a href="#节点详情与实践计划" class="headerlink" title="节点详情与实践计划"></a>节点详情与实践计划</h2><h3 id="节点-01-·-Transformer-结构与-Attention-计算"><a href="#节点-01-·-Transformer-结构与-Attention-计算" class="headerlink" title="节点 01 · Transformer 结构与 Attention 计算"></a>节点 01 · Transformer 结构与 Attention 计算</h3><ul>
<li><strong>核心问题</strong>：模型推理时在算什么？矩阵乘法和 Attention 是什么关系？</li>
<li><strong>参考材料</strong>：<a href="https://jalammar.github.io/illustrated-transformer/">The Illustrated Transformer</a>、<a href="https://arxiv.org/abs/1706.03762">Attention Is All You Need</a></li>
<li><strong>实践</strong>：本机用 Python&#x2F;NumPy 手写 scaled dot-product attention（约 10 行），验证公式</li>
<li><strong>完成标准</strong>：能解释 Q&#x2F;K&#x2F;V 的角色、attention 计算步骤、为什么除以 √d_k</li>
</ul>
<h3 id="节点-02-·-C-必要基础"><a href="#节点-02-·-C-必要基础" class="headerlink" title="节点 02 · C++ 必要基础"></a>节点 02 · C++ 必要基础</h3><ul>
<li><strong>核心问题</strong>：C++ 和 Java 的本质差异？指针、栈堆、RAII 解决了什么问题？</li>
<li><strong>参考材料</strong>：<a href="https://www.learncpp.com/">learncpp.com</a>（Ch9-12 指针&#x2F;引用，Ch15-16 RAII&#x2F;智能指针，Ch26 模板基础）</li>
<li><strong>实践</strong>：本机写 3 个小程序：①手动 malloc&#x2F;free 演示 dangling pointer；②用 unique_ptr 改写；③写一个模板函数</li>
<li><strong>完成标准</strong>：能解释为什么 Java 程序员写 C++ 容易出 UB；能用 CMake 构建运行项目</li>
</ul>
<h3 id="节点-03-·-C-编译模型与-CMake"><a href="#节点-03-·-C-编译模型与-CMake" class="headerlink" title="节点 03 · C++ 编译模型与 CMake"></a>节点 03 · C++ 编译模型与 CMake</h3><ul>
<li><strong>核心问题</strong>：C++ 的编译-链接流程？头文件&#x2F;源文件分离的意义？</li>
<li><strong>参考材料</strong>：<a href="https://www.learncpp.com/cpp-tutorial/introduction-to-the-preprocessor/">learncpp.com Ch7</a>、<a href="https://cmake.org/cmake/help/latest/guide/tutorial/">CMake Tutorial</a></li>
<li><strong>实践</strong>：本机用 CMake 构建多文件项目（.h + .cpp + main），<code>cmake .. &amp;&amp; make</code> 跑通</li>
<li><strong>完成标准</strong>：能解释 .h&#x2F;.cpp 分离的原因；能独立搭建 CMake 项目</li>
</ul>
<h3 id="节点-04-·-CUDA-执行模型"><a href="#节点-04-·-CUDA-执行模型" class="headerlink" title="节点 04 · CUDA 执行模型"></a>节点 04 · CUDA 执行模型</h3><ul>
<li><strong>核心问题</strong>：GPU 和 CPU 的执行模型根本差异？thread&#x2F;warp&#x2F;block&#x2F;grid 如何组织？</li>
<li><strong>参考材料</strong>：<a href="https://docs.nvidia.com/cuda/cuda-c-programming-guide/">CUDA C Programming Guide Ch1-3</a></li>
<li><strong>环境</strong>：AutoDL RTX 3090（选 CUDA 12.x 镜像，按小时计费，用完关机）</li>
<li><strong>实践</strong>：在 AutoDL 上写并运行：①vector add kernel；②改变 block size 观察性能差异</li>
<li><strong>完成标准</strong>：能画出 thread&#x2F;warp&#x2F;block&#x2F;grid 层级图；能解释 warp divergence 是什么</li>
</ul>
<h3 id="节点-05-·-CUDA-内存优化"><a href="#节点-05-·-CUDA-内存优化" class="headerlink" title="节点 05 · CUDA 内存优化"></a>节点 05 · CUDA 内存优化</h3><ul>
<li><strong>核心问题</strong>：为什么 naive kernel 慢？coalescing 和 shared memory tiling 如何解决？</li>
<li><strong>参考材料</strong>：<a href="https://docs.nvidia.com/cuda/cuda-c-programming-guide/">CUDA C Programming Guide Ch5-6</a>、<a href="https://github.com/yzhaiustc/Optimizing-GEMM-on-NVIDIA-Turing-GPUs">how-to-optimize-gemm</a></li>
<li><strong>环境</strong>：AutoDL RTX 3090</li>
<li><strong>实践</strong>：实现三版 GEMM：①naive（global memory）；②shared memory tiling；③对比 GFLOPS</li>
<li><strong>完成标准</strong>：tiling 版比 naive 快 5x+；能解释为什么</li>
</ul>
<h3 id="节点-06-·-GPU-性能模型与分析工具"><a href="#节点-06-·-GPU-性能模型与分析工具" class="headerlink" title="节点 06 · GPU 性能模型与分析工具"></a>节点 06 · GPU 性能模型与分析工具</h3><ul>
<li><strong>核心问题</strong>：kernel 的瓶颈是算力还是带宽？如何用 roofline 判断？nsight 怎么读？</li>
<li><strong>参考材料</strong>：<a href="https://docs.nvidia.com/nsight-compute/">NVIDIA Nsight Compute 文档</a></li>
<li><strong>环境</strong>：AutoDL RTX 3090（nsight-compute 已预装）</li>
<li><strong>实践</strong>：对节点 05 的 GEMM kernel 跑 <code>ncu</code> profile，读 occupancy &#x2F; memory throughput &#x2F; compute throughput</li>
<li><strong>完成标准</strong>：能用 roofline 判断 compute-bound 还是 memory-bound；能解释 occupancy 低的原因</li>
</ul>
<h3 id="节点-07-·-自回归生成与-KV-Cache"><a href="#节点-07-·-自回归生成与-KV-Cache" class="headerlink" title="节点 07 · 自回归生成与 KV Cache"></a>节点 07 · 自回归生成与 KV Cache</h3><ul>
<li><strong>核心问题</strong>：token by token 生成的机制？KV cache 存了什么，为什么能省计算？</li>
<li><strong>参考材料</strong>：<a href="https://blog.vllm.ai/2023/06/20/vllm.html">vLLM 博客</a>、<a href="https://lilianweng.github.io/posts/2023-01-10-inference-optimization/">Lilian Weng: LLM Inference</a></li>
<li><strong>实践</strong>：本机用 Python 写 toy autoregressive decoder，加 KV cache 前后对比计算量；手算 7B 模型的 KV cache 显存占用</li>
<li><strong>完成标准</strong>：能解释 prefill 和 decode 两个阶段为什么特性不同；能手算显存占用</li>
</ul>
<h3 id="节点-08-·-Batching-与调度"><a href="#节点-08-·-Batching-与调度" class="headerlink" title="节点 08 · Batching 与调度"></a>节点 08 · Batching 与调度</h3><ul>
<li><strong>核心问题</strong>：static batching 为什么浪费 GPU？continuous batching 如何解决？</li>
<li><strong>参考材料</strong>：<a href="https://www.usenix.org/conference/osdi22/presentation/yu">Orca 论文</a>、<a href="https://arxiv.org/abs/2309.06180">vLLM 论文</a> Section 3</li>
<li><strong>实践</strong>：阅读 vLLM <code>vllm/core/scheduler.py</code>，能解释 <code>_schedule()</code> 主循环逻辑</li>
<li><strong>完成标准</strong>：能画出 continuous batching 的请求进出图；能解释 iteration-level scheduling</li>
</ul>
<h3 id="节点-09-·-PagedAttention-与显存管理"><a href="#节点-09-·-PagedAttention-与显存管理" class="headerlink" title="节点 09 · PagedAttention 与显存管理"></a>节点 09 · PagedAttention 与显存管理</h3><ul>
<li><strong>核心问题</strong>：KV cache 显存碎片问题是什么？PagedAttention 如何类比 OS 虚拟内存解决？</li>
<li><strong>参考材料</strong>：<a href="https://arxiv.org/abs/2309.06180">vLLM 论文</a>、<a href="https://github.com/vllm-project/vllm/blob/main/vllm/core/block_manager.py">vLLM block_manager 源码</a></li>
<li><strong>环境</strong>：AutoDL（运行 vLLM 观察显存分配）</li>
<li><strong>实践</strong>：阅读 block_manager 源码，解释 physical&#x2F;logical block 映射；在 AutoDL 上运行 vLLM 观察显存</li>
<li><strong>完成标准</strong>：能解释 block 分配策略；能解释 copy-on-write 在 beam search 中的作用</li>
</ul>
<h3 id="节点-10-·-FlashAttention"><a href="#节点-10-·-FlashAttention" class="headerlink" title="节点 10 · FlashAttention"></a>节点 10 · FlashAttention</h3><ul>
<li><strong>核心问题</strong>：标准 attention 的 IO 瓶颈在哪？FlashAttention 的 tiling 如何解决？</li>
<li><strong>参考材料</strong>：<a href="https://arxiv.org/abs/2205.14135">FlashAttention 论文</a>、<a href="https://github.com/Dao-AILab/flash-attention">FlashAttention 源码</a></li>
<li><strong>环境</strong>：AutoDL RTX 3090（安装 flash-attention）</li>
<li><strong>实践</strong>：对比标准 attention 和 FlashAttention 的显存占用与速度；读懂论文 Algorithm 1 伪代码</li>
<li><strong>完成标准</strong>：能手画 FlashAttention tiling 示意图；能解释 online softmax trick</li>
</ul>
<h3 id="节点-11-·-量化"><a href="#节点-11-·-量化" class="headerlink" title="节点 11 · 量化"></a>节点 11 · 量化</h3><ul>
<li><strong>核心问题</strong>：INT8&#x2F;FP8 量化如何减少显存？PTQ 和 QAT 的区别和代价？</li>
<li><strong>参考材料</strong>：<a href="https://arxiv.org/abs/2208.07339">LLM.int8() 论文</a>、<a href="https://arxiv.org/abs/2210.17323">GPTQ 论文</a></li>
<li><strong>环境</strong>：AutoDL（用 bitsandbytes 加载量化模型）</li>
<li><strong>实践</strong>：在 AutoDL 上用 bitsandbytes 加载 INT8 量化模型，对比 FP16 和 INT8 的显存与速度</li>
<li><strong>完成标准</strong>：能解释 absmax 量化计算；能解释为什么大模型量化难（activation outlier）</li>
</ul>
<h3 id="节点-12-·-推理系统设计全景"><a href="#节点-12-·-推理系统设计全景" class="headerlink" title="节点 12 · 推理系统设计全景"></a>节点 12 · 推理系统设计全景</h3><ul>
<li><strong>核心问题</strong>：给定延迟&#x2F;吞吐&#x2F;成本约束，如何选配置？各优化手段如何组合？</li>
<li><strong>参考材料</strong>：<a href="https://docs.vllm.ai/">vLLM 文档</a>、<a href="https://nvidia.github.io/TensorRT-LLM/">TensorRT-LLM 文档</a></li>
<li><strong>环境</strong>：AutoDL（部署 vLLM + benchmark）</li>
<li><strong>实践</strong>：在 AutoDL 上用 vLLM 部署 Qwen-7B，用 benchmark 脚本测 throughput&#x2F;latency，调整 batch size 观察变化</li>
<li><strong>完成标准</strong>：能解释 TTFT&#x2F;TPOT&#x2F;throughput 三个指标；能分析为什么加大 batch size 提升吞吐但增加延迟</li>
</ul>
<hr>
<h2 id="可迁移资产"><a href="#可迁移资产" class="headerlink" title="可迁移资产"></a>可迁移资产</h2><table>
<thead>
<tr>
<th>已有经验</th>
<th>在哪个节点用得上</th>
</tr>
</thead>
<tbody><tr>
<td>OS 底层（内存管理、进程、调度）</td>
<td>节点 5、6、9：显存管理和调度直接类比</td>
</tr>
<tr>
<td>Gluten 向量化执行引擎（算子适配）</td>
<td>节点 6、10：理解执行引擎如何接近硬件</td>
</tr>
<tr>
<td>Spark Plugin 执行引擎内部经验</td>
<td>节点 8、12：理解系统级调度和执行计划</td>
</tr>
<tr>
<td>Java &#x2F; Spark Infra 实习</td>
<td>节点 12：系统设计取舍的工程经验</td>
</tr>
</tbody></table>
<hr>
<h2 id="知识清单对照"><a href="#知识清单对照" class="headerlink" title="知识清单对照"></a>知识清单对照</h2><blockquote>
<p>权威源：<a href="https://github.com/Infrasys-AI/AIInfra">AIInfra Module 05</a>、<a href="https://lilianweng.github.io/posts/2023-01-10-inference-optimization/">Lilian Weng: LLM Inference Optimization</a>、<a href="https://docs.nvidia.com/cuda/cuda-c-programming-guide/">CUDA C Programming Guide</a><br>对照日期：2026-03-29</p>
</blockquote>
<table>
<thead>
<tr>
<th>权威源章节&#x2F;概念</th>
<th>覆盖情况</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>Transformer &#x2F; Attention 结构</td>
<td>已覆盖（节点 01）</td>
<td>—</td>
</tr>
<tr>
<td>C++ 内存模型 &#x2F; RAII</td>
<td>已覆盖（节点 02-03）</td>
<td>前置差距补入</td>
</tr>
<tr>
<td>CUDA thread&#x2F;warp&#x2F;block 执行模型</td>
<td>已覆盖（节点 04）</td>
<td>前置差距补入</td>
</tr>
<tr>
<td>CUDA 内存优化（coalescing &#x2F; shared memory）</td>
<td>已覆盖（节点 05）</td>
<td>前置差距补入</td>
</tr>
<tr>
<td>GPU 性能模型 &#x2F; roofline</td>
<td>已覆盖（节点 06）</td>
<td>—</td>
</tr>
<tr>
<td>KV Cache 优化</td>
<td>已覆盖（节点 07、09）</td>
<td>—</td>
</tr>
<tr>
<td>批处理策略</td>
<td>已覆盖（节点 08）</td>
<td>—</td>
</tr>
<tr>
<td>PagedAttention &#x2F; 显存管理</td>
<td>已覆盖（节点 09）</td>
<td>—</td>
</tr>
<tr>
<td>FlashAttention &#x2F; 算子优化</td>
<td>已覆盖（节点 10）</td>
<td>—</td>
</tr>
<tr>
<td>量化（PTQ &#x2F; QAT）</td>
<td>已覆盖（节点 11）</td>
<td>—</td>
</tr>
<tr>
<td>分布式调度 &#x2F; 系统设计全景</td>
<td>已覆盖（节点 12）</td>
<td>—</td>
</tr>
<tr>
<td>长序列推理 &#x2F; 并行策略</td>
<td>进阶&#x2F;超纲</td>
<td>需先掌握基础系统，Phase 2 再覆盖</td>
</tr>
<tr>
<td>Speculative Decoding &#x2F; MoE 推理</td>
<td>进阶&#x2F;超纲</td>
<td>高级加速技术，当前阶段不覆盖</td>
</tr>
<tr>
<td>知识蒸馏 &#x2F; 剪枝 &#x2F; 稀疏</td>
<td>不需要</td>
<td>偏模型研究方向，非推理 infra 核心</td>
</tr>
</tbody></table>
]]></content>
      <tags>
        <tag>llm</tag>
        <tag>inference</tag>
        <tag>cuda</tag>
        <tag>transformer</tag>
        <tag>ai-infra</tag>
        <tag>cpp</tag>
      </tags>
  </entry>
  <entry>
    <title>计算机网络</title>
    <url>/2026/03/15/network/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/</url>
    <content><![CDATA[<h2 id="TCP-IP-四层模型总览"><a href="#TCP-IP-四层模型总览" class="headerlink" title="TCP&#x2F;IP 四层模型总览"></a>TCP&#x2F;IP 四层模型总览</h2><table>
<thead>
<tr>
<th>层级</th>
<th>名称</th>
<th>职责</th>
<th>核心协议</th>
<th>数据单元</th>
</tr>
</thead>
<tbody><tr>
<td><strong>4</strong></td>
<td>应用层</td>
<td>为用户程序提供网络服务接口，定义数据格式和交互规则</td>
<td>HTTP、DNS、FTP、SSH、SMTP</td>
<td>报文（Message）</td>
</tr>
<tr>
<td><strong>3</strong></td>
<td>传输层</td>
<td>端到端通信，通过端口号区分同一主机上的不同进程</td>
<td>TCP、UDP、QUIC</td>
<td>段 &#x2F; 数据报</td>
</tr>
<tr>
<td><strong>2</strong></td>
<td>网络层</td>
<td>主机到主机的寻址和路由，跨网络转发</td>
<td>IP、ICMP、ARP</td>
<td>包（Packet）</td>
</tr>
<tr>
<td><strong>1</strong></td>
<td>网络接口层</td>
<td>在物理介质上传输比特流，帧封装与硬件寻址</td>
<td>以太网、WiFi、PPP</td>
<td>帧（Frame）</td>
</tr>
</tbody></table>
<blockquote>
<p>与 OSI 七层的对应</p>
</blockquote>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">TCP/IP 四层          OSI 七层</span><br><span class="line">┌──────────┐    ┌──────────────┐</span><br><span class="line">│  应用层   │ ←→ │ 应用/表示/会话 │</span><br><span class="line">├──────────┤    ├──────────────┤</span><br><span class="line">│  传输层   │ ←→ │   传输层      │</span><br><span class="line">├──────────┤    ├──────────────┤</span><br><span class="line">│  网络层   │ ←→ │   网络层      │</span><br><span class="line">├──────────┤    ├──────────────┤</span><br><span class="line">│ 网络接口层 │ ←→ │ 数据链路/物理  │</span><br><span class="line">└──────────┘    └──────────────┘</span><br></pre></td></tr></table></figure>

<blockquote>
<p>数据封装与解封装</p>
</blockquote>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">应用层:     [HTTP 数据]</span><br><span class="line">              ↓ + TCP头</span><br><span class="line">传输层:     [TCP头 | HTTP 数据]              ← 段 Segment</span><br><span class="line">              ↓ + IP头</span><br><span class="line">网络层:     [IP头 | TCP头 | HTTP 数据]        ← 包 Packet</span><br><span class="line">              ↓ + 帧头 + FCS</span><br><span class="line">网络接口层: [帧头 | IP头 | TCP头 | HTTP 数据 | FCS]  ← 帧 Frame</span><br></pre></td></tr></table></figure>

<p>每层只关心<strong>本层头部</strong>，对上层数据视为不透明 payload — 分层解耦的核心。接收方向逆过程，逐层剥头部。</p>
<blockquote>
<p>每层解决的核心问题</p>
</blockquote>
<ul>
<li><strong>应用层</strong>：定义”说什么” — 消息格式、请求&#x2F;响应语义</li>
<li><strong>传输层</strong>：解决”哪个进程” — 通过<strong>端口号</strong>区分应用，TCP 可靠有序，UDP 快速轻量</li>
<li><strong>网络层</strong>：解决”到哪台主机” — 通过 <strong>IP 地址</strong>全局寻址，路由器逐跳转发，IP 是尽力而为（不可靠、无连接）</li>
<li><strong>网络接口层</strong>：解决”下一跳怎么到” — 通过 <strong>MAC 地址</strong>在局域网内定位物理设备，ARP 把 IP 解析成 MAC</li>
</ul>
<h2 id="TCP-UDP"><a href="#TCP-UDP" class="headerlink" title="TCP&#x2F;UDP"></a>TCP&#x2F;UDP</h2><blockquote>
<p>介绍一下TCP</p>
</blockquote>
<p>TCP 是传输层协议，向上层提供<strong>面向连接的、可靠的、基于字节流</strong>的端到端传输服务，具备流量控制与拥塞控制能力。</p>
<p>TCP 建立连接的过程是三次握手，断开连接的过程是四次挥手。</p>
<blockquote>
<p>建立连接的详细 timeline 与双方各自的 action：</p>
</blockquote>
<ol>
<li><strong>服务端准备阶段</strong>：调用 <code>socket()</code> 系统调用，内核创建 <code>struct sock</code> 等内核对象并初始化操作函数与回调。然后调用 <code>bind()</code>，将 socket 绑定到具体的 IP:port。再调用 <code>listen()</code>，申请并初始化半连接队列（哈希表）和全连接队列（链表）。</li>
<li><strong>客户端准备阶段</strong>：同样调用 <code>socket()</code> 创建内核对象。客户端通常<strong>不调用</strong> <code>bind()</code>，端口由后续 <code>connect()</code> 内部自动选择。</li>
<li><strong>第一次握手（客户端 → 服务端 SYN）</strong>：客户端调用 <code>connect()</code>，内核将 socket 状态设为 <code>TCP_SYN_SENT</code>，通过 <code>inet_hash_connect()</code> 从 <code>ip_local_port_range</code> 中动态选择一个可用端口，构造并发送 SYN 包，同时启动重传定时器。</li>
<li><strong>第二次握手（服务端 → 客户端 SYN+ACK）</strong>：服务端网卡收到 SYN 包后，通过 DMA 将数据帧写入内核预分配的 Ring Buffer（环形缓冲区），触发硬中断 → 软中断（NAPI）从 Ring Buffer 取出数据分配 <code>skb</code> → 送入协议栈，由 <code>tcp_v4_rcv</code> → <code>tcp_v4_do_rcv</code> → <code>tcp_rcv_state_process</code> 处理。服务端检查半连接队列和全连接队列是否已满（满则可能丢弃），创建 <code>request_sock</code>，构造并发送 SYN+ACK 响应，将 <code>request_sock</code> 添加到半连接队列，并启动 SYN+ACK 重传定时器。</li>
<li><strong>第三次握手（客户端 → 服务端 ACK）</strong>：客户端收到 SYN+ACK 后，将 socket 状态设为 <code>TCP_ESTABLISHED</code>，初始化拥塞控制，开启保活定时器，发送 ACK，并清除 <code>connect</code> 时设置的重传定时器。</li>
<li><strong>服务端完成连接</strong>：服务端收到 ACK 后，在半连接队列中查找对应的 <code>request_sock</code>，创建子 socket，将其从半连接队列移除并加入全连接队列，子 socket 状态设为 <code>TCP_ESTABLISHED</code>。之后服务端调用 <code>accept()</code> 从全连接队列中取出一个已建立的连接返回给用户进程。</li>
<li><strong>accept() 阻塞机制</strong>：<code>accept()</code> 默认阻塞。若全连接队列为空，内核将进程放入监听 socket 的等待队列，进程状态设为 <code>TASK_INTERRUPTIBLE</code>（可中断睡眠）。当新连接加入全连接队列时，通过 <code>sk_data_ready</code> 回调唤醒进程，进程回到 <code>TASK_RUNNING</code>，从队列头部取出连接返回。</li>
</ol>
<blockquote>
<p>三次握手过程中各阶段创建的内核对象</p>
</blockquote>
<table>
<thead>
<tr>
<th>阶段</th>
<th>创建的对象</th>
<th>存放位置</th>
</tr>
</thead>
<tbody><tr>
<td>服务端 <code>socket()</code></td>
<td>监听 socket（完整 <code>struct sock</code>）</td>
<td>—</td>
</tr>
<tr>
<td>客户端 <code>socket()</code> + <code>connect()</code></td>
<td>客户端 socket（完整 <code>struct sock</code>）</td>
<td>—</td>
</tr>
<tr>
<td>收到 SYN（第二次握手）</td>
<td><code>request_sock</code>（<strong>轻量级</strong>，仅记录握手必要信息）</td>
<td>半连接队列</td>
</tr>
<tr>
<td>收到 ACK（第三次握手）</td>
<td>子 socket（完整 <code>struct sock</code>，由 <code>tcp_v4_syn_recv_sock()</code> 创建）</td>
<td>全连接队列</td>
</tr>
<tr>
<td><code>accept()</code></td>
<td>从全连接队列取出子 socket</td>
<td>返回给用户进程</td>
</tr>
</tbody></table>
<p><strong>关键区分</strong>：半连接队列中存放的是轻量的 <code>request_sock</code>，<strong>不是</strong>完整 socket。只有第三次握手完成后才创建完整的子 socket。这样设计是为了<strong>防御 SYN Flood 攻击</strong>——攻击者发大量伪造 SYN 时，轻量的 <code>request_sock</code> 消耗的资源远小于完整 socket，避免服务端内存被迅速耗尽。</p>
<p><strong>监听 socket 只负责握手，不参与数据通信</strong>。服务端实际用于通信的是第三次握手时创建的子 socket（<code>accept()</code> 返回的 fd）。</p>
<blockquote>
<p>服务端与客户端 Socket 编程示例</p>
</blockquote>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ========== 服务端 ==========</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="comment">// 1. 创建 socket — 内核创建 struct sock，初始化操作函数表</span></span><br><span class="line">    <span class="type">int</span> listen_fd = socket(AF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. bind — 绑定 IP:port</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">addr</span>;</span></span><br><span class="line">    addr.sin_family = AF_INET;</span><br><span class="line">    addr.sin_port = htons(<span class="number">8080</span>);</span><br><span class="line">    addr.sin_addr.s_addr = htonl(INADDR_ANY);</span><br><span class="line">    bind(listen_fd, (<span class="keyword">struct</span> sockaddr *)&amp;addr, <span class="keyword">sizeof</span>(addr));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. listen — 初始化半连接队列（哈希表）和全连接队列（链表）</span></span><br><span class="line">    <span class="comment">//    128 是 backlog，全连接队列长度 = min(backlog, somaxconn)</span></span><br><span class="line">    listen(listen_fd, <span class="number">128</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 4. 循环 accept — 每次从全连接队列取出一个已完成三次握手的连接</span></span><br><span class="line">    <span class="comment">//    队列为空时阻塞，进程进入 TASK_INTERRUPTIBLE</span></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">client_addr</span>;</span></span><br><span class="line">        <span class="type">socklen_t</span> len = <span class="keyword">sizeof</span>(client_addr);</span><br><span class="line">        <span class="type">int</span> conn_fd = accept(listen_fd, (<span class="keyword">struct</span> sockaddr *)&amp;client_addr, &amp;len);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 5. 通信（实际生产中通常 fork 子进程或交给线程池处理）</span></span><br><span class="line">        <span class="type">char</span> buf[<span class="number">1024</span>];</span><br><span class="line">        read(conn_fd, buf, <span class="keyword">sizeof</span>(buf));</span><br><span class="line">        write(conn_fd, <span class="string">&quot;OK&quot;</span>, <span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 6. 关闭该连接 — 触发四次挥手</span></span><br><span class="line">        close(conn_fd);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    close(listen_fd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ========== 客户端 ==========</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="comment">// 1. 创建 socket — 同样创建内核对象</span></span><br><span class="line">    <span class="type">int</span> fd = socket(AF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 不调用 bind()，端口由 connect() 内部自动选择</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. connect — 选端口 + 发 SYN + 三次握手</span></span><br><span class="line">    <span class="comment">//    内部：状态 → TCP_SYN_SENT → 选端口 → 发 SYN → 等 SYN+ACK → 发 ACK → ESTABLISHED</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">server_addr</span>;</span></span><br><span class="line">    server_addr.sin_family = AF_INET;</span><br><span class="line">    server_addr.sin_port = htons(<span class="number">8080</span>);</span><br><span class="line">    inet_pton(AF_INET, <span class="string">&quot;127.0.0.1&quot;</span>, &amp;server_addr.sin_addr);</span><br><span class="line">    connect(fd, (<span class="keyword">struct</span> sockaddr *)&amp;server_addr, <span class="keyword">sizeof</span>(server_addr));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. 通信</span></span><br><span class="line">    write(fd, <span class="string">&quot;Hello&quot;</span>, <span class="number">5</span>);</span><br><span class="line">    <span class="type">char</span> buf[<span class="number">1024</span>];</span><br><span class="line">    read(fd, buf, <span class="keyword">sizeof</span>(buf));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 4. 关闭</span></span><br><span class="line">    close(fd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>断开连接的过程（四次挥手）</p>
</blockquote>
<p>断开连接的双方是对等的，任何一方都可以主动发起关闭。以<strong>客户端主动关闭</strong>为例：</p>
<ol>
<li><strong>第一次挥手（客户端 → 服务端 FIN）</strong>：客户端调用 <code>close(fd)</code>，内核进入 <code>tcp_close()</code>。将 socket 状态设为 <code>TCP_FIN_WAIT1</code>，构造并发送 FIN 包（表示”我不再发送数据了”），启动重传定时器。注意 FIN 包会占用一个序列号。</li>
<li><strong>第二次挥手（服务端 → 客户端 ACK）</strong>：服务端收到 FIN 后，协议栈在 <code>tcp_rcv_state_process</code> 中处理，将服务端 socket 状态设为 <code>TCP_CLOSE_WAIT</code>，<strong>立即回复 ACK</strong>。此时服务端仍可向客户端发送数据（半关闭状态）。客户端收到 ACK 后，状态从 <code>TCP_FIN_WAIT1</code> 变为 <code>TCP_FIN_WAIT2</code>。</li>
<li><strong>第三次挥手（服务端 → 客户端 FIN）</strong>：服务端处理完剩余数据后，调用 <code>close(fd)</code>，内核同样进入 <code>tcp_close()</code>，将 socket 状态设为 <code>TCP_LAST_ACK</code>，构造并发送 FIN 包，启动重传定时器。</li>
<li><strong>第四次挥手（客户端 → 服务端 ACK）</strong>：客户端收到 FIN 后，发送 ACK，状态从 <code>TCP_FIN_WAIT2</code> 变为 <strong><code>TIME_WAIT</code></strong>。服务端收到 ACK 后，状态从 <code>TCP_LAST_ACK</code> 变为 <code>TCP_CLOSE</code>，连接彻底释放。</li>
</ol>
<blockquote>
<p>为什么需要 TIME_WAIT？</p>
</blockquote>
<p>客户端进入 <code>TIME_WAIT</code> 后<strong>不会立即释放</strong>，而是等待 <strong>2MSL</strong>（Maximum Segment Lifetime，通常 60s）后才进入 <code>TCP_CLOSE</code>。原因：</p>
<ul>
<li><strong>确保最后一个 ACK 到达服务端</strong>：如果服务端没收到 ACK，会重传 FIN。客户端在 <code>TIME_WAIT</code> 状态下仍能接收并重新回复 ACK。如果直接关闭，服务端的重传 FIN 会收到 RST，导致异常断连。</li>
<li><strong>防止旧连接的延迟报文干扰新连接</strong>：等 2MSL 确保网络中属于该连接的所有报文都已过期消亡，不会被后续复用相同四元组的新连接误收。</li>
</ul>
<blockquote>
<p>TIME_WAIT 过多的影响与应对</p>
</blockquote>
<p><code>TIME_WAIT</code> 状态的 socket 会占用端口和内核内存。高并发短连接场景下（如 HTTP 短连接），大量 <code>TIME_WAIT</code> 可能导致端口耗尽。常见应对：</p>
<ul>
<li><strong><code>net.ipv4.tcp_tw_reuse = 1</code></strong>：允许在 <code>TIME_WAIT</code> 状态下复用端口建立新的<strong>出站</strong>连接（依赖 TCP 时间戳判断新旧报文）</li>
<li><strong>使用长连接</strong>（HTTP Keep-Alive）减少连接建立&#x2F;断开频率</li>
<li>扩大 <code>ip_local_port_range</code> 范围</li>
</ul>
<blockquote>
<p>四次挥手状态变迁总结</p>
</blockquote>
<table>
<thead>
<tr>
<th>阶段</th>
<th>客户端状态</th>
<th>服务端状态</th>
</tr>
</thead>
<tbody><tr>
<td>客户端 <code>close()</code> 发 FIN</td>
<td><code>FIN_WAIT_1</code></td>
<td>—</td>
</tr>
<tr>
<td>服务端收到 FIN 回 ACK</td>
<td><code>FIN_WAIT_2</code></td>
<td><code>CLOSE_WAIT</code></td>
</tr>
<tr>
<td>服务端 <code>close()</code> 发 FIN</td>
<td><code>FIN_WAIT_2</code></td>
<td><code>LAST_ACK</code></td>
</tr>
<tr>
<td>客户端收到 FIN 回 ACK</td>
<td><code>TIME_WAIT</code></td>
<td><code>CLOSE</code></td>
</tr>
<tr>
<td>等待 2MSL</td>
<td><code>CLOSE</code></td>
<td>—</td>
</tr>
</tbody></table>
<blockquote>
<p>TCP Keep-Alive（保活机制）</p>
</blockquote>
<p>TCP 保活定时器是<strong>内核级别的心跳机制</strong>，用于检测长时间空闲的连接对端是否还存活。</p>
<p><strong>工作原理</strong>：开启 <code>SO_KEEPALIVE</code> 的一方，在连接空闲一段时间后，发送一个<strong>序列号比期望值小 1 的 ACK 包</strong>（不是空包），对端收到后回复正常 ACK。如果收不到回复，则按间隔重复探测，连续多次无响应后判定对端死亡，内核关闭连接。</p>
<p><strong>三个内核参数</strong>：</p>
<table>
<thead>
<tr>
<th>参数</th>
<th>默认值</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td><code>tcp_keepalive_time</code></td>
<td>7200s（2小时）</td>
<td>连接空闲多久后开始发探测包</td>
</tr>
<tr>
<td><code>tcp_keepalive_intvl</code></td>
<td>75s</td>
<td>每次探测之间的间隔</td>
</tr>
<tr>
<td><code>tcp_keepalive_probes</code></td>
<td>9</td>
<td>连续多少次探测无响应就判定对端死亡</td>
</tr>
</tbody></table>
<p><strong>注意</strong>：</p>
<ul>
<li>保活探测<strong>双方都可以开启</strong>，不是只有客户端。实际更常见的是<strong>服务端开启</strong>，用于清理死连接释放资源</li>
<li>需要应用层显式设置 <code>SO_KEEPALIVE</code> 套接字选项才生效</li>
<li>默认 2 小时太慢，很多场景需要秒级感知存活（如 RPC、WebSocket），因此<strong>应用层通常自己实现心跳协议</strong>（如每 30s 发 ping&#x2F;pong），比内核 Keep-Alive 更灵活</li>
</ul>
<blockquote>
<p>MSS 与 MTU</p>
</blockquote>
<p><strong>MTU（Maximum Transmission Unit）</strong> 是<strong>数据链路层</strong>对单个帧能承载的最大数据量的限制。以太网默认 MTU &#x3D; <strong>1500 字节</strong>（指 IP 包的最大长度，不含以太网帧头 14B 和 FCS 4B）。</p>
<p><strong>MSS（Maximum Segment Size）</strong> 是<strong>传输层 TCP</strong> 单个报文段能携带的最大<strong>应用数据</strong>长度，不含 TCP 头部和 IP 头部。计算关系：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">MSS = MTU - IP头部(20B) - TCP头部(20B) = 1500 - 20 - 20 = 1460 字节</span><br></pre></td></tr></table></figure>

<p><strong>MSS 协商发生在三次握手期间</strong>：双方在 SYN 和 SYN+ACK 包的 TCP Options 字段中各自携带自己的 MSS 值，通信时取<strong>双方较小值</strong>作为实际 MSS。</p>
<p><strong>为什么需要 MSS？</strong> 如果 TCP 不限制报文段大小，一个很大的 TCP 段在 IP 层会被分片（IP Fragmentation）。IP 分片的问题：</p>
<ul>
<li><strong>任何一个分片丢失，整个 IP 数据报都要重传</strong>，效率极低</li>
<li>分片和重组消耗路由器&#x2F;主机资源</li>
<li>中间设备可能有更小的 MTU（路径 MTU），导致二次分片</li>
</ul>
<p>MSS 让 TCP 在源端就将数据切成不超过链路 MTU 的大小，<strong>避免 IP 层分片</strong>，提高传输效率。</p>
<p><strong>常见值</strong>：</p>
<table>
<thead>
<tr>
<th>场景</th>
<th>MTU</th>
<th>MSS</th>
</tr>
</thead>
<tbody><tr>
<td>以太网</td>
<td>1500B</td>
<td>1460B</td>
</tr>
<tr>
<td>PPPoE（拨号&#x2F;光猫）</td>
<td>1492B</td>
<td>1452B</td>
</tr>
<tr>
<td>本地回环（lo）</td>
<td>65535B</td>
<td>65495B</td>
</tr>
</tbody></table>
<blockquote>
<p>拥塞控制与流量控制</p>
</blockquote>
<p><strong>流量控制</strong>和<strong>拥塞控制</strong>解决的是两个不同层面的问题：</p>
<table>
<thead>
<tr>
<th></th>
<th>流量控制</th>
<th>拥塞控制</th>
</tr>
</thead>
<tbody><tr>
<td><strong>解决什么问题</strong></td>
<td>发送方发太快，<strong>接收方</strong>来不及处理</td>
<td>所有发送方合起来发太多，<strong>网络</strong>承载不住</td>
</tr>
<tr>
<td><strong>作用范围</strong></td>
<td>端到端（发送方 ↔ 接收方）</td>
<td>全局（发送方 ↔ 网络）</td>
</tr>
<tr>
<td><strong>核心机制</strong></td>
<td>滑动窗口（接收窗口 rwnd）</td>
<td>拥塞窗口（cwnd）</td>
</tr>
</tbody></table>
<p><strong>实际发送窗口 &#x3D; min(rwnd, cwnd)</strong>，两者中更小的那个决定发送速率。</p>
<hr>
<p><strong>流量控制 — 滑动窗口（rwnd）</strong></p>
<ul>
<li>接收方在每个 ACK 中通过 TCP 头部的 <strong>Window 字段</strong>告知发送方自己还能接收多少数据（即 rwnd）</li>
<li>发送方保证已发送但未确认的数据量不超过 rwnd</li>
<li>如果接收方缓冲区满了，rwnd &#x3D; 0，发送方停止发送，进入<strong>窗口探测</strong>（Window Probe）：定期发送 1 字节的探测包，等待接收方窗口恢复</li>
<li>这是一个<strong>端到端的、被动的</strong>速率调节机制</li>
</ul>
<hr>
<p><strong>拥塞控制 — 四个阶段</strong></p>
<p>拥塞控制的核心变量是 <strong>cwnd</strong>（拥塞窗口）和 <strong>ssthresh</strong>（慢启动阈值）。</p>
<ol>
<li><strong>慢启动（Slow Start）</strong>：连接刚建立时 cwnd &#x3D; 1 MSS（或 initcwnd，Linux 默认 10），每收到一个 ACK，cwnd 翻倍（指数增长）。目的是快速探测网络容量。当 cwnd ≥ ssthresh 时，进入拥塞避免。</li>
<li><strong>拥塞避免（Congestion Avoidance）</strong>：cwnd 每个 RTT 只增加 1 MSS（线性增长），谨慎探测网络极限。持续增长直到检测到丢包。</li>
<li><strong>快重传（Fast Retransmit）</strong>：发送方收到 <strong>3 个重复 ACK</strong> 时，不等超时定时器，立即重传丢失的报文段。这比等待超时更快地恢复丢包。</li>
<li><strong>快恢复（Fast Recovery）</strong>：快重传后，ssthresh &#x3D; cwnd &#x2F; 2，cwnd &#x3D; ssthresh + 3 MSS，然后进入拥塞避免（线性增长）。<strong>不回到慢启动</strong>，因为收到重复 ACK 说明网络还能传数据，没有严重拥塞。</li>
</ol>
<p><strong>超时重传（RTO Timeout）</strong> 的处理更激进：ssthresh &#x3D; cwnd &#x2F; 2，cwnd <strong>直接重置为 1 MSS</strong>，回到慢启动。因为超时意味着网络可能严重拥塞。</p>
<hr>
<p><strong>拥塞控制算法演进</strong></p>
<table>
<thead>
<tr>
<th>算法</th>
<th>特点</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Reno</strong></td>
<td>经典四阶段（慢启动 + 拥塞避免 + 快重传 + 快恢复）</td>
</tr>
<tr>
<td><strong>Cubic</strong></td>
<td>Linux 默认算法，cwnd 增长函数是三次方曲线，对高带宽长延迟（BDP 大）网络更友好</td>
</tr>
<tr>
<td><strong>BBR</strong></td>
<td>Google 提出，不依赖丢包信号，而是主动探测瓶颈带宽和最小 RTT，适合高丢包率和长肥管道场景</td>
</tr>
</tbody></table>
<hr>
<p><strong>RTO 计算 — Jacobson 算法</strong></p>
<p>RTO（Retransmission Timeout）决定”等多久没收到 ACK 就判定丢包”，与拥塞控制（决定”发多快”）是<strong>独立的两个机制</strong>。RTO 直接基于 RTT 采样计算（RFC 6298）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">SRTT    = (1 - α) × SRTT + α × RTT_sample          // 平滑 RTT，α = 1/8</span><br><span class="line">RTTVAR  = (1 - β) × RTTVAR + β × |SRTT - RTT_sample|  // RTT 偏差，β = 1/4</span><br><span class="line">RTO     = SRTT + 4 × RTTVAR                          // 重传超时</span><br></pre></td></tr></table></figure>

<ul>
<li>RTT 波动大 → RTTVAR 大 → RTO 长，避免误判丢包</li>
<li>RTT 稳定 → RTTVAR 小 → RTO 短，快速检测真正的丢包</li>
<li>RTO 有下限（通常 200ms）和上限（通常 120s），防止极端值</li>
<li>连续超时重传时，RTO <strong>指数退避</strong>（每次翻倍），直到收到有效 ACK 才恢复正常计算</li>
</ul>
<blockquote>
<p>TCP与UDP 优缺点对比</p>
</blockquote>
<table>
<thead>
<tr>
<th>维度</th>
<th>TCP</th>
<th>UDP</th>
</tr>
</thead>
<tbody><tr>
<td><strong>连接</strong></td>
<td>面向连接，需三次握手建立、四次挥手断开</td>
<td>无连接，直接发送</td>
</tr>
<tr>
<td><strong>可靠性</strong></td>
<td>可靠传输（确认、重传、序列号、校验和）</td>
<td>不可靠，尽力而为，不保证到达和顺序</td>
</tr>
<tr>
<td><strong>有序性</strong></td>
<td>保证有序交付（通过序列号重排）</td>
<td>不保证顺序</td>
</tr>
<tr>
<td><strong>流量控制</strong></td>
<td>有（滑动窗口）</td>
<td>无</td>
</tr>
<tr>
<td><strong>拥塞控制</strong></td>
<td>有（慢启动、拥塞避免、快重传、快恢复）</td>
<td>无</td>
</tr>
<tr>
<td><strong>传输方式</strong></td>
<td>字节流（无边界，应用层需自行分包）</td>
<td>数据报（保留消息边界，一次 send 对应一次 recv）</td>
</tr>
<tr>
<td><strong>头部开销</strong></td>
<td>最小 20B（含序列号、确认号、窗口等）</td>
<td>固定 8B（源端口、目的端口、长度、校验和）</td>
</tr>
<tr>
<td><strong>通信模式</strong></td>
<td>一对一（点对点）</td>
<td>一对一、一对多（广播&#x2F;组播）</td>
</tr>
<tr>
<td><strong>速度</strong></td>
<td>较慢（连接建立 + 确认重传开销）</td>
<td>快（无连接、无确认）</td>
</tr>
<tr>
<td><strong>资源消耗</strong></td>
<td>高（维护连接状态、缓冲区、定时器）</td>
<td>低</td>
</tr>
</tbody></table>
<p><strong>TCP 适用场景</strong>：需要可靠传输的场景 — HTTP&#x2F;HTTPS、FTP、SSH、数据库连接、邮件（SMTP&#x2F;IMAP）</p>
<p><strong>UDP 适用场景</strong>：对实时性要求高、允许少量丢包的场景 — DNS 查询、视频&#x2F;语音通话（RTP）、直播、游戏状态同步、QUIC（基于 UDP 实现的可靠传输协议）</p>
<p><strong>核心取舍</strong>：TCP 用复杂机制换可靠性，UDP 用简单性换速度。实际中也可以在 UDP 之上自行实现可靠传输（如 QUIC、KCP），兼得灵活性和性能。</p>
<h2 id="I-O-多路复用"><a href="#I-O-多路复用" class="headerlink" title="I&#x2F;O 多路复用"></a>I&#x2F;O 多路复用</h2><blockquote>
<p>从阻塞式 I&#x2F;O 到多路复用</p>
</blockquote>
<p>回顾上面服务端代码，<code>accept()</code> 和 <code>read()</code> 都是<strong>阻塞调用</strong>：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 阻塞式服务端 — 一次只能处理一个连接</span></span><br><span class="line"><span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">    <span class="type">int</span> conn_fd = accept(listen_fd, ...);  <span class="comment">// 阻塞，等全连接队列非空</span></span><br><span class="line">    <span class="type">char</span> buf[<span class="number">1024</span>];</span><br><span class="line">    read(conn_fd, buf, <span class="keyword">sizeof</span>(buf));       <span class="comment">// 阻塞，等数据到达</span></span><br><span class="line">    write(conn_fd, <span class="string">&quot;OK&quot;</span>, <span class="number">2</span>);</span><br><span class="line">    close(conn_fd);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>问题</strong>：模型是串行的 — <code>read()</code> 等待 A 客户端数据时，B 客户端的连接请求被阻塞在 <code>accept()</code>。</p>
<p><strong>传统解法</strong>：每个连接 fork 子进程或创建线程。但到 C10K（1 万并发）时，线程&#x2F;进程的创建、调度、上下文切换、内存开销（每个线程栈默认 8MB）无法承受。</p>
<p><strong>I&#x2F;O 多路复用的核心思想</strong>：用<strong>一个线程</strong>同时监控<strong>多个 fd</strong> 的就绪状态，哪个 fd 有事件（可读&#x2F;可写&#x2F;异常）就处理哪个，避免在任何一个 fd 上阻塞等待。”多路”指多个 I&#x2F;O 通道（fd），”复用”指复用同一个线程。</p>
<blockquote>
<p>select &#x2F; poll — 早期方案</p>
</blockquote>
<p><strong>select</strong>：</p>
<ul>
<li>调用 <code>select(nfds, readfds, writefds, exceptfds, timeout)</code>，传入感兴趣的 fd 集合（bitmap）</li>
<li>内核遍历所有传入的 fd，检查哪些就绪，标记后返回</li>
<li><strong>缺陷</strong>：<ul>
<li>fd 集合用 bitmap 实现，默认上限 <strong>1024</strong>（<code>FD_SETSIZE</code>）</li>
<li>每次调用都要把 fd 集合从用户态<strong>拷贝到内核态</strong></li>
<li>内核每次都要<strong>线性遍历</strong>所有 fd，O(n)</li>
<li>返回后用户态还要再遍历一遍才知道哪个 fd 就绪</li>
</ul>
</li>
</ul>
<p><strong>poll</strong>：</p>
<ul>
<li>用 <code>struct pollfd</code> 数组替代 bitmap，<strong>去掉了 1024 限制</strong></li>
<li>但其他问题不变：每次拷贝、每次线性遍历、O(n)</li>
</ul>
<blockquote>
<p>epoll — Linux 高性能方案</p>
</blockquote>
<p>epoll 是 Linux 2.6 引入的 I&#x2F;O 多路复用机制，解决了 select&#x2F;poll 的所有性能瓶颈。</p>
<p><strong>三个系统调用</strong>：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 1. 创建 epoll 实例，返回 epoll fd</span></span><br><span class="line"><span class="type">int</span> epfd = epoll_create1(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 注册/修改/删除要监控的 fd 及其关注的事件</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">epoll_event</span> <span class="title">ev</span>;</span></span><br><span class="line">ev.events = EPOLLIN;    <span class="comment">// 关注可读事件</span></span><br><span class="line">ev.data.fd = conn_fd;</span><br><span class="line">epoll_ctl(epfd, EPOLL_CTL_ADD, conn_fd, &amp;ev);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 3. 等待事件就绪，返回就绪的 fd 列表</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">epoll_event</span> <span class="title">events</span>[<span class="title">MAX_EVENTS</span>];</span></span><br><span class="line"><span class="type">int</span> n = epoll_wait(epfd, events, MAX_EVENTS, timeout_ms);</span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">    <span class="comment">// events[i].data.fd 就绪，直接处理</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>内核实现原理</strong>：</p>
<ol>
<li><p><strong><code>epoll_create1()</code></strong>：内核创建 <code>eventpoll</code> 结构体，包含：</p>
<ul>
<li><strong>红黑树（<code>rbr</code>）</strong>：存储所有被监控的 fd（<code>epitem</code> 节点），增删查 O(log n)</li>
<li><strong>就绪链表（<code>rdllist</code>）</strong>：存储已就绪的 fd</li>
</ul>
</li>
<li><p><strong><code>epoll_ctl(ADD)</code></strong>：在红黑树中插入 <code>epitem</code> 节点，同时向该 fd 对应的设备驱动（如 socket 的等待队列）<strong>注册回调函数</strong>。当 fd 就绪时（如数据到达触发 <code>sk_data_ready</code>），回调将对应 <code>epitem</code> 加入就绪链表</p>
</li>
<li><p><strong><code>epoll_wait()</code></strong>：检查就绪链表是否为空。非空则直接返回就绪事件列表；为空则阻塞，直到有回调将 <code>epitem</code> 加入就绪链表后唤醒</p>
</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">eventpoll 结构体</span><br><span class="line">┌──────────────────────────────────────────────────┐</span><br><span class="line">│                                                  │</span><br><span class="line">│  红黑树 (rbr)             就绪链表 (rdllist)       │</span><br><span class="line">│  ┌────────┐                                      │</span><br><span class="line">│  │ epitem │──────┐                               │</span><br><span class="line">│  │ fd=3   │      │     ┌────────┐  ┌────────┐    │</span><br><span class="line">│  ├────────┤      │     │ epitem │→ │ epitem │    │</span><br><span class="line">│  │ epitem │      │     │ fd=5   │  │ fd=8   │    │</span><br><span class="line">│  │ fd=5   │──────┘     └────────┘  └────────┘    │</span><br><span class="line">│  ├────────┤                  ↑                    │</span><br><span class="line">│  │ epitem │      回调触发时加入就绪链表              │</span><br><span class="line">│  │ fd=8   │                                      │</span><br><span class="line">│  └────────┘                                      │</span><br><span class="line">│                                                  │</span><br><span class="line">│  epoll_wait() 只需检查 rdllist                    │</span><br><span class="line">└──────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>

<p><strong>为什么 epoll 高效</strong>：</p>
<ul>
<li><code>epoll_ctl</code> 只在增删 fd 时调用一次，不需要每次 wait 都重新传入 fd 集合 → <strong>无重复拷贝</strong></li>
<li>fd 就绪通过回调<strong>主动上报</strong>到就绪链表，不需要遍历所有 fd → <strong>无线性扫描</strong></li>
<li><code>epoll_wait</code> 只返回就绪的 fd，用户态不需要遍历所有监控的 fd → <strong>精准通知</strong></li>
</ul>
<blockquote>
<p>水平触发（LT）与边缘触发（ET）</p>
</blockquote>
<table>
<thead>
<tr>
<th></th>
<th>水平触发（Level-Triggered，默认）</th>
<th>边缘触发（Edge-Triggered）</th>
</tr>
</thead>
<tbody><tr>
<td><strong>触发条件</strong></td>
<td>fd 处于就绪状态就<strong>持续通知</strong></td>
<td>fd 从非就绪变为就绪时通知<strong>一次</strong></td>
</tr>
<tr>
<td><strong>行为</strong></td>
<td>缓冲区有数据未读完 → 下次 <code>epoll_wait</code> 仍然返回该 fd</td>
<td>缓冲区有数据未读完 → 下次 <code>epoll_wait</code> <strong>不再返回</strong>，直到有新数据到达</td>
</tr>
<tr>
<td><strong>编程要求</strong></td>
<td>简单，可以分多次读</td>
<td>必须一次性读完（循环 read 直到 <code>EAGAIN</code>），且 fd 必须设为<strong>非阻塞</strong></td>
</tr>
<tr>
<td><strong>性能</strong></td>
<td>可能多次通知同一个 fd，有一定开销</td>
<td>通知次数少，适合高性能场景</td>
</tr>
</tbody></table>
<p><strong>ET 模式的典型写法</strong>：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ET 模式下必须非阻塞 + 循环读完</span></span><br><span class="line">ev.events = EPOLLIN | EPOLLET;      <span class="comment">// 边缘触发</span></span><br><span class="line">fcntl(fd, F_SETFL, O_NONBLOCK);     <span class="comment">// 必须非阻塞</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 事件处理</span></span><br><span class="line"><span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">    <span class="type">ssize_t</span> n = read(fd, buf, <span class="keyword">sizeof</span>(buf));</span><br><span class="line">    <span class="keyword">if</span> (n == <span class="number">-1</span> &amp;&amp; errno == EAGAIN) <span class="keyword">break</span>;  <span class="comment">// 读完了</span></span><br><span class="line">    <span class="keyword">if</span> (n == <span class="number">0</span>) &#123; close(fd); <span class="keyword">break</span>; &#125;       <span class="comment">// 对端关闭</span></span><br><span class="line">    process(buf, n);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>为什么 ET 必须非阻塞？</strong> 如果用阻塞 fd + 循环读，读完最后一块数据后 <code>read()</code> 会阻塞等待新数据，而不是返回 <code>EAGAIN</code>，导致整个事件循环卡死。</p>
<blockquote>
<p>epoll 典型使用模式 — Reactor</p>
</blockquote>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> epfd = epoll_create1(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 监听 socket 注册到 epoll</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">epoll_event</span> <span class="title">ev</span>;</span></span><br><span class="line">ev.events = EPOLLIN;</span><br><span class="line">ev.data.fd = listen_fd;</span><br><span class="line">epoll_ctl(epfd, EPOLL_CTL_ADD, listen_fd, &amp;ev);</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">epoll_event</span> <span class="title">events</span>[1024];</span></span><br><span class="line"><span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">    <span class="type">int</span> n = epoll_wait(epfd, events, <span class="number">1024</span>, <span class="number">-1</span>);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (events[i].data.fd == listen_fd) &#123;</span><br><span class="line">            <span class="comment">// 监听 socket 可读 → 有新连接</span></span><br><span class="line">            <span class="type">int</span> conn_fd = accept(listen_fd, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br><span class="line">            fcntl(conn_fd, F_SETFL, O_NONBLOCK);</span><br><span class="line">            ev.events = EPOLLIN | EPOLLET;</span><br><span class="line">            ev.data.fd = conn_fd;</span><br><span class="line">            epoll_ctl(epfd, EPOLL_CTL_ADD, conn_fd, &amp;ev);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// 连接 socket 可读 → 有数据到达</span></span><br><span class="line">            handle_read(events[i].data.fd);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这就是 <strong>Reactor 模式</strong>：主循环只负责事件分发，具体 I&#x2F;O 操作交给事件处理函数。Nginx、Redis（单线程模型）、Netty 都基于此模式。</p>
<blockquote>
<p>阻塞 I&#x2F;O vs select vs poll vs epoll 对比</p>
</blockquote>
<table>
<thead>
<tr>
<th>维度</th>
<th>阻塞 I&#x2F;O</th>
<th>select</th>
<th>poll</th>
<th>epoll</th>
</tr>
</thead>
<tbody><tr>
<td><strong>并发模型</strong></td>
<td>一连接一线程</td>
<td>单线程多 fd</td>
<td>单线程多 fd</td>
<td>单线程多 fd</td>
</tr>
<tr>
<td><strong>fd 上限</strong></td>
<td>受线程数限制</td>
<td>1024（FD_SETSIZE）</td>
<td>无硬限制</td>
<td>无硬限制</td>
</tr>
<tr>
<td><strong>fd 传递</strong></td>
<td>—</td>
<td>每次调用拷贝到内核</td>
<td>每次调用拷贝到内核</td>
<td><code>epoll_ctl</code> 注册一次</td>
</tr>
<tr>
<td><strong>就绪检测</strong></td>
<td>—</td>
<td>内核遍历所有 fd O(n)</td>
<td>内核遍历所有 fd O(n)</td>
<td>回调驱动，就绪链表</td>
</tr>
<tr>
<td><strong>返回结果</strong></td>
<td>—</td>
<td>需遍历找就绪 fd</td>
<td>需遍历找就绪 fd</td>
<td>只返回就绪 fd</td>
</tr>
<tr>
<td><strong>触发模式</strong></td>
<td>—</td>
<td>LT</td>
<td>LT</td>
<td>LT + ET</td>
</tr>
<tr>
<td><strong>适用连接数</strong></td>
<td>&lt; 几百</td>
<td>&lt; 千</td>
<td>&lt; 千</td>
<td>百万级</td>
</tr>
<tr>
<td><strong>典型应用</strong></td>
<td>简单工具</td>
<td>跨平台兼容</td>
<td>select 的改进替代</td>
<td>Nginx、Redis、Netty</td>
</tr>
</tbody></table>
<p><strong>面试一句话</strong>：epoll 通过<strong>红黑树</strong>管理 fd、<strong>回调驱动</strong>上报就绪事件、<strong>就绪链表</strong>实现 O(1) 获取，解决了 select&#x2F;poll 的 fd 限制、重复拷贝和线性扫描三大瓶颈，是 Linux 高并发服务器的核心 I&#x2F;O 模型。</p>
<h2 id="HTTP"><a href="#HTTP" class="headerlink" title="HTTP"></a>HTTP</h2><blockquote>
<p>HTTP 报文结构</p>
</blockquote>
<p><strong>请求报文</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">请求行:    方法 URL HTTP版本        例: GET /index.html HTTP/1.1</span><br><span class="line">请求头:    Host: example.com</span><br><span class="line">           Content-Type: application/json</span><br><span class="line">           Connection: keep-alive</span><br><span class="line">           ...</span><br><span class="line">空行:      \r\n</span><br><span class="line">请求体:    (GET 通常无 body，POST/PUT 有)</span><br></pre></td></tr></table></figure>

<p><strong>响应报文</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">状态行:    HTTP版本 状态码 原因短语    例: HTTP/1.1 200 OK</span><br><span class="line">响应头:    Content-Type: text/html</span><br><span class="line">           Content-Length: 1234</span><br><span class="line">           Set-Cookie: ...</span><br><span class="line">           ...</span><br><span class="line">空行:      \r\n</span><br><span class="line">响应体:    (HTML/JSON/二进制等)</span><br></pre></td></tr></table></figure>

<blockquote>
<p>HTTP 常见状态码</p>
</blockquote>
<table>
<thead>
<tr>
<th>分类</th>
<th>含义</th>
<th>常见状态码</th>
</tr>
</thead>
<tbody><tr>
<td><strong>1xx</strong></td>
<td>信息性，请求已接收，继续处理</td>
<td>100 Continue、101 Switching Protocols</td>
</tr>
<tr>
<td><strong>2xx</strong></td>
<td>成功</td>
<td>200 OK、201 Created、204 No Content</td>
</tr>
<tr>
<td><strong>3xx</strong></td>
<td>重定向，需要进一步操作</td>
<td>301 永久重定向、302 临时重定向、304 Not Modified</td>
</tr>
<tr>
<td><strong>4xx</strong></td>
<td>客户端错误</td>
<td>400 Bad Request、401 Unauthorized、403 Forbidden、404 Not Found、405 Method Not Allowed</td>
</tr>
<tr>
<td><strong>5xx</strong></td>
<td>服务端错误</td>
<td>500 Internal Server Error、502 Bad Gateway、503 Service Unavailable、504 Gateway Timeout</td>
</tr>
</tbody></table>
<blockquote>
<p>302 状态码</p>
</blockquote>
<p><strong>302 Found（临时重定向）</strong>：服务端告诉客户端”你请求的资源<strong>临时</strong>在另一个 URL”，响应头中包含 <code>Location: &lt;新URL&gt;</code>，浏览器会自动跳转到该 URL。</p>
<p><strong>与 301 的区别</strong>：</p>
<ul>
<li><strong>301 Moved Permanently</strong>：永久重定向，浏览器和搜索引擎会<strong>缓存</strong>这个重定向，后续请求直接访问新 URL</li>
<li><strong>302 Found</strong>：临时重定向，浏览器<strong>不缓存</strong>，每次仍先访问原 URL，再被重定向</li>
</ul>
<p><strong>实际场景</strong>：302 常用于登录跳转（未登录 → 302 → 登录页）、短链接服务、A&#x2F;B 测试等。</p>
<blockquote>
<p>没有 Keep-Alive 时 TCP 连接的关闭时机</p>
</blockquote>
<p><strong>HTTP&#x2F;1.0（默认短连接）</strong>：每个请求&#x2F;响应完成后，<strong>服务端发送完响应体的最后一个字节后立即 <code>close(fd)</code></strong>，触发四次挥手。每个 HTTP 请求独占一个 TCP 连接的完整生命周期，下一个请求需要重新三次握手。</p>
<p><strong>HTTP&#x2F;1.1 显式关闭</strong>：如果设置 <code>Connection: close</code>，行为与 HTTP&#x2F;1.0 一样，服务端发完响应后关闭连接。</p>
<p><strong>接收方如何判断”响应结束”</strong>：</p>
<ul>
<li><strong><code>Content-Length</code></strong>：明确告知响应体长度，收满即完成</li>
<li><strong><code>Transfer-Encoding: chunked</code></strong>：分块传输，收到长度为 0 的 chunk 表示结束</li>
<li><strong>连接关闭本身</strong>：如果既没有 <code>Content-Length</code> 也没有 chunked，接收方只能靠<strong>收到 FIN</strong> 来判断数据结束。短连接模式下，关闭连接不仅是释放资源，还承担了<strong>标记消息边界</strong>的作用</li>
</ul>
<blockquote>
<p>同浏览器打开同网页两次是否复用 TCP 连接</p>
</blockquote>
<p><strong>答案：通常会复用</strong>。</p>
<p>HTTP&#x2F;1.1 默认开启 <code>Connection: keep-alive</code>（长连接），同一域名下的多个请求会复用同一个 TCP 连接，避免重复三次握手的开销。</p>
<p><strong>具体行为</strong>：</p>
<ul>
<li><strong>HTTP&#x2F;1.1</strong>：浏览器对同一域名通常维护 <strong>6 个并发 TCP 连接</strong>（Chrome 默认），请求在这些连接上排队复用</li>
<li><strong>HTTP&#x2F;2</strong>：同一域名只需 <strong>1 个 TCP 连接</strong>，通过多路复用（Multiplexing）在单连接上并发传输多个请求&#x2F;响应</li>
<li>如果连接空闲超过 Keep-Alive 超时时间（服务端配置，如 nginx 默认 75s），连接会被关闭，下次需要重新建连</li>
</ul>
<p><strong>如何验证</strong>：</p>
<ol>
<li><strong>浏览器 DevTools → Network 面板</strong>：查看 <code>Connection ID</code> 列，相同 ID 表示复用了同一个 TCP 连接</li>
<li><strong><code>chrome://net-internals/#sockets</code></strong>：查看活跃的 socket 连接池</li>
<li><strong>抓包（Wireshark&#x2F;tcpdump）</strong>：观察两次 HTTP 请求是否在同一个四元组的 TCP 连接上进行，没有额外的三次握手</li>
</ol>
<blockquote>
<p>HTTP&#x2F;2 与 HTTP&#x2F;1.1 的区别</p>
</blockquote>
<table>
<thead>
<tr>
<th>维度</th>
<th>HTTP&#x2F;1.1</th>
<th>HTTP&#x2F;2</th>
</tr>
</thead>
<tbody><tr>
<td><strong>传输格式</strong></td>
<td>文本协议（明文可读）</td>
<td>二进制帧（Binary Framing），更高效解析</td>
</tr>
<tr>
<td><strong>多路复用</strong></td>
<td>单连接串行（队头阻塞），靠多个 TCP 连接并发</td>
<td>单连接多路复用（Stream），多个请求&#x2F;响应并发交错传输</td>
</tr>
<tr>
<td><strong>队头阻塞</strong></td>
<td>HTTP 层有（前一个请求未完成，后面的排队等待）</td>
<td>HTTP 层解决了（但 TCP 层队头阻塞仍存在）</td>
</tr>
<tr>
<td><strong>头部压缩</strong></td>
<td>无，每次携带完整头部（Cookie 可能很大）</td>
<td>HPACK 算法压缩头部，维护动态&#x2F;静态表，增量传输</td>
</tr>
<tr>
<td><strong>服务端推送</strong></td>
<td>不支持</td>
<td>Server Push：服务端可以主动推送客户端可能需要的资源</td>
</tr>
<tr>
<td><strong>连接数</strong></td>
<td>同域名通常 6 个并发 TCP 连接</td>
<td>同域名 1 个 TCP 连接即可</td>
</tr>
<tr>
<td><strong>优先级</strong></td>
<td>无</td>
<td>支持流优先级和依赖关系</td>
</tr>
</tbody></table>
<p><strong>HTTP&#x2F;2 的核心改进</strong>：在传输层和 HTTP 语义之间加了一层<strong>二进制帧层</strong>，将请求&#x2F;响应拆成帧，帧带有 Stream ID，不同 Stream 的帧可以交错发送，从而在单个 TCP 连接上实现真正的并发。</p>
<p><strong>HTTP&#x2F;2 未解决的问题</strong>：TCP 层队头阻塞 — 如果某个 TCP 包丢失，即使是不同 Stream 的数据，都要等重传完成后才能交付。这正是 HTTP&#x2F;3（QUIC）要解决的问题。</p>
<h2 id="QUIC-协议（HTTP-3）"><a href="#QUIC-协议（HTTP-3）" class="headerlink" title="QUIC 协议（HTTP&#x2F;3）"></a>QUIC 协议（HTTP&#x2F;3）</h2><blockquote>
<p>QUIC 是什么</p>
</blockquote>
<p>QUIC（Quick UDP Internet Connections）是 Google 设计、IETF 标准化的传输层协议，<strong>基于 UDP 实现</strong>，是 HTTP&#x2F;3 的底层传输协议。核心目标：解决 TCP + TLS 的固有缺陷。</p>
<blockquote>
<p>为什么需要 QUIC — TCP 的三个痛点</p>
</blockquote>
<ol>
<li><strong>TCP 队头阻塞</strong>：TCP 是字节流协议，一个包丢失会阻塞该连接上所有数据的交付，即使其他 Stream 的数据已到达。HTTP&#x2F;2 的多路复用在 TCP 上反而放大了这个问题。</li>
<li><strong>连接建立慢</strong>：TCP 三次握手（1 RTT）+ TLS 1.2 握手（2 RTT）&#x3D; 首次连接需要 3 RTT。即使 TLS 1.3 优化到 1 RTT，TCP + TLS 1.3 仍需 2 RTT。</li>
<li><strong>协议僵化</strong>：TCP 实现在操作系统内核中，中间设备（防火墙、NAT）对 TCP 头部有固定预期，升级 TCP 协议极其困难。</li>
</ol>
<blockquote>
<p>QUIC 如何解决这些问题</p>
</blockquote>
<table>
<thead>
<tr>
<th>问题</th>
<th>QUIC 的解决方案</th>
</tr>
</thead>
<tbody><tr>
<td><strong>队头阻塞</strong></td>
<td>每个 Stream 独立可靠传输，一个 Stream 丢包<strong>不影响</strong>其他 Stream。丢包重传粒度是 Stream 级别而非连接级别</td>
</tr>
<tr>
<td><strong>连接建立慢</strong></td>
<td>首次连接 <strong>1 RTT</strong>（握手 + TLS 合并），恢复连接 <strong>0 RTT</strong>（利用缓存的密钥直接发送数据）</td>
</tr>
<tr>
<td><strong>协议僵化</strong></td>
<td>基于 UDP，协议逻辑在用户态实现，不受内核和中间设备限制，迭代升级更快</td>
</tr>
<tr>
<td><strong>连接迁移</strong></td>
<td>用 <strong>Connection ID</strong> 标识连接（而非四元组），WiFi ↔ 4G 切换时 IP 变了连接不断</td>
</tr>
</tbody></table>
<blockquote>
<p>QUIC 的核心特性</p>
</blockquote>
<p><strong>1. 内建加密</strong>：QUIC 强制使用 TLS 1.3，不仅加密应用数据，连大部分传输层头部也加密。没有明文传输的选项。</p>
<p><strong>2. 独立 Stream</strong>：QUIC 的多路复用是在传输层原生实现的（不像 HTTP&#x2F;2 是在应用层），每个 Stream 有独立的序列号和重传机制，真正消除了队头阻塞。</p>
<p><strong>3. 0-RTT 恢复</strong>：客户端缓存之前协商的密钥参数，恢复连接时可以在握手的同时发送加密数据，实现 0-RTT。代价是 0-RTT 数据存在重放攻击风险，需要服务端做幂等保护。</p>
<p><strong>4. 连接迁移</strong>：TCP 用四元组（源IP、源端口、目的IP、目的端口）标识连接，IP 变了连接就断了。QUIC 用随机生成的 Connection ID 标识，网络切换时只要 Connection ID 不变，连接就可以无缝迁移。</p>
<blockquote>
<p>HTTP&#x2F;1.1 vs HTTP&#x2F;2 vs HTTP&#x2F;3（QUIC）总结</p>
</blockquote>
<table>
<thead>
<tr>
<th>维度</th>
<th>HTTP&#x2F;1.1</th>
<th>HTTP&#x2F;2</th>
<th>HTTP&#x2F;3（QUIC）</th>
</tr>
</thead>
<tbody><tr>
<td><strong>传输层</strong></td>
<td>TCP</td>
<td>TCP</td>
<td>UDP（QUIC）</td>
</tr>
<tr>
<td><strong>加密</strong></td>
<td>可选（HTTPS）</td>
<td>实践中强制 TLS</td>
<td>强制 TLS 1.3</td>
</tr>
<tr>
<td><strong>多路复用</strong></td>
<td>无（多连接并发）</td>
<td>有（单 TCP 连接）</td>
<td>有（单 QUIC 连接，Stream 级独立）</td>
</tr>
<tr>
<td><strong>队头阻塞</strong></td>
<td>HTTP 层 + TCP 层</td>
<td>TCP 层</td>
<td>无</td>
</tr>
<tr>
<td><strong>连接建立</strong></td>
<td>TCP 1 RTT + TLS</td>
<td>TCP 1 RTT + TLS</td>
<td>首次 1 RTT，恢复 0 RTT</td>
</tr>
<tr>
<td><strong>连接迁移</strong></td>
<td>不支持</td>
<td>不支持</td>
<td>支持（Connection ID）</td>
</tr>
</tbody></table>
<h2 id="综合"><a href="#综合" class="headerlink" title="综合"></a>综合</h2><blockquote>
<p>输入 URL 到渲染内容发生了什么</p>
</blockquote>
<h3 id="一、宏观端到端流程"><a href="#一、宏观端到端流程" class="headerlink" title="一、宏观端到端流程"></a>一、宏观端到端流程</h3><ol>
<li><strong>URL 解析</strong>：浏览器解析输入的 URL，提取协议（https）、域名（example.com）、端口（443）、路径（&#x2F;index.html）。</li>
<li><strong>DNS 解析</strong>：将域名解析为 IP 地址。查找顺序：浏览器缓存 → 操作系统缓存（&#x2F;etc&#x2F;hosts） → 本地 DNS 服务器（递归查询） → 根域名服务器 → 顶级域名服务器 → 权威域名服务器（迭代查询）。</li>
<li><strong>TCP 连接建立</strong>：浏览器与目标 IP:port 进行三次握手建立 TCP 连接。</li>
<li><strong>TLS 握手</strong>（HTTPS）：在 TCP 之上进行 TLS 握手，协商加密算法、交换密钥、验证服务端证书。TLS 1.2 需要额外 2 RTT，TLS 1.3 需要 1 RTT。</li>
<li><strong>发送 HTTP 请求</strong>：浏览器构造 HTTP 请求报文（请求行 + 请求头 + 请求体），通过已建立的加密连接发送。</li>
<li><strong>服务端处理</strong>：服务端（Nginx&#x2F;应用服务器）接收请求，可能涉及反向代理、负载均衡、路由分发、业务逻辑处理、数据库查询等，最终构造 HTTP 响应。</li>
<li><strong>接收 HTTP 响应</strong>：浏览器接收响应报文（状态行 + 响应头 + 响应体）。</li>
<li><strong>HTML 解析与渲染</strong>：<ul>
<li>解析 HTML 构建 <strong>DOM 树</strong></li>
<li>遇到 <code>&lt;link&gt;</code> 加载 CSS，解析构建 <strong>CSSOM 树</strong></li>
<li>遇到 <code>&lt;script&gt;</code> 加载并执行 JS（可能阻塞 DOM 解析，除非 async&#x2F;defer）</li>
<li>DOM + CSSOM 合成 <strong>渲染树</strong>（Render Tree）</li>
<li><strong>布局（Layout）</strong>：计算每个节点的位置和大小</li>
<li><strong>绘制（Paint）</strong>：将节点绘制成像素</li>
<li><strong>合成（Composite）</strong>：GPU 合成各图层，最终显示到屏幕</li>
</ul>
</li>
</ol>
<h3 id="二、微观视角-—-协议栈、中断与-Ring-Buffer"><a href="#二、微观视角-—-协议栈、中断与-Ring-Buffer" class="headerlink" title="二、微观视角 — 协议栈、中断与 Ring Buffer"></a>二、微观视角 — 协议栈、中断与 Ring Buffer</h3><p>以<strong>客户端发出 HTTP 请求、服务端响应数据到达客户端</strong>为例，展开收包链路：</p>
<p><strong>发送方向（客户端 → 网络）</strong>：</p>
<ol>
<li>应用层：浏览器调用 <code>write(fd, data)</code> &#x2F; <code>send()</code>，数据通过系统调用进入内核</li>
<li>传输层：TCP 将数据按 MSS 分段，加上 TCP 头部（序列号、端口等），放入发送队列</li>
<li>网络层：加上 IP 头部（源&#x2F;目的 IP），进行路由查找</li>
<li>数据链路层：加上以太网帧头（源&#x2F;目的 MAC），通过网卡驱动发送</li>
<li>网卡：将帧转成电信号&#x2F;光信号发到网线上</li>
</ol>
<p><strong>接收方向（网络 → 客户端浏览器）</strong>：</p>
<ol>
<li><strong>网卡收包</strong>：网卡接收电信号，还原成数据帧</li>
<li><strong>DMA 写入 Ring Buffer</strong>：网卡通过 DMA 将数据帧<strong>直接写入内核预分配的环形缓冲区</strong>，CPU 不参与数据搬运</li>
<li><strong>硬中断</strong>：网卡触发硬中断通知 CPU “有数据到了”。硬中断处理极短，只做两件事：记录时间戳 + 调度软中断（NAPI）</li>
<li><strong>软中断（NET_RX_SOFTIRQ）</strong>：<ul>
<li>从 Ring Buffer 中取出数据帧，分配 <code>skb</code>（socket buffer）</li>
<li>送入协议栈处理</li>
</ul>
</li>
<li><strong>网络层</strong>：<code>ip_rcv()</code> 解析 IP 头，校验，根据协议字段分发到传输层</li>
<li><strong>传输层</strong>：<code>tcp_v4_rcv()</code> 解析 TCP 头，根据四元组找到对应的 socket，将数据放入 socket 的接收队列（<code>sk_receive_queue</code>）</li>
<li><strong>唤醒进程</strong>：调用 <code>sk_data_ready</code> 回调，唤醒在 <code>read()</code> &#x2F; <code>recv()</code> &#x2F; <code>epoll_wait()</code> 上阻塞的进程</li>
<li><strong>应用层</strong>：浏览器从内核接收队列读出数据，交给 HTTP 解析器 → HTML 解析器 → 渲染引擎</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────────────┐</span><br><span class="line">│ 网卡收到帧                                                │</span><br><span class="line">│   ↓ DMA                                                  │</span><br><span class="line">│ Ring Buffer                                              │</span><br><span class="line">│   ↓ 硬中断 → 调度软中断                                    │</span><br><span class="line">│ 软中断（NAPI）取出 skb                                     │</span><br><span class="line">│   ↓                                                      │</span><br><span class="line">│ ip_rcv()  → 网络层解包                                     │</span><br><span class="line">│   ↓                                                      │</span><br><span class="line">│ tcp_v4_rcv() → 传输层解包 → 放入 sk_receive_queue          │</span><br><span class="line">│   ↓ sk_data_ready 唤醒                                    │</span><br><span class="line">│ 用户进程 read() 读取数据                                    │</span><br><span class="line">│   ↓                                                      │</span><br><span class="line">│ 浏览器 HTTP 解析 → DOM/CSSOM → 渲染                        │</span><br><span class="line">└──────────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>

<blockquote>
<p>DNS 的迭代查询和递归查询</p>
</blockquote>
<p><strong>递归查询</strong>：客户端问本地 DNS 服务器，本地 DNS <strong>代替客户端</strong>去逐级查询，最终把结果返回给客户端。客户端只发一次请求，等一个最终答案。</p>
<p><strong>迭代查询</strong>：本地 DNS 服务器向上级服务器查询时，上级服务器<strong>不代替查询</strong>，而是返回”你去问谁”，本地 DNS 再自己去问下一个服务器。</p>
<p><strong>实际查询流程</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">客户端 ──递归──→ 本地DNS服务器</span><br><span class="line">                    │</span><br><span class="line">                    ├─迭代→ 根域名服务器     → 返回 &quot;.com 找这个TLD服务器&quot;</span><br><span class="line">                    │</span><br><span class="line">                    ├─迭代→ .com TLD服务器    → 返回 &quot;example.com 找这个权威服务器&quot;</span><br><span class="line">                    │</span><br><span class="line">                    ├─迭代→ 权威DNS服务器     → 返回 &quot;example.com → 93.184.216.34&quot;</span><br><span class="line">                    │</span><br><span class="line">                    └──────→ 客户端          ← 最终 IP 地址</span><br></pre></td></tr></table></figure>

<p><strong>关键区分</strong>：</p>
<ul>
<li><strong>客户端 → 本地 DNS</strong>：<strong>递归查询</strong>（客户端发一次，本地 DNS 负责到底）</li>
<li><strong>本地 DNS → 根&#x2F;TLD&#x2F;权威</strong>：<strong>迭代查询</strong>（本地 DNS 自己一轮一轮地问）</li>
</ul>
<p><strong>DNS 缓存层级</strong>（越靠前越先命中则越快）：</p>
<ol>
<li><strong>浏览器缓存</strong>：Chrome 默认缓存 60s</li>
<li><strong>操作系统缓存</strong>：如 <code>/etc/hosts</code>、nscd&#x2F;systemd-resolved 缓存</li>
<li><strong>本地 DNS 服务器缓存</strong>：ISP 提供的 DNS 或 8.8.8.8 等公共 DNS，根据 TTL 缓存</li>
<li>以上都未命中才走完整的迭代查询链路</li>
</ol>
<p><strong>DNS 使用的传输层协议</strong>：</p>
<ul>
<li>默认 <strong>UDP 端口 53</strong>（查询报文通常很小，一个 UDP 包搞定）</li>
<li>如果响应超过 <strong>512 字节</strong>（如区域传输），会切换到 <strong>TCP 端口 53</strong></li>
<li>现代趋势：DoH（DNS over HTTPS）、DoT（DNS over TLS）提供加密 DNS 查询</li>
</ul>
<p><strong>域名层级结构</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">www . example . com .</span><br><span class="line"> │      │      │   │</span><br><span class="line"> │      │      │   └─ 根域（通常省略的最后一个点）</span><br><span class="line"> │      │      └─ 顶级域（TLD，如 .com .cn .org）</span><br><span class="line"> │      └─ 二级域（SLD，如 example）</span><br><span class="line"> └─ 主机名 / 子域名</span><br></pre></td></tr></table></figure>

<p>DNS 查询就是从根开始逐级缩小范围：根 → <code>.com</code> → <code>example.com</code> → <code>www.example.com</code>。</p>
<p><strong>TTL（Time To Live）</strong>：权威 DNS 返回的记录带 TTL（如 3600s &#x3D; 1小时），各级缓存按 TTL 决定缓存多久。TTL 到期后缓存失效，下次查询需要重新走迭代。TTL 越短 → DNS 变更生效越快，但缓存命中率低、DNS 压力大。</p>
<p><strong>常见 DNS 记录类型</strong>：</p>
<table>
<thead>
<tr>
<th>记录类型</th>
<th>含义</th>
<th>示例</th>
</tr>
</thead>
<tbody><tr>
<td><strong>A</strong></td>
<td>域名 → IPv4 地址</td>
<td><code>example.com → 93.184.216.34</code></td>
</tr>
<tr>
<td><strong>AAAA</strong></td>
<td>域名 → IPv6 地址</td>
<td><code>example.com → 2606:2800:220:1:...</code></td>
</tr>
<tr>
<td><strong>CNAME</strong></td>
<td>域名 → 另一个域名（别名）</td>
<td><code>www.example.com → cdn.example.net</code>，还需再查 cdn.example.net 的 A 记录</td>
</tr>
<tr>
<td><strong>MX</strong></td>
<td>邮件服务器记录</td>
<td><code>example.com → mail.example.com</code></td>
</tr>
<tr>
<td><strong>NS</strong></td>
<td>域名的权威 DNS 服务器</td>
<td><code>example.com → ns1.example.com</code></td>
</tr>
<tr>
<td><strong>TXT</strong></td>
<td>任意文本（常用于 SPF&#x2F;DKIM 验证）</td>
<td><code>v=spf1 include:_spf.google.com</code></td>
</tr>
</tbody></table>
<p><strong>CNAME 与 CDN</strong>：CDN 场景大量使用 CNAME。例如 <code>www.example.com</code> CNAME 到 <code>www.example.com.cdn.cloudflare.net</code>，CDN 的 DNS 再根据用户地理位置返回最近的边缘节点 IP。</p>
<p><strong>DNS 负载均衡</strong>：权威 DNS 可以对同一域名返回不同的 IP（轮询&#x2F;权重&#x2F;地理位置），实现简单的服务端负载均衡。但 DNS 负载均衡粒度粗、生效慢（受 TTL 影响），通常作为第一层分流，配合 Nginx&#x2F;LVS 等做更精细的负载均衡。</p>
<blockquote>
<p>正向代理与反向代理</p>
</blockquote>
<table>
<thead>
<tr>
<th></th>
<th>正向代理（Forward Proxy）</th>
<th>反向代理（Reverse Proxy）</th>
</tr>
</thead>
<tbody><tr>
<td><strong>代理谁</strong></td>
<td>代理<strong>客户端</strong></td>
<td>代理<strong>服务端</strong></td>
</tr>
<tr>
<td><strong>谁知道它的存在</strong></td>
<td>客户端知道，服务端不知道</td>
<td>服务端知道，客户端不知道</td>
</tr>
<tr>
<td><strong>典型场景</strong></td>
<td>VPN、科学上网、企业出口网关</td>
<td>Nginx、CDN、API Gateway</td>
</tr>
<tr>
<td><strong>隐藏谁</strong></td>
<td>隐藏客户端的真实 IP</td>
<td>隐藏后端服务器的真实 IP 和架构</td>
</tr>
</tbody></table>
<p>一句话区分：正向代理是”客户端的代言人”，反向代理是”服务端的代言人”。</p>
<p><strong>反向代理工作流程</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">客户端  ──→  反向代理（Nginx）  ──→  后端服务器 A</span><br><span class="line">                                ──→  后端服务器 B</span><br><span class="line">                                ──→  后端服务器 C</span><br></pre></td></tr></table></figure>

<p>客户端访问域名，DNS 指向反向代理的 IP。反向代理接收请求，根据规则转发给后端服务器，后端响应经反向代理返回给客户端。客户端全程不知道后端架构。</p>
<p><strong>反向代理的核心作用</strong>：</p>
<ol>
<li><strong>负载均衡</strong>：把请求分发到多台后端，策略有轮询、加权轮询、IP Hash、最少连接</li>
<li><strong>隐藏后端架构</strong>：后端用内网 IP，不暴露在公网，提升安全性</li>
<li><strong>SSL 终结</strong>：反向代理统一处理 TLS 握手和加解密，后端之间走 HTTP 明文，减轻后端压力</li>
<li><strong>缓存加速</strong>：缓存静态资源直接响应客户端，不必每次打到后端</li>
<li><strong>请求过滤&#x2F;限流</strong>：在代理层做 WAF、限速、黑白名单</li>
<li><strong>路径路由</strong>：根据 URL 路径分发到不同后端服务（<code>/api</code> → 应用服务器，<code>/static</code> → 文件服务器）</li>
</ol>
<p>CDN 本质上就是分布在全球的反向代理缓存节点。</p>
]]></content>
      <categories>
        <category>Network</category>
      </categories>
      <tags>
        <tag>计算机网络</tag>
        <tag>TCP</tag>
        <tag>HTTP</tag>
        <tag>QUIC</tag>
        <tag>DNS</tag>
        <tag>epoll</tag>
        <tag>IO多路复用</tag>
      </tags>
  </entry>
  <entry>
    <title>进程树</title>
    <url>/2026/03/11/notes/%E8%BF%9B%E7%A8%8B%E6%A0%91/</url>
    <content><![CDATA[<h1 id="进程树"><a href="#进程树" class="headerlink" title="进程树"></a>进程树</h1><blockquote>
<p>返回综述: <a href="%E8%BF%9B%E7%A8%8B%E7%AE%A1%E7%90%86.md">进程管理</a></p>
</blockquote>
<h2 id="进程树的数据结构"><a href="#进程树的数据结构" class="headerlink" title="进程树的数据结构"></a>进程树的数据结构</h2><p>在 <code>task_struct</code> 中, 通过以下字段构成一棵<strong>多叉双向树</strong>:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> __<span class="title">rcu</span> *<span class="title">real_parent</span>;</span> <span class="comment">// 生物学父进程: 谁fork了我</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> __<span class="title">rcu</span> *<span class="title">parent</span>;</span>     <span class="comment">// 法律上的父进程: 谁来wait我、收我的SIGCHLD</span></span><br><span class="line"><span class="comment">// 通常 real_parent == parent, 仅在 ptrace 调试时不同</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">children</span>;</span> <span class="comment">// 作为父进程时: 子进程链表的哨兵头(入口)</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">sibling</span>;</span>  <span class="comment">// 作为子进程时: 挂在父进程children链表上的节点</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *<span class="title">group_leader</span>;</span> <span class="comment">// 线程组的主线程(和进程树无关, 是线程组维度的字段)</span></span><br></pre></td></tr></table></figure>

<h3 id="parent-和-real-parent-为什么要有两个-parent"><a href="#parent-和-real-parent-为什么要有两个-parent" class="headerlink" title="parent 和 real_parent, 为什么要有两个 parent?"></a><code>parent</code> 和 <code>real_parent</code>, 为什么要有两个 parent?</h3><p>这个场景主要是为了应对 debug 即 ptrace 场景, 在不使用调试器的时候, 两者指向的都是父进程.<br>但是在使用调试器的场景下, <code>real_parent</code> 仍然是 fork parent, 但是 <code>parent</code> 指向 ptrace 调试器进程, 这个 <code>parent</code> 会接收 <code>wait</code> 和 <code>SIGCHLD</code> 信号</p>
<h3 id="children-和-sibling-是怎么构成一棵多叉树的"><a href="#children-和-sibling-是怎么构成一棵多叉树的" class="headerlink" title="children 和 sibling 是怎么构成一棵多叉树的?"></a><code>children</code> 和 <code>sibling</code> 是怎么构成一棵多叉树的?</h3><p>这是一个经典的 Linux 内核侵入式链表设计. 逻辑上等效于 LCRS(Left-Child Right-Sibling) 树:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">tree_node</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">tree_node</span> *<span class="title">parent</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">tree_node</span> *<span class="title">first_child</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">tree_node</span> *<span class="title">next_sibling</span>;</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>但 LCRS 的写法存在问题:</p>
<ol>
<li><code>task_struct</code> 关联了非常多的链表关系(进程树、全局进程列表、线程组等). LCRS 的遍历逻辑是进程树专用的, 每种链表关系都要单独维护一套增删&#x2F;遍历 API. 实际上链表操作是通用的, 应该和具体的使用场景解耦.</li>
<li>LCRS 通常是单向链表, 删除需要先遍历找前驱, 时间复杂度 O(n)</li>
<li>不能用动态数组存 <code>children</code>, 因为内核关键路径上绝不能有可能失败的内存分配</li>
</ol>
<p>因此 Linux 使用了侵入式链表: 把一个通用的链表节点(<code>list_head</code>)嵌入到数据结构内部:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> *<span class="title">next</span>, *<span class="title">prev</span>;</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>每个 <code>task_struct</code> 同时扮演两个角色:</p>
<ul>
<li><strong>作为父进程</strong>: <code>children</code> 是子进程链表的哨兵头(入口, 不代表任何子进程)</li>
<li><strong>作为子进程</strong>: <code>sibling</code> 是挂在父进程 <code>children</code> 链表上的普通节点</li>
</ul>
<p><code>children.next</code> 指向第一个子进程的 <code>sibling</code> 地址, 每个子进程的 <code>sibling.next</code> 指向下一个兄弟的 <code>sibling</code> 地址, 最后一个兄弟的 <code>sibling.next</code> 回到父进程的 <code>children</code>, 形成双向循环链表:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">父进程.children ←→ 子A.sibling ←→ 子B.sibling ←→ 子C.sibling ←→ (回到父进程.children)</span><br></pre></td></tr></table></figure>

<h3 id="fork-的时候-怎么将新子进程挂到父进程的进程树上"><a href="#fork-的时候-怎么将新子进程挂到父进程的进程树上" class="headerlink" title="fork 的时候, 怎么将新子进程挂到父进程的进程树上?"></a>fork 的时候, 怎么将新子进程挂到父进程的进程树上?</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// kernel/fork.c</span></span><br><span class="line"><span class="comment">// p 是新创建的子进程, p-&gt;real_parent 是执行fork的父进程</span></span><br><span class="line">list_add_tail(&amp;p-&gt;sibling, &amp;p-&gt;real_parent-&gt;children);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 等效于(伪代码):</span></span><br><span class="line"><span class="comment">// 把 p-&gt;sibling 插到 real_parent-&gt;children 链表的尾部</span></span><br><span class="line">tail = parent-&gt;children.prev;</span><br><span class="line">tail-&gt;next = &amp;p-&gt;sibling;</span><br><span class="line">p-&gt;sibling.prev = tail;</span><br><span class="line">p-&gt;sibling.next = &amp;parent-&gt;children;</span><br><span class="line">parent-&gt;children.prev = &amp;p-&gt;sibling;</span><br></pre></td></tr></table></figure>

<h3 id="遍历-怎么找到某个进程的所有子进程"><a href="#遍历-怎么找到某个进程的所有子进程" class="headerlink" title="遍历: 怎么找到某个进程的所有子进程?"></a>遍历: 怎么找到某个进程的所有子进程?</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *<span class="title">child</span>;</span></span><br><span class="line">list_for_each_entry(child, &amp;parent-&gt;children, sibling) &#123;</span><br><span class="line">    <span class="comment">// child 就是每个子进程的 task_struct *</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>list_for_each_entry</code> 展开后等效于:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> *<span class="title">pos</span> =</span> parent-&gt;children.next; <span class="comment">// 从哨兵头的next开始</span></span><br><span class="line"><span class="keyword">while</span> (pos != &amp;parent-&gt;children) &#123;             <span class="comment">// 走回哨兵头就停</span></span><br><span class="line">    child = container_of(pos, <span class="keyword">struct</span> task_struct, sibling);</span><br><span class="line">    <span class="comment">// ... 使用 child ...</span></span><br><span class="line">    pos = pos-&gt;next;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="container-of-怎么从-sibling-的地址反推出-task-struct-的地址"><a href="#container-of-怎么从-sibling-的地址反推出-task-struct-的地址" class="headerlink" title="container_of: 怎么从 sibling 的地址反推出 task_struct 的地址?"></a><code>container_of</code>: 怎么从 <code>sibling</code> 的地址反推出 <code>task_struct</code> 的地址?</h3><p><code>list_head</code> 内部没有 data 字段, 它不知道自己属于哪个 <code>task_struct</code>. 但因为 <code>sibling</code> 是嵌在 <code>task_struct</code> 内部的, 它在结构体中的偏移量是编译时确定的常量, 所以: <strong><code>sibling</code> 的地址 - 偏移量 &#x3D; <code>task_struct</code> 的起始地址</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// include/linux/container_of.h</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> container_of(ptr, type, member) \</span></span><br><span class="line"><span class="meta">    ((type *)((char *)(ptr) - offsetof(type, member)))</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 用法:</span></span><br><span class="line">container_of(&amp;某子进程.sibling, <span class="keyword">struct</span> task_struct, sibling)</span><br><span class="line"><span class="comment">// = &amp;某子进程.sibling 的地址 - sibling在task_struct中的偏移</span></span><br><span class="line"><span class="comment">// = 某子进程 task_struct 的起始地址</span></span><br></pre></td></tr></table></figure>

<p>这就是侵入式链表的核心: <strong>不是链表节点包含数据, 而是数据结构包含链表节点, 通过地址减偏移反推出数据结构地址.</strong></p>
<hr>
<h2 id="进程树为什么存在"><a href="#进程树为什么存在" class="headerlink" title="进程树为什么存在"></a>进程树为什么存在</h2><h3 id="为什么内核必须记录-parent、children、sibling-而不是只维护一个-PID-列表"><a href="#为什么内核必须记录-parent、children、sibling-而不是只维护一个-PID-列表" class="headerlink" title="为什么内核必须记录 parent、children、sibling, 而不是只维护一个 PID 列表?"></a>为什么内核必须记录 <code>parent</code>、<code>children</code>、<code>sibling</code>, 而不是只维护一个 PID 列表?</h3><p>只维护了 PID 列表, 相当于对于内核来说只知道有哪些进程, 而不知道这些进程之间的关系, 则需要亲缘关系的时候(如 <code>wait()</code>, 托孤, 信号传播), 都需要遍历 PID 列表来拿到亲缘关系, 时间复杂度 O(n), 但是维护了亲缘关系以后, 就只用遍历子进程, 变成了 O(子进程数)</p>
<h3 id="这些关系分别支撑了哪些操作-wait、发送信号、回收退出状态、托孤"><a href="#这些关系分别支撑了哪些操作-wait、发送信号、回收退出状态、托孤" class="headerlink" title="这些关系分别支撑了哪些操作: wait、发送信号、回收退出状态、托孤?"></a>这些关系分别支撑了哪些操作: <code>wait</code>、发送信号、回收退出状态、托孤?</h3><ul>
<li><strong>托孤</strong>: 父进程退出的时候, 在 exit 路径中通过 <code>forget_original_parent()</code> → <code>find_new_reaper()</code> 将自己所有的子进程的 <code>real_parent</code> 改成 init&#x2F;subreaper. 由 init 进程来调用 <code>wait()</code>&#x2F;<code>waitpid()</code> 来完成 <code>task_struct</code> 最后的 PID 资源的回收和 <code>exit_state</code> 的回收. 这个机制依赖于父进程通过 <code>children</code> 链表遍历所有子进程</li>
<li><strong>信号传播</strong>:<ol>
<li><code>kill(-pgid, sig)</code> 向整个进程组发信号</li>
<li>会话断开的时候, 会话 leader(常常是 bash&#x2F;zsh…) 进程退出, 发送 <code>SIGHUP</code> 信号, 沿进程树传播要通知所有的前台进程&#x2F;子进程, 子进程收到后退出, 如果没有树结构, 就只能全局扫描</li>
<li><code>parent</code> 支撑 <code>SIGCHLD</code>: 子进程退出的时候(<code>do_exit()</code> → <code>exit_notify()</code>), 内核通过 <code>parent</code> 指针找到父进程, 向他发送 <code>SIGCHLD</code> 信号, 通知父进程有个进程退出了, 调用 <code>wait()</code> 回收资源</li>
</ol>
</li>
<li><strong>回收状态&#x2F;<code>wait()</code></strong>: <code>do_wait()</code> 的实现就是遍历 <code>current-&gt;children</code> 链表, 逐个检查子进程的 <code>exit_state</code>, 找到 <code>EXIT_ZOMBIE</code> 的子进程回收, 没有 <code>children</code> 链表, 就无法找到自己的子进程, 可能会产生永久僵尸</li>
</ul>
<hr>
<h2 id="进程树和任务控制的关系"><a href="#进程树和任务控制的关系" class="headerlink" title="进程树和任务控制的关系"></a>进程树和任务控制的关系</h2><h3 id="进程树、进程组、Session-之间是什么关系"><a href="#进程树、进程组、Session-之间是什么关系" class="headerlink" title="进程树、进程组、Session 之间是什么关系?"></a>进程树、进程组、Session 之间是什么关系?</h3><p>三者是<strong>不同维度的组织方式, 层层嵌套但不相同</strong>:</p>
<ul>
<li><strong>进程组 (Process Group)</strong>: 由 PGID(Process Group ID) 标识, 通过 <code>task_pgrp()</code> 获取. 同一条管道里的进程共享同一个 PGID. 进程组 leader 的 PID &#x3D;&#x3D; PGID. 注意 PGID 和 TGID(Thread Group ID, 线程组ID) 是两个完全不同的概念.</li>
<li><strong>进程树 (Process Tree)</strong>: 维护所有进程之间的亲缘关系, 是 <code>fork()</code> 的产物. <code>fork()</code> 创建的子进程会继承父进程的 SID 和 PGID. <code>execve()</code> 不创建新进程, 只替换程序映像, PID&#x2F;PGID&#x2F;SID 保持不变. 进程树关系是自动的、不可变的; 进程组关系是 shell 按作业逻辑主动设置的、可变的.</li>
<li><strong>Session (会话)</strong>: 一次终端连接, 是最外层的容器, 包含一个或多个进程组. 通过 <code>setsid()</code> 能让一个非 leader 的进程创建一个新的 Session, 并成为这个 Session&#x2F;Group 的 leader. 这也是为什么守护进程要执行 <code>fork()</code> → <code>setsid()</code>: 如果调用者的 PID &#x3D;&#x3D; PGID(即它是进程组 leader), <code>setsid()</code> 会失败, 所以先 fork 出子进程(子进程 PID ≠ 父进程 PGID), 再在子进程中 <code>setsid()</code>. 如果一个 Session leader 终止了, 会发送 <code>SIGHUP</code> 信号到前台进程组.</li>
</ul>
<table>
<thead>
<tr>
<th>维度</th>
<th>进程树</th>
<th>进程组</th>
<th>会话</th>
</tr>
</thead>
<tbody><tr>
<td><strong>组织依据</strong></td>
<td>谁 fork 了谁</td>
<td>谁在做同一个 job</td>
<td>谁共享同一个终端</td>
</tr>
<tr>
<td><strong>标识符</strong></td>
<td>无(通过 <code>parent</code>&#x2F;<code>children</code> 指针)</td>
<td>PGID</td>
<td>SID</td>
</tr>
<tr>
<td><strong>建立时机</strong></td>
<td>fork 时自动建立</td>
<td>shell 启动 job 时 <code>setpgid()</code></td>
<td>登录时 <code>setsid()</code></td>
</tr>
<tr>
<td><strong>可否改变</strong></td>
<td>不可(除 ptrace&#x2F;托孤)</td>
<td>可以 <code>setpgid()</code></td>
<td>可以 <code>setsid()</code></td>
</tr>
<tr>
<td><strong>包含关系</strong></td>
<td>无层级嵌套</td>
<td>一个会话包含多个进程组</td>
<td>最外层容器</td>
</tr>
</tbody></table>
<h3 id="为什么-Ctrl-C-SIGINT-、Ctrl-Z-SIGTSTP-往往不是只作用在单个进程上"><a href="#为什么-Ctrl-C-SIGINT-、Ctrl-Z-SIGTSTP-往往不是只作用在单个进程上" class="headerlink" title="为什么 Ctrl+C (SIGINT)、Ctrl+Z (SIGTSTP) 往往不是只作用在单个进程上?"></a>为什么 <code>Ctrl+C</code> (<code>SIGINT</code>)、<code>Ctrl+Z</code> (<code>SIGTSTP</code>) 往往不是只作用在单个进程上?</h3><p>实际上是作用在一整个前台进程组上的. 在最经典的管道场景中, <code>cat file | grep foo | wc -l</code>, 如果只发给一个前台的进程, 就会导致 <code>cat</code> 被终止了, 但是 <code>grep</code>&#x2F;<code>wc</code> 还在运行, 还在往管道里面写数据, 行为就乱了</p>
<h3 id="这和-shell、前台任务、后台任务有什么关系"><a href="#这和-shell、前台任务、后台任务有什么关系" class="headerlink" title="这和 shell、前台任务、后台任务有什么关系?"></a>这和 shell、前台任务、后台任务有什么关系?</h3><ul>
<li><strong>shell</strong>: 是作业控制的实现者, 通过进程组和会话机制来管理前台&#x2F;后台任务, 启动时会通过 <code>setsid()</code> 成为 Session leader &#x2F; 进程组 leader</li>
<li><strong>前台任务</strong>: 是会接受终端输入的一组进程. 如 <code>cat file | grep foo</code>, shell 首先通过 <code>fork()</code> 创建出两个子进程, 然后调用 <code>setpgid()</code> 将这两个进程放到一个进程组里面, 调用 <code>tcsetpgrp()</code> 将这个进程组变成前台进程组. 终端的键盘信号都会发送给前台进程组, shell 自己调用 <code>waitpid()</code> 阻塞等待前台任务完成</li>
<li><strong>后台任务</strong>: 没有调用 <code>tcsetpgrp()</code> 这一个设置成前台进程组的步骤, 后台进程组不持有前台, 所以 <code>Ctrl+C</code> 不影响它</li>
</ul>
<table>
<thead>
<tr>
<th>操作</th>
<th>本质</th>
</tr>
</thead>
<tbody><tr>
<td><code>Ctrl+Z</code></td>
<td>终端驱动向前台进程组发 <code>SIGTSTP</code> → 整组暂停</td>
</tr>
<tr>
<td><code>jobs</code></td>
<td>shell 遍历自己管理的进程组列表, 显示状态</td>
</tr>
<tr>
<td><code>fg %1</code></td>
<td>shell 调用 <code>tcsetpgrp()</code> 把目标进程组设为前台 + 发 <code>SIGCONT</code> 恢复</td>
</tr>
<tr>
<td><code>bg %1</code></td>
<td>shell 向目标进程组发 <code>SIGCONT</code>, 但<strong>不</strong>设为前台 → 后台继续运行</td>
</tr>
<tr>
<td><code>kill %1</code></td>
<td>shell 向目标进程组发 <code>SIGTERM</code></td>
</tr>
</tbody></table>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul>
<li>进程树: linux kernel里面为进程之间的亲子关系维护的一颗双向多叉树, 使用linux侵入式链表设计, 进程树对于托孤机制(僵尸清理), 沿亲缘关系的传播的信号来说是不可获取的机制, 比如wait()操作本身就是遍历进程的所有的children, 挨个回收PID和退出状态.</li>
<li>shell任务控制: 任务分成前台任务和后台任务, 拿管道的运用举例子, 创建任务的步骤是shell fork出来一系列的子进程, 然后setpgid(), 将这些进程放到一个进程组里面. 对于前台进程(比如我们输入的命令), 还有一个额外的设置成前台进程的步骤(tcsetpgrp()), 前台进程会接受键盘信号(Ctrl + C &#x2F; Ctrl +Z). 这些信号传播的最小单元是一个进程组. 无论是前台任务还是后台任务, 都会共享一个session id来代表他们是经由这个session代表的shell进程创建的. shell会执行waitpid()来阻塞式等待, 并且完成最后的资源的清理. shell退出的时候, 会沿进程树的所有子进程的所在的进程组发送SIGHUP.</li>
<li>守护进程: daemon进程就是一个后台进程, 创建的核心就是要和原来的session解绑(创立新的pgid, sid), 然后不再是shell进程的子进程, 所有流程是fork(leader进程不能setsid) - 父进程不wait(成为孤儿进程, 挂在init下面) - setsid(重制pgid&#x2F;sid) - fork</li>
<li>僵尸进程: 由父进程不wait()&#x2F;waitpid(), 导致没有执行进程的最后的PID&#x2F;exit_state的回收导致的中间态死亡进程, 大量的僵尸进程会导致PID空间不足</li>
<li>孤儿进程: 父进程死亡, 子进程就变成了孤儿进程. 父进程exit的时候会调用exit() -&gt; forget_original_parent() -&gt; find_new_reaper()流程将子进程托孤给init&#x2F;subreaper, 这两类进程会定期执行wait()清理僵尸进程.</li>
</ul>
]]></content>
      <categories>
        <category>OS</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>进程管理</tag>
        <tag>进程树</tag>
        <tag>task_struct</tag>
        <tag>侵入式链表</tag>
      </tags>
  </entry>
  <entry>
    <title>进程管理</title>
    <url>/2026/03/08/notes/%E8%BF%9B%E7%A8%8B%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<h1 id="进程管理"><a href="#进程管理" class="headerlink" title="进程管理"></a>进程管理</h1><h2 id="进程数据结构"><a href="#进程数据结构" class="headerlink" title="进程数据结构"></a>进程数据结构</h2><p>进程的数据结构是task struct</p>
<ol>
<li>cpu资源:<ol>
<li>调度优先级</li>
</ol>
</li>
<li>内存地址空间资源:<ol>
<li>mm_struct</li>
</ol>
</li>
<li>打开的文件资源:<ol>
<li>file_struct files (一个数组, 存的就是打开的文件的地址, 索引即是文件描符 fd)</li>
</ol>
</li>
</ol>
<p>进程自己的信息与状态</p>
<ol>
<li>进程状态: 存储在task-&gt;state, task-&gt;exit_state两个字段中<ol>
<li>如TASK_RUNNING, TASK_INTERRUPTIBLE, TASK_UNINTERRUPTIBLE, __TASK_STOPED…</li>
</ol>
</li>
<li>唯一ID<ol>
<li>pid: 线程级别的id</li>
<li>gtid: 进程级别的id</li>
</ol>
</li>
<li>文件系统信息 <code>struct fs_struct *fs</code></li>
<li>namespace</li>
<li>进程树关系: 该进程在整个进程树里面的位置</li>
</ol>
<h3 id="进程的状态"><a href="#进程的状态" class="headerlink" title="进程的状态"></a>进程的状态</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* Used in tsk-&gt;state: */</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_RUNNING            0x00000000</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_INTERRUPTIBLE      0x00000001</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_UNINTERRUPTIBLE    0x00000002</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> __TASK_STOPPED          0x00000004</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> __TASK_TRACED           0x00000008</span></span><br><span class="line"><span class="comment">/* Used in tsk-&gt;exit_state: */</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> EXIT_DEAD               0x00000010</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> EXIT_ZOMBIE             0x00000020</span></span><br><span class="line"><span class="comment">/* Used in tsk-&gt;state again: */</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_DEAD               0x00000080</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* 组合状态（便利宏）: */</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_KILLABLE           (TASK_WAKEKILL | TASK_UNINTERRUPTIBLE)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_STOPPED            (TASK_WAKEKILL | __TASK_STOPPED)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_IDLE               (TASK_UNINTERRUPTIBLE | TASK_NOLOAD)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_NORMAL			(TASK_INTERRUPTIBLE | TASK_UNINTERRUPTIBLE)</span></span><br><span class="line"><span class="comment">// ...</span></span><br></pre></td></tr></table></figure>

<p>常见状态语义说明:</p>
<p><strong>存储在task-&gt;state的</strong></p>
<ul>
<li>TASK_RUNNING: 进程正在运行队列中, 准备运行&#x2F;正在运行</li>
<li>TASK_NORAML&#x2F;TASK_INTERRUPTIBLE&#x2F;TASK_UNINTERRUPTIBLE:<ul>
<li>TASK_NORMAL &#x3D; <code>(TASK_INTERRUPTIBLE | TASK_UNINTERRUPTIBLE)</code> 是这两个状态的综合, 表明进程正在睡眠</li>
<li>TASK_INTERRUPTIBLE: 进程处于可打断的睡眠状态, 正在等待某个条件满足, 被wake_up唤醒, 或者被信号唤醒. 不会被计入load average</li>
<li>TASK_UNINTERRUPTIBLE: 进程处于不可中断的睡眠状态, 只能在条件满足时, 被wake_up唤醒, 不能被信号唤醒, 常用于(如磁盘 I&#x2F;O)等不能被中途打断的SLEEP, 这也代表, 在这个状态的进程的数量能一定程度反应当前计算机的物理负载, 这个状态会被计入load average</li>
<li>TASK_KILLABLE &#x3D; <code>(TASK_WAKEKILL | TASK_UNINTERRUPTIBLE)</code> 可以被致命信号杀死(SIGKILL)的不可中断睡眠</li>
<li>__TASK_STOPPED &#x3D; 进程被暂停执行了, 不会被调度. 1. 收到SIGSTOP &#x2F; SIGSTP &#x2F; SIGTTIN &#x2F; SIGTTOU 信号 2. ptrace attach 后发送 SIGSTOP. 常见的进入情景: 1. Ctrl + z 2. debug. 退出的时机: 收到 SIGCONT 信号</li>
<li>__TASK_TRACED &#x3D; 进程正在被调试器跟踪</li>
<li>TASK_IDLE (TASK_UNINTERRUPTIBLE | TASK_NOLOAD):<ul>
<li>语义: TASK_UNINTERRUPTIBLE类似的不可中断睡眠, 但<strong>不计入 load average</strong></li>
<li>内核中空闲时会运行的 idle线程, 内核中某些无关紧要的等待</li>
<li>如果 idle线程用TASK_UNINTERRUPTIBLE而包含TASK_NOLOAD会导致load average空长, 不能反应真实物理资源负载</li>
</ul>
</li>
</ul>
</li>
</ul>
<p><strong>存储在task-&gt;exit_code的</strong></p>
<ul>
<li>EXIT_ZOMBIE: 僵尸状态<ul>
<li>语义: 代码已经执行完毕</li>
<li>进入的时机: 进程调用exit() -&gt; do_exit() -&gt; exit_notify() 中设置 tsk-&gt;exit_state &#x3D; EXIT_ZOMBIE</li>
<li>退出的时机: 父进程调用 <code>wait</code> &#x2F; <code>waitpid()</code> 读取退出状态 -&gt; <code>wait_task_zombie()</code> -&gt; <code>exit_state</code>中改为 <code>EXIT_DEAD</code> -&gt; <code>task_struct</code> 被释放</li>
</ul>
</li>
<li>EXIT_DEAD (0x10) : 彻底死亡<ul>
<li>语义: 进程的task_struct正在被回收或者已经被回收</li>
<li>进入的时机: 父进程 <code>wait()</code> 成功后, 状态从 <code>EXIT_ZOMBIE</code> 变成 <code>EXIT_DEAD</code></li>
<li>退出的时机: 这是一个瞬态, 存在的时间极短, 之后 <code>task_struct</code> 被回收</li>
</ul>
</li>
</ul>
<p><strong>常见的进程类型</strong></p>
<ul>
<li><p><strong>僵尸进程</strong>: 进程(task_struct)已经被释放, 但是还保留PID和exit_state为EXIT_ZOMBIE</p>
<ul>
<li>为什么需要这个状态: 父进程可能需要在进程被关闭以后, 获取进程的PID并检查它的退出码来衡量进程是不是正常退出的</li>
<li>资源泄漏: 状态为EXIT_ZOMBIE的进程占用一个PID和少量的内核内存. 如果大量堆积, 会导致内核内存泄漏, 或PID耗尽</li>
<li>怎么产生的: 父进程没有调用wait()或者waitpid()</li>
<li>解决方式:<ul>
<li>父进程调用 wait() &#x2F; waitpid()</li>
<li>设置 signal(SIGCHLD, SIG_IGN) 让内核自动回收 (do_notigy_parent返回true的时候直接设置状态为EXIT_DEAD)</li>
<li>杀死父进程 -&gt; 僵尸被init(PID&#x3D;1)收养 -&gt; init会自动 <code>wait()</code>清理</li>
</ul>
</li>
</ul>
</li>
<li><p><strong>孤儿进程</strong>: 进程的父进程先于子进程被杀死了, 子进程就成为了孤儿进程</p>
<ul>
<li>内核会将孤儿进程的父进程指向init, 或者当前 PID namespace 中的 subreaper 进程, init进程会定期调用 <code>wait()</code>来清理</li>
<li>本身是正常的, 不会造成问题</li>
</ul>
</li>
<li><p><strong>守护进程</strong>: 在后台运行的, 不再与任何终端关联的长期运行的进程. 如 <code>sshd, nginx</code>等</p>
<ul>
<li>不是一个内核概念, 而是一个用户空间的编程的概念</li>
<li>经典的创建过程: double-fork<ul>
<li><code>fork()</code> - 创建子进程</li>
<li>父进程退出 - 子进程被托孤给init</li>
<li>setsid - 创建新的session (和原终端解绑)</li>
<li>再次 <code>fork()</code> - 确保不会重新获得中断控制能力</li>
<li>chdir(“&#x2F;) - 避免占用可卸载的文件系统</li>
<li>关闭&#x2F;重定向 stdin&#x2F;stdout&#x2F;stderr</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="进程的唯一ID"><a href="#进程的唯一ID" class="headerlink" title="进程的唯一ID"></a>进程的唯一ID</h3><p><strong>tgid和pid, 为什么要有两个ID</strong><br>经典的用途是区分进程和线程, 本身是进程组概念, 父进程和他fork出来的子进程组成一个进程组, 共享一个tgid.<br>他们的id (pid, tgid)会是 (1, 1), (2, 1), (3, 1). 对于操作系统来说, 只有进程这个概念, 线程实际上是一个轻量级进程, 对于操作系统的调度来说, 进程和线程是同等地位的实体, 唯一通过PID来作为唯一标识, 而tgid是标识”进程”而非”线程”的唯一标识.</p>
<h3 id="进程树"><a href="#进程树" class="headerlink" title="进程树"></a>进程树</h3><p>通过 <code>task_struct</code> 中的 <code>parent</code>&#x2F;<code>real_parent</code>、<code>children</code>&#x2F;<code>sibling</code> 字段, 所有进程构成一棵<strong>多叉双向树</strong>. Linux 使用侵入式链表(<code>list_head</code>)而非传统多叉树来实现, 使得增删 O(1)、零内存分配、API 全内核复用.</p>
<p>进程树支撑了 <code>wait()</code> 回收、托孤、<code>SIGCHLD</code> 通知等核心操作. 进程树与进程组(PGID)、会话(Session) 共同构成了完整的作业控制体系.</p>
<p><strong>详细内容</strong>: <a href="%E8%BF%9B%E7%A8%8B%E6%A0%91.md">进程树</a></p>
<h2 id="进程调度"><a href="#进程调度" class="headerlink" title="进程调度"></a>进程调度</h2><h3 id="运行队列-struct-rq"><a href="#运行队列-struct-rq" class="headerlink" title="运行队列 struct rq"></a>运行队列 <code>struct rq</code></h3><p>内核声明了一个per-CPU变量: <code>runqueues</code>, 每个CPU都有自己的rq变量, 通过<code>cpu_rq(cpu)</code>等宏能执行拿到第N号CPU的运行队列指针等操作.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">DECLARE_PER_CPU_SHARED_ALIGNED(<span class="keyword">struct</span> rq, runqueues);</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> cpu_rq(cpu)		(&amp;per_cpu(runqueues, (cpu)))</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> this_rq()		this_cpu_ptr(&amp;runqueues)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> task_rq(p)		cpu_rq(task_cpu(p))</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> cpu_curr(cpu)		(cpu_rq(cpu)-&gt;curr)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> raw_rq()		raw_cpu_ptr(&amp;runqueues)</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>这个数据结构嵌入了<code>cfs_rq</code>, <code>rt_rq</code>, <code>dl_rq</code>三个内部运行队列(注意这里是嵌入, 这三个字段不是指针). 这意味着 rq本身就包含了所有调度类各自的运行队列数据. 每个调度类管自己的调度队列.</p>
</blockquote>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rq</span> &#123;</span></span><br><span class="line">	<span class="type">raw_spinlock_t</span>		__lock; <span class="comment">// 自旋锁, 保护整个队列的并发访问</span></span><br><span class="line"></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> __<span class="title">rcu</span>	*<span class="title">curr</span>;</span> <span class="comment">// 当前正在运行的进程</span></span><br><span class="line"></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">cfs_rq</span>		<span class="title">cfs</span>;</span> <span class="comment">// cfs rq, 完全公平调度队列, 直接嵌入</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">rt_rq</span>		<span class="title">rt</span>;</span> <span class="comment">// real time rq, 实时调度队列, 直接嵌入</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">dl_rq</span>		<span class="title">dl</span>;</span> <span class="comment">// deadline rq, 强实时调度队列, 有完成截止时间, 直接嵌入</span></span><br><span class="line"></span><br><span class="line">	<span class="type">unsigned</span> <span class="type">int</span>		nr_running; <span class="comment">// 一个CPU上最多有多少进程可以是running状态</span></span><br><span class="line">	u64			nr_switches; <span class="comment">// 上下文切换的次数</span></span><br><span class="line">  <span class="comment">// 其他...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="调度类-struct-sched-class"><a href="#调度类-struct-sched-class" class="headerlink" title="调度类 - struct sched_class"></a>调度类 - <code>struct sched_class</code></h3><p>调度类本身是一个虚函数表, 里面定义了一系列的虚函数(接口), 里面的各个函数实际上是函数指针. 可以把调度类就看作是一个interface&#x2F;vitrual class.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sched_class</span> &#123;</span></span><br><span class="line">  <span class="comment">// 把一个进程加入到该调度类的子队列</span></span><br><span class="line">	<span class="type">void</span> (*enqueue_task) (<span class="keyword">struct</span> rq *rq, <span class="keyword">struct</span> task_struct *p, <span class="type">int</span> flags);</span><br><span class="line">  <span class="comment">// 新进程唤醒的时候, 检查是否应该要抢占当前进程</span></span><br><span class="line">	<span class="type">void</span> (*check_preempt_curr)(<span class="keyword">struct</span> rq *rq, <span class="keyword">struct</span> task_struct *p, <span class="type">int</span> flags);</span><br><span class="line">  <span class="comment">// 把一个进程移除到该调度类的子队列</span></span><br><span class="line">	<span class="type">void</span> (*dequeue_task) (<span class="keyword">struct</span> rq *rq, <span class="keyword">struct</span> task_struct *p, <span class="type">int</span> flags);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 从该调度类中选出下一个要运行的进程</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *(*<span class="title">pick_next_task</span>)(<span class="keyword">struct</span> <span class="title">rq</span> *<span class="title">rq</span>);</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 当前进程被换下前的清理工作, 比如对于cfs调度器来说, 就会重新计算vruntime, 然后重新放回cfs.timeline(红黑树)里面</span></span><br><span class="line">	<span class="type">void</span> (*put_prev_task)(<span class="keyword">struct</span> rq *rq, <span class="keyword">struct</span> task_struct *p);</span><br><span class="line">  <span class="comment">// 新进程被换上前的初始化工作</span></span><br><span class="line">	<span class="type">void</span> (*set_next_task)(<span class="keyword">struct</span> rq *rq, <span class="keyword">struct</span> task_struct *p, <span class="type">bool</span> first);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 每个时钟tick时被调用, 检查时间片/vruntime</span></span><br><span class="line">	<span class="type">void</span> (*task_tick)(<span class="keyword">struct</span> rq *rq, <span class="keyword">struct</span> task_struct *p, <span class="type">int</span> queued);</span><br><span class="line">  <span class="comment">// 更新当前进程的运行时间</span></span><br><span class="line">  <span class="type">void</span> (*update_curr)(<span class="keyword">struct</span> rq *rq);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>调度类的实现类有五个</p>
<ol>
<li>stop_sched_class</li>
<li>dl_sched_class</li>
<li>rt_sched_class</li>
<li>fair_sched_class</li>
<li>idle_sched_class</li>
</ol>
<p>这五个类实现了上面的各个函数, 使用不同的策略和子队列.<br>每个task_struct里面都一个sched_class字段, 指向这个进程对应的调度类全局实例(看成是静态类即可)</p>
<p>在rq挑选下一个任务的时候, 会按照上面的顺序遍历所有的调度类, 尝试调用他们各自的pick_next_task.<br>如果调度到的不是NULL, 返回找到的任务.</p>
<h3 id="调度的整体运行流程"><a href="#调度的整体运行流程" class="headerlink" title="调度的整体运行流程"></a>调度的整体运行流程</h3><blockquote>
<p><code>rq</code>是容器, <code>sched_class</code>是行为, 子队列 (cfs_rq, rt_rq, dl_rq) 是各调度类在容器内的私有存储队列. 三者通过<code>task_struct.sched_class</code>桥接</p>
</blockquote>
<p><strong>1. 进程入队: 从唤醒到进入到子队列里面</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">try_wake_up(task_struct* p)</span><br><span class="line">  -&gt; activate_task(rq, p, flags)</span><br><span class="line">    -&gt; enqueue_task(rq, p, flags)</span><br><span class="line">      -&gt; p-&gt;sched_class-&gt;enqueue_task</span><br></pre></td></tr></table></figure>
<ul>
<li>如果这里的进程是普通的进程, 这里的p-&gt;sched_class就是fair_sched_class,  p-&gt;sched_class-&gt;enqueue_task最后调用的就是enqueue_task_fair(), 这个函数会将p插入到cfs rq中的红黑树子队列中</li>
<li>如果p是RT进程, 实际调用的就是enqueue_task_rt()</li>
</ul>
<p><strong>2. 进程出队: 睡眠或退出</strong></p>
<p>__schedule()<br>  -&gt; deactivate_task(rq, prev, DEQUEUE_SLEEP)<br>    -&gt; dequeue_task(rq, prev, flags)<br>      -&gt; p-&gt;sched_class-&gt;dequeue_task(rq, p, flags)</p>
<p><strong>3. 选择下一个进程: pick_next_task的两条路径</strong></p>
<p><em>这里的pick_next_task并不是调度类里面的这个函数, 而是sched.h里面的一个文件</em></p>
<p>__scheduler()<br>  -&gt; pick_next_task(rq, prev, &amp;rf)<br>    -&gt; __pick_next_task(rq, prev, rf)</p>
<p><strong>快速路径</strong></p>
<p>rq-&gt;nr_running &#x3D;&#x3D; rq-&gt;cfs.h_nr_running ? (全部进程都是normal&#x2F;CFS进程)<br>  -&gt; pick_next_task_fair(rq, prev, rf)<br>    -&gt; 从rq-&gt;cfs.tasks_timeline 取出来最左边的节点</p>
<p><strong>慢速路径</strong></p>
<p>for_each_class(class)<br>  class-&gt;pick_next_task(rq)<br>  -&gt; stop_sched_class.pick_next_task(rq) -&gt; 检查 rq-&gt;stop<br>  -&gt; dl_sched_class.pick_next_task(rq)   -&gt; 检查 rq-&gt;dl 红黑树<br>  -&gt; rt_sched_class.pick_next_task(rq)   -&gt; 检查 rq-&gt;rt 优先级数组<br>  -&gt; fair_sched_class.pick_next_task(rq) -&gt; 检查 rq-&gt;cfs 红黑树<br>  -&gt; idle_sched_class.pick_next_task(rq) -&gt; 返回 rq-&gt;idle（兜底）</p>
<p>for_each_class是一个宏, 会遍历所有的sched_class, 尝试调用每个调度类自己实现的pick_next_task, 直到返回的不是NULL, 返回. 这样就构造出来了不同rq&#x2F;调度器之间的优先级问题.</p>
<h3 id="将task-struct挂载在运行队列上"><a href="#将task-struct挂载在运行队列上" class="headerlink" title="将task_struct挂载在运行队列上"></a>将task_struct挂载在运行队列上</h3><p><em>这里主要聚焦的是, rq中的子队列, 比如cfs, 是把task_struct直接存在里面的吗, 是怎么挂载上去的?</em></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span>&#123;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">sched_entity</span>		<span class="title">se</span>;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">sched_rt_entity</span>		<span class="title">rt</span>;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">sched_dl_entity</span>		<span class="title">dl</span>;</span></span><br><span class="line">	<span class="type">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">sched_class</span>	*<span class="title">sched_class</span>;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>同样是侵入式数据结构设计, 通过将这几个嵌入式的字段挂载在task_struct中, 通过container_of来取出task_struct.<br>同时rb_node嵌入到sched_entity中, 来将这个数据结构挂在红黑树上</p>
<p><em>为什么不直接操作task_struct呢?</em></p>
<p>在cgroup分组调度下, CFS需要把“一组进程”当成一个整体来调度, sched_entity可以代表一个进程, 也可以代表一个进程组.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sched_entity</span> &#123;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">load_weight</span>		<span class="title">load</span>;</span> <span class="comment">// 该实体的权重, 通过nice值转化出来的, CFS通过这个计算出来应该分到多少CPU时间</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">rb_node</span>			<span class="title">run_node</span>;</span> <span class="comment">//  红黑树节点, 挂在rq.cfs_rq-&gt;task_timeline上</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">list_head</span>		<span class="title">group_node</span>;</span> <span class="comment">// 挂在rq-&gt;cfs_tasks链表上, 用于SMP均衡负载</span></span><br><span class="line">	<span class="type">unsigned</span> <span class="type">int</span>			on_rq; <span class="comment">// 是否在运行队列里面</span></span><br><span class="line"></span><br><span class="line">	u64				exec_start; <span class="comment">// 当前这一轮开始执行的时间戳, 每次tick或调度的时候更新</span></span><br><span class="line">	u64				sum_exec_runtime; <span class="comment">// 累计实际的运行时间</span></span><br><span class="line">	u64				vruntime; <span class="comment">// 虚拟运行时间, 值越小越优先被选中</span></span><br><span class="line">	u64				prev_sum_exec_runtime; <span class="comment">// 上次调度时候实际运行时间的快照, 用来算, 这次用了多少的时间片</span></span><br><span class="line">	u64				nr_migrations; <span class="comment">// 这个entity被迁移到其他的cpu上的次数</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>sched_entity 是 CFS 调度器的操作单元，嵌入在 task_struct.se 中。它通过 run_node 挂在 cfs_rq 的红黑树上，以 vruntime 为排序键。CFS 选人就是取 vruntime 最小的 sched_entity，再通过 container_of 反推到 task_struct。分组调度下，一个 sched_entity 可以代表整个 task_group，通过 parent&#x2F;cfs_rq&#x2F;my_q 构成层级树。</p>
</blockquote>
]]></content>
      <categories>
        <category>OS</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>进程管理</tag>
        <tag>task_struct</tag>
      </tags>
  </entry>
  <entry>
    <title>01 · Core Concepts</title>
    <url>/2026/04/01/kafka/notes/01-core-concepts/</url>
    <content><![CDATA[<h1 id="01-·-Core-Concepts"><a href="#01-·-Core-Concepts" class="headerlink" title="01 · Core Concepts"></a>01 · Core Concepts</h1><blockquote>
<p><strong>目标</strong>：理解 Kafka 的四个核心抽象及其关系，能画出消息从 produce 到 consume 的完整路径</p>
</blockquote>
<hr>
<h2 id="核心概念"><a href="#核心概念" class="headerlink" title="核心概念"></a>核心概念</h2><h3 id="Topic"><a href="#Topic" class="headerlink" title="Topic"></a>Topic</h3><p>Topic 是逻辑概念，是消息的命名分类单位（如 <code>order-events</code>、<code>user-clicks</code>）。Topic 本身不存储数据，真正存储数据的是 partition。</p>
<h3 id="Partition"><a href="#Partition" class="headerlink" title="Partition"></a>Partition</h3><p>Partition 是物理存储单元。每个 partition 在磁盘上是一个<strong>目录</strong>，目录下包含多个 segment 文件（<code>.log</code>、<code>.index</code>、<code>.timeindex</code>）。Kafka 按大小或时间把数据切割成多个 segment，避免单个文件无限增长。</p>
<p>Partition 解决两个问题：</p>
<ul>
<li><strong>并行度</strong>：多个 partition 分布在不同 broker 上，读写可以同时进行，吞吐量线性扩展。消费端同理，N 个 partition 最多支持 N 个消费者并行消费。</li>
<li><strong>水平扩展</strong>：partition 是 Kafka 的最小分布单元，不同 partition 可以在不同 broker 上，单个 topic 的数据量不受单台机器限制。</li>
</ul>
<p>注意：<strong>partition 内部消息严格有序，partition 之间没有顺序保证。</strong> 相同 key 的消息会路由到同一个 partition，这是实现”同一实体的事件有序”的机制。</p>
<h3 id="Offset"><a href="#Offset" class="headerlink" title="Offset"></a>Offset</h3><p>Offset 是消费者在 partition 中的位置指针，是一个递增整数，存在于 <strong>partition 级</strong>（不是 topic 级）。</p>
<p><strong>Offset 由消费者自己管理，不是 broker 管。</strong> 消费者定期把当前 offset checkpoint 到 Kafka 内部的特殊 topic <code>__consumer_offsets</code>，崩溃重启后从上次 checkpoint 的位置继续消费。</p>
<p>传统消息系统让 broker 追踪消费状态，带来两难困境：</p>
<ul>
<li>broker 立刻标记已消费 → consumer 崩溃会丢消息</li>
<li>等 consumer 确认 → 确认前崩溃会重复消费，且 broker 要为每条消息维护多个状态，性能差</li>
</ul>
<p>Kafka 的解法让状态极简：每个 partition 只需一个整数。代价是 <strong>at-least-once</strong> 语义——消息不会丢，但可能重复消费（处理完但提交 offset 前崩溃）。Exactly-once 需要额外机制（幂等 producer + 事务）。</p>
<p>Offset 带来的副作用也是核心特性：<strong>消费者可以主动倒回任意 offset 重放历史消息</strong>，传统队列消费后即删除做不到这点。</p>
<h3 id="Consumer-Group"><a href="#Consumer-Group" class="headerlink" title="Consumer Group"></a>Consumer Group</h3><p>Consumer Group 是一组消费者的逻辑分组，共同消费一个 topic。</p>
<p><strong>核心规则：同一个 group 内，每个 partition 同一时刻只被一个消费者消费。</strong></p>
<p>这个约束是 offset 能用单个整数表示的前提——如果多个消费者同时消费同一个 partition，就需要复杂协调机制，状态就不再是一个简单数字了。</p>
<p>两种消费模式：</p>
<ul>
<li><strong>组内（负载均衡）</strong>：partition 在组内消费者间分配，每条消息只被一个消费者处理。消费者数量不能超过 partition 数量，超出的消费者会 idle（没有 partition 可分配）。</li>
<li><strong>组间（广播）</strong>：不同 group 独立维护各自的 offset，同一条消息对每个 group 完整可见。</li>
</ul>
<p>判断是否同一个 group 的标准：<strong>这些消费者是在协作处理同一件事（用同一个 group），还是独立处理不同的事（用不同的 group）？</strong></p>
<p>例：库存系统和物流系统都消费 <code>order-events</code>，它们是<strong>不同的 group</strong>——各自需要完整看到所有订单事件，互不影响。</p>
<hr>
<h2 id="消息从-produce-到-consume-的完整路径"><a href="#消息从-produce-到-consume-的完整路径" class="headerlink" title="消息从 produce 到 consume 的完整路径"></a>消息从 produce 到 consume 的完整路径</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Producer</span><br><span class="line">  → 按 key 路由到指定 partition（相同 key → 同一 partition）</span><br><span class="line">  → 写入 broker 上的 partition（append-only log）</span><br><span class="line">  → Consumer 从 broker pull 数据（指定 offset）</span><br><span class="line">  → 处理消息</span><br><span class="line">  → 提交新的 offset 到 __consumer_offsets</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="参考材料"><a href="#参考材料" class="headerlink" title="参考材料"></a>参考材料</h2><ol>
<li><strong>Kafka 官方文档 — Introduction</strong>：<a href="https://kafka.apache.org/42/getting-started/introduction">https://kafka.apache.org/42/getting-started/introduction</a></li>
<li><strong>Kafka 设计文档 — The Consumer</strong>：<a href="https://kafka.apache.org/42/design/design/%EF%BC%88%E6%89%BE">https://kafka.apache.org/42/design/design/（找</a> <code>The Consumer → Consumer Position</code>）</li>
<li><strong>LinkedIn 工程博客 — The Log</strong>：<a href="https://engineering.linkedin.com/distributed-systems/log-what-every-software-engineer-should-know-about-real-time-datas-unifying">https://engineering.linkedin.com/distributed-systems/log-what-every-software-engineer-should-know-about-real-time-datas-unifying</a></li>
</ol>
<hr>
<h2 id="CLI-验证"><a href="#CLI-验证" class="headerlink" title="CLI 验证"></a>CLI 验证</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 进入容器（apache/kafka:4.2.0）</span></span><br><span class="line">docker <span class="built_in">exec</span> -it &lt;container-name&gt; bash</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建 topic，3 个 partition</span></span><br><span class="line">/opt/kafka/bin/kafka-topics.sh --create --topic test-topic \</span><br><span class="line">  --partitions 3 --replication-factor 1 \</span><br><span class="line">  --bootstrap-server localhost:9092</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看 partition 分布</span></span><br><span class="line">/opt/kafka/bin/kafka-topics.sh --describe --topic test-topic \</span><br><span class="line">  --bootstrap-server localhost:9092</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看 consumer group offset 状态</span></span><br><span class="line">/opt/kafka/bin/kafka-consumer-groups.sh --describe --group group-A \</span><br><span class="line">  --bootstrap-server localhost:9092</span><br><span class="line"><span class="comment"># 观察：CURRENT-OFFSET / LOG-END-OFFSET / LAG 三个字段</span></span><br></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>kafka</tag>
        <tag>topic</tag>
        <tag>partition</tag>
        <tag>offset</tag>
        <tag>consumer-group</tag>
      </tags>
  </entry>
  <entry>
    <title>02 · 日志抽象与 Push vs Pull</title>
    <url>/2026/04/01/kafka/notes/02-log-and-pull/</url>
    <content><![CDATA[<h1 id="02-·-日志抽象与-Push-vs-Pull"><a href="#02-·-日志抽象与-Push-vs-Pull" class="headerlink" title="02 · 日志抽象与 Push vs Pull"></a>02 · 日志抽象与 Push vs Pull</h1><blockquote>
<p><strong>目标</strong>：理解 Kafka 的存储基础——日志抽象，以及消费模型选择 Pull 的设计动机</p>
</blockquote>
<hr>
<h2 id="核心概念"><a href="#核心概念" class="headerlink" title="核心概念"></a>核心概念</h2><h3 id="一、什么是日志"><a href="#一、什么是日志" class="headerlink" title="一、什么是日志"></a>一、什么是日志</h3><p>日志是最简单的存储抽象：<strong>只追加、全局有序、按时间排列的记录序列。</strong></p>
<p>每条记录追加到末尾，每条记录被分配一个唯一的单调递增序号。这个序号定义了一种”时间”概念——序号小的记录比序号大的旧。</p>
<p><strong>关键性质：这种”时间”与物理时钟无关。</strong></p>
<p>在分布式系统里，不同机器的物理时钟不同步，即使同步了，同一毫秒内发生的两件事也无法用物理时间判断先后。日志序号解决的是<strong>全局定序</strong>问题——整个系统里所有事件有一个统一的、无歧义的顺序。这是 Kafka 能作为”事件的单一来源”的基础。</p>
<p>注意区分两种”日志”：</p>
<ul>
<li><strong>应用日志</strong>（log4j、syslog）：给人读的非结构化文本</li>
<li><strong>数据日志</strong>（Kafka partition）：给程序读的结构化序列，是这里讨论的概念</li>
</ul>
<hr>
<h3 id="二、只追加带来的三个好处"><a href="#二、只追加带来的三个好处" class="headerlink" title="二、只追加带来的三个好处"></a>二、只追加带来的三个好处</h3><p><strong>1. 顺序写性能极高</strong></p>
<p>磁盘随机写慢，顺序写可以接近内存速度。日志只追加意味着所有写入都是顺序的，Kafka 因此能在普通机械硬盘上达到很高的吞吐量。</p>
<p><strong>2. 消息不因消费而删除，天然支持重放</strong></p>
<p>传统队列消费一条删一条。Kafka 的 partition 只追加，消息按 retention 策略保留（如 7 天）。重放不是 Kafka 特意实现的功能，而是日志本来就不删的自然结果——结合 offset 机制，回退 offset 即可重放。</p>
<p><strong>3. offset 是一个整数就够了</strong></p>
<p>只追加保证了日志是连续递增的有序序列。offset 作为分界线，能将序列精确地划分成两部分：offset 之前是已消费的，offset 之后是未消费的。</p>
<p>如果日志可以修改或删除过去的消息，这个分界线就会失效——offset 左边的内容可能已经变了，一个整数无法再准确表达消费进度。</p>
<hr>
<h3 id="三、Push-vs-Pull"><a href="#三、Push-vs-Pull" class="headerlink" title="三、Push vs Pull"></a>三、Push vs Pull</h3><p><strong>Push 模式的根本问题：broker 控制速率，消费者被动接收。</strong></p>
<p>生产速率超过消费速率时，消费者会被压垮。要解决这个问题，broker 需要感知每个消费者的处理能力并动态调整——类似 TCP 的流量控制，实现复杂，且不同消费者能力差异大，broker 很难精确控制。</p>
<p><strong>Pull 模式的优点：消费者自己控制节奏。</strong></p>
<p>消费者落后了慢慢追，能力强就多拉，broker 完全不需要感知消费者状态。同时天然支持批量拉取——每次拉取当前 offset 之后所有可用消息，批量大小由消费者决定。</p>
<p><strong>Pull 模式引入的新问题：空轮询。</strong></p>
<p>broker 没有数据时，消费者不断发请求，broker 不断回”没有数据”，白白消耗资源。</p>
<p><strong>Kafka 的解决方案：长轮询（long poll）。</strong></p>
<p>消费者发出请求后，如果 broker 暂时没有数据，请求阻塞等待，直到有数据或超时才返回。保留了 Pull 的优点，同时避免了空轮询的浪费。</p>
<hr>
<h2 id="参考材料"><a href="#参考材料" class="headerlink" title="参考材料"></a>参考材料</h2><ol>
<li><strong>LinkedIn 工程博客 — The Log</strong>：<a href="https://engineering.linkedin.com/distributed-systems/log-what-every-software-engineer-should-know-about-real-time-datas-unifying%EF%BC%88Part">https://engineering.linkedin.com/distributed-systems/log-what-every-software-engineer-should-know-about-real-time-datas-unifying（Part</a> One: What is a log?）</li>
<li><strong>Kafka 设计文档 — Push vs Pull</strong>：<a href="https://kafka.apache.org/42/design/design/%EF%BC%88%E6%89%BE">https://kafka.apache.org/42/design/design/（找</a> <code>The Consumer → Push vs. pull</code>）</li>
</ol>
]]></content>
      <tags>
        <tag>kafka</tag>
        <tag>log</tag>
        <tag>append-only</tag>
        <tag>pull</tag>
        <tag>long-poll</tag>
      </tags>
  </entry>
  <entry>
    <title>03 · 存储设计：Segment、Page Cache、零拷贝、Retention、Log Compaction</title>
    <url>/2026/04/01/kafka/notes/03-storage/</url>
    <content><![CDATA[<h1 id="03-·-存储设计：Segment、Page-Cache、零拷贝、Retention、Log-Compaction"><a href="#03-·-存储设计：Segment、Page-Cache、零拷贝、Retention、Log-Compaction" class="headerlink" title="03 · 存储设计：Segment、Page Cache、零拷贝、Retention、Log Compaction"></a>03 · 存储设计：Segment、Page Cache、零拷贝、Retention、Log Compaction</h1><blockquote>
<p><strong>目标</strong>：理解 Kafka partition 在磁盘上的物理结构，以及高性能背后的存储设计</p>
</blockquote>
<hr>
<h2 id="核心概念"><a href="#核心概念" class="headerlink" title="核心概念"></a>核心概念</h2><h3 id="一、Segment：Partition-的物理存储单元"><a href="#一、Segment：Partition-的物理存储单元" class="headerlink" title="一、Segment：Partition 的物理存储单元"></a>一、Segment：Partition 的物理存储单元</h3><p>一个 partition 目录下有多组文件，每组对应一个 Segment：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/data/kafka/order-events-0/</span><br><span class="line">├── 00000000000000000000.log        ← segment 1 的消息数据</span><br><span class="line">├── 00000000000000000000.index      ← segment 1 的 offset 稀疏索引</span><br><span class="line">├── 00000000000000000000.timeindex  ← segment 1 的时间索引</span><br><span class="line">├── 00000000000001048576.log        ← segment 2</span><br><span class="line">├── 00000000000001048576.index</span><br><span class="line">├── 00000000000001048576.timeindex</span><br><span class="line">└── ...</span><br></pre></td></tr></table></figure>

<p>文件名是该 segment <strong>第一条消息的 offset</strong>，光看文件名就知道每个 segment 的起始位置。</p>
<p><strong>三种文件的作用：</strong></p>
<ul>
<li><strong><code>.log</code></strong>：真正存消息，append-only，消息按顺序写入</li>
<li><strong><code>.index</code></strong>：稀疏 offset 索引，不是每条消息都有记录，而是每隔若干字节记一条，用于快速定位消息的物理位置</li>
<li><strong><code>.timeindex</code></strong>：按时间戳索引，用于按时间查找消息</li>
</ul>
<p><strong>Segment 切割的触发条件（满足任意一个）：</strong></p>
<ul>
<li>文件大小达到 <code>log.segment.bytes</code>（默认 1GB）</li>
<li>最新一条消息的时间超过 <code>log.segment.ms</code>（默认 7 天）</li>
<li><code>.index</code> 文件满了（<code>log.index.size.max.bytes</code>，默认 10MB）</li>
</ul>
<p><strong>为什么切割成多个 segment：</strong></p>
<ol>
<li><strong>删除高效</strong>：Retention 到期时直接删整个 segment 文件，O(1) 操作，不需要在大文件里截断</li>
<li><strong>查找高效</strong>：根据 offset 快速定位 segment，再用索引缩小范围，最后顺序扫描</li>
</ol>
<p><strong>完整的 offset 查找流程：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">目标 offset</span><br><span class="line">→ 根据 segment 文件名（起始 offset）二分定位到对应 segment</span><br><span class="line">→ 在该 segment 的 .index 文件里二分找到最近的稀疏索引点</span><br><span class="line">→ 从索引点对应的物理位置开始顺序扫描 .log 文件</span><br><span class="line">→ 找到目标 offset 的消息</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="二、Page-Cache：为什么不在-JVM-堆内缓存"><a href="#二、Page-Cache：为什么不在-JVM-堆内缓存" class="headerlink" title="二、Page Cache：为什么不在 JVM 堆内缓存"></a>二、Page Cache：为什么不在 JVM 堆内缓存</h3><p>Kafka 刻意不在 JVM 堆内维护消息缓存，而是直接写入文件系统，依赖 OS 的 page cache。</p>
<p><strong>三个好处：</strong></p>
<ol>
<li><strong>避免 GC 压力</strong>：JVM 对象内存开销通常是原始数据的 2 倍，大堆导致 GC 停顿严重。依赖 page cache 几乎不带来额外开销</li>
<li><strong>OS 缓存管理更智能</strong>：OS 自动做预读（read-ahead）和延迟写（write-behind），不需要 Kafka 自己实现</li>
<li><strong>进程重启不丢缓存</strong>：broker 重启时 JVM 堆缓存全部丢失，但 OS page cache 还在，重启后无需重新预热</li>
</ol>
<p><strong>实际效果</strong>：大多数情况下 Consumer 消费的消息还在 page cache 里，拉取时根本不需要读磁盘，延迟极低。</p>
<hr>
<h3 id="三、零拷贝（sendfile）"><a href="#三、零拷贝（sendfile）" class="headerlink" title="三、零拷贝（sendfile）"></a>三、零拷贝（sendfile）</h3><p><strong>传统文件发送：四次拷贝</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">磁盘 → 内核 page cache（DMA）→ 用户空间缓冲区（CPU）→ Socket 缓冲区（CPU）→ 网卡（DMA）</span><br></pre></td></tr></table></figure>

<p>两次 CPU 拷贝 + 两次用户态↔内核态上下文切换。</p>
<p><strong>Kafka 使用 sendfile（Java: <code>FileChannel.transferTo</code>）：两次拷贝</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">磁盘 → 内核 page cache（DMA）→ 网卡（DMA）</span><br></pre></td></tr></table></figure>

<p>数据完全在内核空间流转，<strong>CPU 拷贝从 2 次变成 0 次</strong>。”零拷贝”的”零”指的是零次 CPU 拷贝，不是零次 DMA 拷贝。</p>
<p><strong>为什么 Kafka 能用零拷贝：</strong></p>
<p>前提是 broker 不需要修改消息内容，只是透传。所有消息处理（压缩、加密等）都交给 Producer 和 Consumer 两端，broker 保持”哑管道”。如果 broker 需要解密或转换消息，数据必须进入用户空间，零拷贝就无法使用。</p>
<hr>
<h3 id="四、Retention：按时间-大小整段删除"><a href="#四、Retention：按时间-大小整段删除" class="headerlink" title="四、Retention：按时间&#x2F;大小整段删除"></a>四、Retention：按时间&#x2F;大小整段删除</h3><p>消息不因消费而删除，按 Retention 策略保留：</p>
<ul>
<li><strong>按时间</strong>（<code>log.retention.hours</code>，默认 168 小时）：segment 内最新消息超过保留时间，整个 segment 被删除</li>
<li><strong>按大小</strong>（<code>log.retention.bytes</code>）：partition 总大小超过阈值，从最旧的 segment 开始删除</li>
</ul>
<p>删除单位是整个 segment 文件，高效且简单。</p>
<hr>
<h3 id="五、Log-Compaction：按-key-保留最新值"><a href="#五、Log-Compaction：按-key-保留最新值" class="headerlink" title="五、Log Compaction：按 key 保留最新值"></a>五、Log Compaction：按 key 保留最新值</h3><p>对每个 key，只保留最新的那条消息，旧值被后台线程清理。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">写入：key=user1,value=A → key=user1,value=B → key=user1,value=C</span><br><span class="line">Compaction 后：key=user1,value=C</span><br></pre></td></tr></table></figure>

<p><strong>删除操作</strong>：写入 value&#x3D;null 的消息（tombstone），Compaction 后该 key 从 log 里消失。</p>
<p><strong>Retention vs Log Compaction：</strong></p>
<table>
<thead>
<tr>
<th></th>
<th>Retention</th>
<th>Log Compaction</th>
</tr>
</thead>
<tbody><tr>
<td>删除依据</td>
<td>时间 &#x2F; 大小</td>
<td>key 的旧值被新值覆盖</td>
</tr>
<tr>
<td>保留内容</td>
<td>一段时间内的所有消息</td>
<td>每个 key 的最新值</td>
</tr>
<tr>
<td>适用场景</td>
<td>事件流（订单、点击、日志）</td>
<td>状态流（配置、用户信息）</td>
</tr>
<tr>
<td>配置</td>
<td><code>log.retention.hours</code></td>
<td><code>log.cleanup.policy=compact</code></td>
</tr>
</tbody></table>
<hr>
<h2 id="参考材料"><a href="#参考材料" class="headerlink" title="参考材料"></a>参考材料</h2><ol>
<li><strong>Kafka 设计文档 — Persistence</strong>：<a href="https://kafka.apache.org/42/design/design/%EF%BC%88%E6%89%BE">https://kafka.apache.org/42/design/design/（找</a> <code>Persistence</code> 章节）</li>
<li><strong>Kafka 设计文档 — Log Compaction</strong>：<a href="https://kafka.apache.org/42/design/design/%EF%BC%88%E6%89%BE">https://kafka.apache.org/42/design/design/（找</a> <code>Log Compaction</code> 章节）</li>
<li><strong>Kafka 实现文档 — Log</strong>：<a href="https://kafka.apache.org/42/implementation/log/">https://kafka.apache.org/42/implementation/log/</a></li>
<li><strong>Kafka 实现文档 — Network Layer</strong>：<a href="https://kafka.apache.org/42/implementation/network-layer/">https://kafka.apache.org/42/implementation/network-layer/</a></li>
</ol>
]]></content>
      <tags>
        <tag>kafka</tag>
        <tag>storage</tag>
        <tag>segment</tag>
        <tag>page-cache</tag>
        <tag>zero-copy</tag>
        <tag>retention</tag>
        <tag>log-compaction</tag>
      </tags>
  </entry>
  <entry>
    <title>05 · Replication / ISR / Leader Election</title>
    <url>/2026/04/01/kafka/notes/05-replication/</url>
    <content><![CDATA[<h1 id="03-·-Replication-ISR-Leader-Election"><a href="#03-·-Replication-ISR-Leader-Election" class="headerlink" title="03 · Replication &#x2F; ISR &#x2F; Leader Election"></a>03 · Replication &#x2F; ISR &#x2F; Leader Election</h1><blockquote>
<p><strong>目标</strong>：理解 Kafka 如何通过副本机制保证数据不丢，以及 ISR、Leader Election、acks 的设计取舍</p>
</blockquote>
<hr>
<h2 id="核心概念"><a href="#核心概念" class="headerlink" title="核心概念"></a>核心概念</h2><h3 id="一、为什么需要副本"><a href="#一、为什么需要副本" class="headerlink" title="一、为什么需要副本"></a>一、为什么需要副本</h3><p>单台 broker 随时可能挂掉。如果 partition 只存在一台 broker 上，broker 挂了数据就永远丢了。</p>
<p>解决方案是副本（Replica）：每个 partition 在多台 broker 上各存一份。副本数量由 <code>replication factor</code> 控制，比如 replication factor &#x3D; 3，意味着这个 partition 在 3 台不同的 broker 上各有一份完整数据。</p>
<hr>
<h3 id="二、Leader-和-Follower"><a href="#二、Leader-和-Follower" class="headerlink" title="二、Leader 和 Follower"></a>二、Leader 和 Follower</h3><p>多个副本里，有且只有一个是 <strong>Leader</strong>，其余是 <strong>Follower</strong>。</p>
<ul>
<li><strong>Leader</strong>：负责所有读写请求。Producer 写给 Leader，Consumer 也从 Leader 读。</li>
<li><strong>Follower</strong>：唯一职责是从 Leader 同步数据，保持冗余备份，不直接服务读写请求。</li>
</ul>
<p><strong>为什么不让 Follower 分担读请求？</strong></p>
<p>从 Follower 读不会读到”错误的数据”（日志只追加，已同步的数据和 Leader 完全一致），但会读到<strong>滞后的视图</strong>——Follower 可能还没同步到最新的几条消息。要解决这个问题需要引入复杂的一致性协议。Kafka 的取舍是用”只读 Leader”换实现的简洁性。</p>
<p>（Kafka 2.4 之后引入了有条件的 Follower 读，是进阶内容。）</p>
<hr>
<h3 id="三、ISR"><a href="#三、ISR" class="headerlink" title="三、ISR"></a>三、ISR</h3><p><strong>ISR（In-Sync Replicas）</strong> 是与 Leader 保持同步的副本集合，是一个动态维护的列表。</p>
<p>Follower 持续从 Leader 拉取数据。如果某个 Follower 落后超过阈值（<code>replica.lag.time.max.ms</code>），就会被踢出 ISR。追上之后重新加入。</p>
<p>注意：ISR 只保证”没有落后超过阈值”，不保证和 Leader 完全一致。新 Leader 选出后，可能有少量还未同步的消息丢失。</p>
<p><strong>ISR 的核心作用：只有 ISR 里的副本才有资格成为新的 Leader。</strong></p>
<hr>
<h3 id="四、Leader-Election"><a href="#四、Leader-Election" class="headerlink" title="四、Leader Election"></a>四、Leader Election</h3><p>当 Leader 所在的 broker 挂掉，Kafka 从 ISR 里选一个 Follower 升级为新 Leader，Producer 和 Consumer 自动切换。</p>
<p><strong>如果 ISR 为空怎么办？</strong> 由 <code>unclean.leader.election.enable</code> 控制：</p>
<ul>
<li><strong>false（默认）</strong>：只允许从 ISR 里选 Leader。ISR 为空时 partition 直接不可用——Producer 写入异常，Consumer 消费失败，直到原 Leader 的 broker 恢复上线。宁可不可用，也不丢数据。</li>
<li><strong>true</strong>：允许从 ISR 之外的落后副本选 Leader。Partition 继续服务，但可能有大量数据丢失。保证了可用性，牺牲了数据完整性。</li>
</ul>
<p>这是<strong>可用性 vs 数据完整性</strong>的经典取舍，Kafka 默认优先保数据。</p>
<hr>
<h3 id="五、acks：Producer-对”写入成功”的定义"><a href="#五、acks：Producer-对”写入成功”的定义" class="headerlink" title="五、acks：Producer 对”写入成功”的定义"></a>五、acks：Producer 对”写入成功”的定义</h3><table>
<thead>
<tr>
<th>acks</th>
<th>含义</th>
<th>风险</th>
</tr>
</thead>
<tbody><tr>
<td>0</td>
<td>发出去就认为成功，不等任何确认</td>
<td>可能丢消息</td>
</tr>
<tr>
<td>1</td>
<td>Leader 写入成功即返回</td>
<td>Leader 挂了但 Follower 未同步，消息丢失</td>
</tr>
<tr>
<td>all（-1）</td>
<td>ISR 里所有副本写入成功才返回</td>
<td>延迟高，ISR 缩小时可能阻塞写入</td>
</tr>
</tbody></table>
<ul>
<li><strong>acks&#x3D;0 &#x2F; 1</strong>：最大化性能，接受数据丢失风险</li>
<li><strong>acks&#x3D;all</strong>：最大化数据持久性，接受更高延迟和潜在的可用性下降</li>
</ul>
<p>注意：acks&#x3D;all 不等于绝对不丢。如果 ISR 里只剩一个副本，acks&#x3D;all 退化成 acks&#x3D;1。真正强持久性需要同时配置 <code>min.insync.replicas &gt;= 2</code>，保证 ISR 里至少有 2 个副本才允许写入。</p>
<hr>
<h2 id="参考材料"><a href="#参考材料" class="headerlink" title="参考材料"></a>参考材料</h2><ol>
<li><strong>Kafka 设计文档 — Replication</strong>：<a href="https://kafka.apache.org/42/design/design/%EF%BC%88%E6%89%BE">https://kafka.apache.org/42/design/design/（找</a> <code>Replication</code> 章节）</li>
</ol>
]]></content>
      <tags>
        <tag>kafka</tag>
        <tag>replication</tag>
        <tag>ISR</tag>
        <tag>leader-election</tag>
        <tag>acks</tag>
      </tags>
  </entry>
  <entry>
    <title>06 · Producer：路由、批量、压缩、幂等、事务、Exactly-Once</title>
    <url>/2026/04/01/kafka/notes/06-producer/</url>
    <content><![CDATA[<h1 id="06-·-Producer：路由、批量、压缩、幂等、事务、Exactly-Once"><a href="#06-·-Producer：路由、批量、压缩、幂等、事务、Exactly-Once" class="headerlink" title="06 · Producer：路由、批量、压缩、幂等、事务、Exactly-Once"></a>06 · Producer：路由、批量、压缩、幂等、事务、Exactly-Once</h1><blockquote>
<p><strong>目标</strong>：理解 Producer 的完整工作机制——从消息如何路由、如何提升吞吐，到如何保证数据可靠性</p>
</blockquote>
<hr>
<h2 id="一、key-路由：消息写入哪个-Partition"><a href="#一、key-路由：消息写入哪个-Partition" class="headerlink" title="一、key 路由：消息写入哪个 Partition"></a>一、key 路由：消息写入哪个 Partition</h2><p>Producer 直接把消息发给目标 partition 的 leader broker，没有中间路由层。路由方式有三种：</p>
<p><strong>1. 指定 key（语义分区）</strong><br>对 key 做哈希映射到某个 partition。相同 key 的消息永远进同一个 partition，实现<strong>局部保序</strong>。</p>
<p>局部保序的核心价值：<strong>让有因果关系的事件在消费侧天然有序，简化业务逻辑。</strong></p>
<p>例：用 order_id 作为 key，同一个订单的”创建、支付、发货”三个事件全在同一个 partition，消费者按顺序读就能拿到完整的状态变更序列，不需要跨 partition 聚合。</p>
<p><strong>2. 随机分配（无 key）</strong><br>消息随机分配到各个 partition，实现负载均衡，不保证顺序。</p>
<p><strong>3. 自定义分区函数</strong><br>完全覆盖默认的 hash 策略，实现业务定制的路由逻辑。</p>
<hr>
<h2 id="二、批量发送：用延迟换吞吐"><a href="#二、批量发送：用延迟换吞吐" class="headerlink" title="二、批量发送：用延迟换吞吐"></a>二、批量发送：用延迟换吞吐</h2><p>Producer 不是每条消息单独发一次网络请求，而是在内存里积累一批消息，满足条件后一次性发出。</p>
<p>两个触发条件（满足任意一个就发送）：</p>
<ul>
<li><strong><code>batch.size</code></strong>（默认 16KB）：积累的消息达到这个大小</li>
<li><strong><code>linger.ms</code></strong>（默认 0ms）：等待时间超过这个值</li>
</ul>
<p>调大 <code>linger.ms</code> 会引入少量延迟，但让更多消息积累进同一个 batch，吞吐量更高。<strong>核心取舍：用少量额外延迟换更高吞吐。</strong></p>
<hr>
<h2 id="三、端到端批量压缩"><a href="#三、端到端批量压缩" class="headerlink" title="三、端到端批量压缩"></a>三、端到端批量压缩</h2><p>压缩率取决于数据中的重复内容。批量压缩比单条压缩效率高得多，消息之间的相似内容（字段名、重复 key 等）可以被充分利用。</p>
<p><strong>压缩数据的生命周期：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Producer 批量压缩</span><br><span class="line">→ 发给 Broker（压缩状态）</span><br><span class="line">→ Broker 解压验证（只做校验，不存明文）</span><br><span class="line">→ 以压缩形式写入磁盘</span><br><span class="line">→ 以压缩形式发给 Consumer</span><br><span class="line">→ Consumer 解压</span><br></pre></td></tr></table></figure>

<p>数据在磁盘和网络传输中全程保持压缩状态。支持算法：GZIP、Snappy、LZ4、ZStandard。</p>
<p><strong>与零拷贝的关系：互补，不冲突。</strong> 压缩在 Producer 端完成，Broker 收到的已经是压缩好的数据，Broker 不修改内容，Broker → Consumer 这段依然可以用零拷贝。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Producer（压缩）→ Broker（零拷贝透传）→ Consumer（解压）</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="四、at-least-once-的重复写入问题"><a href="#四、at-least-once-的重复写入问题" class="headerlink" title="四、at-least-once 的重复写入问题"></a>四、at-least-once 的重复写入问题</h2><p>Kafka 默认是 at-least-once：消息不会丢，但可能重复。重复来源是网络不确定性：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Producer 发送消息 → Broker 写入成功</span><br><span class="line">→ 网络故障，Producer 没收到确认</span><br><span class="line">→ Producer 以为失败，重试</span><br><span class="line">→ Broker 收到重复消息，再次写入</span><br></pre></td></tr></table></figure>

<p>即使 acks&#x3D;all 也存在这个问题——Producer 无法区分”broker 没收到”和”broker 收到了但确认没回来”。</p>
<hr>
<h2 id="五、幂等-Producer：解决单-Partition-的重复写入"><a href="#五、幂等-Producer：解决单-Partition-的重复写入" class="headerlink" title="五、幂等 Producer：解决单 Partition 的重复写入"></a>五、幂等 Producer：解决单 Partition 的重复写入</h2><p>开启方式：<code>enable.idempotence=true</code>（Kafka 3.0 之后默认开启）</p>
<p><strong>实现机制：PID + Sequence Number</strong></p>
<ul>
<li>每个 Producer 启动时被分配唯一的 Producer ID（PID）</li>
<li>每条消息发送时携带单调递增的序列号</li>
<li>Broker 收到消息时检查 PID + Sequence Number：第一次见到正常写入，已写入过则丢弃但返回成功</li>
</ul>
<p><strong>两个限制：</strong></p>
<ol>
<li>只保证<strong>单个 partition</strong> 内的幂等性</li>
<li>PID 是 session 级别的，Producer 重启后幂等性失效</li>
</ol>
<hr>
<h2 id="六、事务：跨-Partition-的原子写入"><a href="#六、事务：跨-Partition-的原子写入" class="headerlink" title="六、事务：跨 Partition 的原子写入"></a>六、事务：跨 Partition 的原子写入</h2><p>事务解决跨 partition 的原子性——一批消息要么全部对 Consumer 可见，要么全部不可见。</p>
<p><strong>Transaction Coordinator（TC）</strong> 是 broker 上的组件，负责协调事务状态。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Producer 开启事务 → 向 TC 注册 transactional.id</span><br><span class="line">→ 写入多个 partition 的消息（带事务标记）</span><br><span class="line">→ 向 TC 提交事务</span><br><span class="line">→ TC 指示各 partition 追加 COMMIT 控制消息</span><br><span class="line">→ Broker 将该事务从未完成列表移除 → LSO 推进</span><br><span class="line">→ read_committed 的 Consumer 才能看到这批消息</span><br></pre></td></tr></table></figure>

<p>失败时 abort，各 partition 追加 ABORT 控制消息，Consumer 跳过。COMMIT&#x2F;ABORT 都是追加到 log 末尾的控制消息，不修改原有消息。</p>
<hr>
<h2 id="七、LSO（Last-Stable-Offset）"><a href="#七、LSO（Last-Stable-Offset）" class="headerlink" title="七、LSO（Last Stable Offset）"></a>七、LSO（Last Stable Offset）</h2><p>Broker 维护 LSO &#x3D; 所有未完成事务中最早那条消息的 offset。</p>
<p><code>read_committed</code> 的 Consumer 只能读到 LSO 之前的消息。<strong>关键风险：长时间未提交的事务会阻塞所有 <code>read_committed</code> Consumer。</strong> 事务必须尽量短。</p>
<hr>
<h2 id="八、Exactly-Once"><a href="#八、Exactly-Once" class="headerlink" title="八、Exactly-Once"></a>八、Exactly-Once</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">enable.idempotence=true</span><br><span class="line">+ transactional.id=&lt;唯一ID&gt;</span><br><span class="line">+ isolation.level=read_committed（Consumer 侧）</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="九、三种语义对比"><a href="#九、三种语义对比" class="headerlink" title="九、三种语义对比"></a>九、三种语义对比</h2><table>
<thead>
<tr>
<th>语义</th>
<th>配置</th>
<th>可能丢消息</th>
<th>可能重复</th>
<th>适用场景</th>
</tr>
</thead>
<tbody><tr>
<td>at-most-once</td>
<td>acks&#x3D;0</td>
<td>是</td>
<td>否</td>
<td>日志、监控等可丢场景</td>
</tr>
<tr>
<td>at-least-once</td>
<td>acks&#x3D;1 或 all，无幂等</td>
<td>否</td>
<td>是</td>
<td>大多数业务场景，消费端自己做幂等</td>
</tr>
<tr>
<td>exactly-once</td>
<td>幂等 + 事务 + read_committed</td>
<td>否</td>
<td>否</td>
<td>金融、账务等不能容忍重复的场景</td>
</tr>
</tbody></table>
<p>大多数场景用 at-least-once，在消费端做幂等处理（如数据库唯一键约束）往往比开启 Kafka 事务更简单、开销更小。</p>
<hr>
<h2 id="参考材料"><a href="#参考材料" class="headerlink" title="参考材料"></a>参考材料</h2><ol>
<li><strong>Kafka 设计文档 — The Producer</strong>：<a href="https://kafka.apache.org/42/design/design/">https://kafka.apache.org/42/design/design/</a></li>
<li><strong>Kafka 设计文档 — Efficiency</strong>：<a href="https://kafka.apache.org/42/design/design/">https://kafka.apache.org/42/design/design/</a></li>
<li><strong>Kafka 设计文档 — Exactly Once Semantics</strong>：<a href="https://kafka.apache.org/42/design/design/">https://kafka.apache.org/42/design/design/</a></li>
</ol>
]]></content>
      <tags>
        <tag>kafka</tag>
        <tag>producer</tag>
        <tag>routing</tag>
        <tag>batching</tag>
        <tag>compression</tag>
        <tag>idempotent</tag>
        <tag>transaction</tag>
        <tag>exactly-once</tag>
        <tag>LSO</tag>
      </tags>
  </entry>
  <entry>
    <title>07 · Consumer：Rebalance 与 Offset 提交策略</title>
    <url>/2026/04/01/kafka/notes/07-consumer/</url>
    <content><![CDATA[<h1 id="05-·-Consumer：Rebalance-与-Offset-提交策略"><a href="#05-·-Consumer：Rebalance-与-Offset-提交策略" class="headerlink" title="05 · Consumer：Rebalance 与 Offset 提交策略"></a>05 · Consumer：Rebalance 与 Offset 提交策略</h1><blockquote>
<p><strong>目标</strong>：理解 Consumer Group 如何协调 partition 分配，以及 offset 提交策略的取舍</p>
</blockquote>
<hr>
<h2 id="核心概念"><a href="#核心概念" class="headerlink" title="核心概念"></a>核心概念</h2><h3 id="一、Group-Coordinator-与-Group-Leader"><a href="#一、Group-Coordinator-与-Group-Leader" class="headerlink" title="一、Group Coordinator 与 Group Leader"></a>一、Group Coordinator 与 Group Leader</h3><p><strong>Group Coordinator（GC）</strong>：某个 Broker 上的组件，负责管理 Consumer Group 的成员关系和协调流程。不负责计算分配方案，只负责协调过程和下发结果。</p>
<p><strong>Group Leader（GL）</strong>：Coordinator 从 JoinGroup 的成员中选出的一个 Consumer，负责计算 partition 分配方案，计算完成后发回给 Coordinator，再由 Coordinator 通知所有成员。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Consumer 启动</span><br><span class="line">→ 找到 Group Coordinator（特定 broker）</span><br><span class="line">→ 发送 JoinGroup 请求</span><br><span class="line">→ Coordinator 选出 Group Leader</span><br><span class="line">→ Group Leader 计算 partition 分配方案</span><br><span class="line">→ 发回给 Coordinator</span><br><span class="line">→ Coordinator 把方案下发给所有 Consumer</span><br><span class="line">→ 每个 Consumer 开始消费分配到的 partition</span><br></pre></td></tr></table></figure>

<p><strong>为什么由 Group Leader 计算而不是 Coordinator？</strong><br>分配策略是可插拔的（轮询、按 key、自定义等），放在客户端计算更灵活，Coordinator 只需协调流程，不需要理解业务分配逻辑。</p>
<hr>
<h3 id="二、Rebalance：触发时机与代价"><a href="#二、Rebalance：触发时机与代价" class="headerlink" title="二、Rebalance：触发时机与代价"></a>二、Rebalance：触发时机与代价</h3><p><strong>触发时机：</strong></p>
<ul>
<li>有新的 Consumer 加入 group</li>
<li>有 Consumer 正常离开 group</li>
<li>Consumer 心跳超时（Coordinator 认为它已死）</li>
<li>Group 订阅的 topic 新增或减少 partition</li>
</ul>
<p><strong>心跳超时是最常见的坑：</strong> Consumer 没有崩溃，只是处理一批消息太慢，长时间没发心跳，Coordinator 认为它死了，触发 Rebalance，把它的 partition 分给别人。Consumer 处理完想提交 offset，发现自己已经不属于这个 group 了，提交失败。解决方式是合理配置 <code>max.poll.interval.ms</code> 和 <code>session.timeout.ms</code>。</p>
<p><strong>经典协议的全局同步屏障（Kafka 4.0 之前）：</strong></p>
<p>Rebalance 开始时，所有 Consumer 必须停止消费，等所有成员重新发送 JoinGroup，Group Leader 重新计算分配方案，Coordinator 下发，所有成员响应，才能继续消费。</p>
<p>代价高的两个原因：</p>
<ol>
<li><strong>范围太大</strong>：任何一个成员的变化都让整个 group 全部停下来</li>
<li><strong>等最慢的节点</strong>：整个过程需要等所有成员响应，group 越大，越容易遇到慢节点，整体耗时越长</li>
</ol>
<p><strong>Kafka 4.0 新协议（KIP-848）：</strong></p>
<p>增量式分配，只调整受影响的 partition，未受影响的 Consumer 继续消费，不再需要全局同步屏障。配置 <code>group.protocol=consumer</code>，Kafka 4.0 默认开启。</p>
<hr>
<h3 id="三、Offset-提交策略"><a href="#三、Offset-提交策略" class="headerlink" title="三、Offset 提交策略"></a>三、Offset 提交策略</h3><p>Consumer 消费后需要提交 offset 到 <code>__consumer_offsets</code>。</p>
<p><strong>1. 自动提交（enable.auto.commit&#x3D;true，默认）</strong></p>
<p>每隔 <code>auto.commit.interval.ms</code>（默认 5000ms）在后台自动提交。</p>
<p>两个问题：</p>
<ul>
<li><strong>重复消费</strong>：两次提交之间 Consumer 崩溃，这段时间的消息重启后重新消费（at-least-once）</li>
<li><strong>消息丢失（更严重）</strong>：消息被拉下来还在异步处理中，自动提交定时器触发，offset 提交成功，Consumer 崩溃。重启后从新 offset 继续，那批还没处理完的消息永远丢失——消息被提交了但没处理完</li>
</ul>
<p><strong>2. 手动提交</strong></p>
<ul>
<li><strong>commitSync（同步提交）</strong>：处理完一批后调用，阻塞等待提交成功再继续。安全不丢，但吞吐低。</li>
<li><strong>commitAsync（异步提交）</strong>：提交请求发出后不等结果，继续处理下一批。吞吐高，但失败时<strong>不自动重试</strong>——用旧 offset 重试可能覆盖已成功提交的新 offset，导致重复消费。</li>
</ul>
<p><strong>实践中的结合方式：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">正常消费循环：commitAsync（保持吞吐）</span><br><span class="line">Consumer 关闭时：commitSync（确保最后一次提交成功）</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="四、auto-offset-reset"><a href="#四、auto-offset-reset" class="headerlink" title="四、auto.offset.reset"></a>四、auto.offset.reset</h3><p>新 Consumer Group 第一次消费，或 offset 已过期被清理时的行为：</p>
<table>
<thead>
<tr>
<th>配置值</th>
<th>行为</th>
</tr>
</thead>
<tbody><tr>
<td>latest（默认）</td>
<td>从当前最新消息开始，历史消息全部忽略</td>
</tr>
<tr>
<td>earliest</td>
<td>从最早消息开始，消费所有历史</td>
</tr>
<tr>
<td>none</td>
<td>直接抛异常，要求必须有已知 offset</td>
</tr>
</tbody></table>
<hr>
<h3 id="五、Static-Membership：减少不必要的-Rebalance"><a href="#五、Static-Membership：减少不必要的-Rebalance" class="headerlink" title="五、Static Membership：减少不必要的 Rebalance"></a>五、Static Membership：减少不必要的 Rebalance</h3><p><strong>问题背景：</strong></p>
<p>默认情况下，Consumer 每次重启（滚动部署、JVM crash）都会触发两次 Rebalance——旧成员离开一次，新成员加入一次。对有状态的流处理应用（Kafka Streams）代价极高，不仅消费暂停，本地处理状态还要重新构建。</p>
<p><strong>解法：给每个 Consumer 实例配置持久化的 Group Instance ID</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ConsumerConfig.GROUP_INSTANCE_ID_CONFIG = &quot;consumer-instance-1&quot;</span><br></pre></td></tr></table></figure>

<p>Coordinator 识别这个固定 ID：</p>
<ul>
<li><strong>Consumer 重启</strong>：Coordinator 发现是同一个 Instance ID 回来了，不触发 Rebalance，直接恢复之前的 partition 分配</li>
<li><strong>Consumer 真正离线</strong>（超过 <code>session.timeout.ms</code>）：才会触发 Rebalance</li>
</ul>
<table>
<thead>
<tr>
<th></th>
<th>动态 Membership（默认）</th>
<th>Static Membership</th>
</tr>
</thead>
<tbody><tr>
<td>Consumer 重启</td>
<td>触发 Rebalance</td>
<td>不触发 Rebalance</td>
</tr>
<tr>
<td>partition 分配</td>
<td>重启后重新分配</td>
<td>重启后恢复原有分配</td>
</tr>
<tr>
<td>适用场景</td>
<td>无状态消费者</td>
<td>有状态流处理、滚动部署</td>
</tr>
</tbody></table>
<p>注意：同一 group 内 Instance ID 必须唯一，重复会导致旧实例被强制踢出。需要 broker 版本 &gt;&#x3D; 2.3。</p>
<p><strong>等待期间的取舍：</strong></p>
<p>Static Membership 下，Consumer 重启回来之前，对应的 partition 暂时没有消费者，消息堆积，LAG 增加。这是有意识的取舍：</p>
<ul>
<li><strong>用单个 partition 的短暂空窗</strong>，换<strong>整个 group 不需要 Rebalance</strong></li>
<li>能接受短暂堆积但不能接受 Rebalance 代价（有状态流处理）→ 用 Static Membership</li>
<li>不能接受任何堆积（高实时性无状态消费）→ 用默认动态 Membership</li>
</ul>
<p>等待时间就是 <code>session.timeout.ms</code>，不是额外的新配置。Coordinator 在这个窗口期内，如果同一 Instance ID 回来了，直接恢复原分配；超时才判定死亡并触发 Rebalance。</p>
<hr>
<h2 id="参考材料"><a href="#参考材料" class="headerlink" title="参考材料"></a>参考材料</h2><ol>
<li><strong>Kafka 4.0 Consumer Rebalance Protocol</strong>：<a href="https://kafka.apache.org/42/operations/consumer-rebalance-protocol/">https://kafka.apache.org/42/operations/consumer-rebalance-protocol/</a></li>
<li><strong>Kafka Consumer 配置</strong>：<a href="https://kafka.apache.org/42/configuration/consumer-configs/">https://kafka.apache.org/42/configuration/consumer-configs/</a></li>
<li><strong>Kafka 设计文档 — Static Membership</strong>：<a href="https://kafka.apache.org/42/design/design/%EF%BC%88%E6%89%BE">https://kafka.apache.org/42/design/design/（找</a> <code>The Consumer</code> → <code>Static Membership</code>）</li>
</ol>
]]></content>
      <tags>
        <tag>kafka</tag>
        <tag>offset</tag>
        <tag>consumer</tag>
        <tag>rebalance</tag>
        <tag>static-membership</tag>
        <tag>group-coordinator</tag>
        <tag>commit</tag>
      </tags>
  </entry>
  <entry>
    <title>08 · KRaft：为什么去掉 ZooKeeper，元数据如何管理</title>
    <url>/2026/04/01/kafka/notes/08-kraft/</url>
    <content><![CDATA[<h1 id="08-·-KRaft：为什么去掉-ZooKeeper，元数据如何管理"><a href="#08-·-KRaft：为什么去掉-ZooKeeper，元数据如何管理" class="headerlink" title="08 · KRaft：为什么去掉 ZooKeeper，元数据如何管理"></a>08 · KRaft：为什么去掉 ZooKeeper，元数据如何管理</h1><blockquote>
<p><strong>目标</strong>：理解 ZooKeeper 模式的根本问题，以及 KRaft 如何用内部机制替代它</p>
</blockquote>
<hr>
<h2 id="一、ZooKeeper-模式是什么"><a href="#一、ZooKeeper-模式是什么" class="headerlink" title="一、ZooKeeper 模式是什么"></a>一、ZooKeeper 模式是什么</h2><p>Kafka 4.0 之前，集群元数据（哪些 broker 在线、partition leader 是谁、ISR 列表、topic 配置）由外部的 <strong>ZooKeeper</strong> 集群管理。运维 Kafka 意味着同时运维两套系统。</p>
<hr>
<h2 id="二、ZooKeeper-模式的三个核心问题"><a href="#二、ZooKeeper-模式的三个核心问题" class="headerlink" title="二、ZooKeeper 模式的三个核心问题"></a>二、ZooKeeper 模式的三个核心问题</h2><p><strong>1. 元数据状态经常不一致</strong></p>
<p>ZooKeeper 用 watch 机制监控元数据变化，但 watch 机制性能有限，经常导致 ZooKeeper 里的元数据和 broker 内存里的状态不同步。同时，ZooKeeper 的推送通知不是原子操作，可能有些 broker 收到了变更，有些没收到，导致不同 broker 对集群状态的认知出现分歧。</p>
<p><strong>2. 扩展性瓶颈</strong></p>
<p>所有元数据都存在 ZooKeeper 里，ZooKeeper 的容量和性能成了 Kafka 集群规模的上限，直接限制了单个集群能支持的 partition 数量。</p>
<p><strong>3. 运维复杂</strong></p>
<p>两套系统意味着两套配置、监控、安全策略和故障排查流程，资源消耗大，部署维护成本高。</p>
<hr>
<h2 id="三、KRaft-的解法"><a href="#三、KRaft-的解法" class="headerlink" title="三、KRaft 的解法"></a>三、KRaft 的解法</h2><p><strong>不再依赖外部系统，用 Kafka 内部的 Controller 节点和 Raft 协议管理元数据。</strong></p>
<p>元数据存成一个 <strong>metadata log</strong>，本质上就是一个内部 topic：</p>
<ul>
<li><strong>Producer</strong>：Active Controller（负责写入元数据变更）</li>
<li><strong>Consumer</strong>：所有 broker（主动 pull，订阅元数据更新）</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ZooKeeper 模式：Kafka ←→ ZooKeeper（外部推送）</span><br><span class="line">KRaft 模式：  broker（pull）← metadata log ← Controller（内置 Raft）</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="四、Controller-Quorum-与-Raft"><a href="#四、Controller-Quorum-与-Raft" class="headerlink" title="四、Controller Quorum 与 Raft"></a>四、Controller Quorum 与 Raft</h2><p>通常选 3 或 5 个节点作为 controller，用 Raft 协议选出一个 Active Controller 处理所有元数据写入，其他 controller 是 follower。</p>
<p><strong>为什么选奇数？</strong> Raft 是多数派协议，奇数个节点总能出现多数派：</p>
<ul>
<li>3 个 controller：容忍 1 个节点故障</li>
<li>5 个 controller：容忍 2 个节点故障</li>
<li>偶数节点（如 4 个）：容忍 1 个故障，但成本比 3 个高，没有额外收益</li>
</ul>
<p>Raft 保证 metadata log 的全局写入顺序一致，所有 broker 看到的元数据变更顺序完全相同。</p>
<hr>
<h2 id="五、元数据一致性：KRaft-vs-ZooKeeper"><a href="#五、元数据一致性：KRaft-vs-ZooKeeper" class="headerlink" title="五、元数据一致性：KRaft vs ZooKeeper"></a>五、元数据一致性：KRaft vs ZooKeeper</h2><p><strong>ZooKeeper 模式的不一致是结构性的</strong>：推送机制本身不保证所有 broker 同时收到变更，可能出现”跳着应用”的情况。</p>
<p><strong>KRaft 的不一致是暂时性的</strong>：broker 按顺序消费 metadata log，可能落后几条 entry，但最终会追上，且追上过程是有序的，不会出现跳过或乱序。</p>
<p><strong>原子性的边界</strong>：单条 metadata log entry 是原子的（Raft 多数派提交保证），但跨多条 entry 的逻辑操作没有跨条目的原子性。这比 ZooKeeper 的推送更可靠，但不是完全的事务语义。</p>
<hr>
<h2 id="六、KRaft-带来的改进"><a href="#六、KRaft-带来的改进" class="headerlink" title="六、KRaft 带来的改进"></a>六、KRaft 带来的改进</h2><table>
<thead>
<tr>
<th></th>
<th>ZooKeeper 模式</th>
<th>KRaft 模式</th>
</tr>
</thead>
<tbody><tr>
<td>外部依赖</td>
<td>需要独立 ZooKeeper 集群</td>
<td>无外部依赖</td>
</tr>
<tr>
<td>元数据一致性</td>
<td>结构性不一致，可能永久分歧</td>
<td>暂时性落后，最终有序追上</td>
</tr>
<tr>
<td>扩展性</td>
<td>partition 数量受 ZooKeeper 限制</td>
<td>理论上支持百万级 partition</td>
</tr>
<tr>
<td>运维复杂度</td>
<td>两套系统</td>
<td>一套系统</td>
</tr>
<tr>
<td>单节点部署</td>
<td>不支持</td>
<td>支持（开发环境友好）</td>
</tr>
</tbody></table>
<p>Kafka 4.0 起完全移除 ZooKeeper 支持，只支持 KRaft 模式。</p>
<hr>
<h2 id="参考材料"><a href="#参考材料" class="headerlink" title="参考材料"></a>参考材料</h2><ol>
<li><strong>Kafka KRaft 运维文档</strong>：<a href="https://kafka.apache.org/42/operations/kraft/">https://kafka.apache.org/42/operations/kraft/</a></li>
<li><strong>KIP-500（设计动机）</strong>：<a href="https://cwiki.apache.org/confluence/display/KAFKA/KIP-500">https://cwiki.apache.org/confluence/display/KAFKA/KIP-500</a></li>
</ol>
]]></content>
      <tags>
        <tag>kafka</tag>
        <tag>kraft</tag>
        <tag>zookeeper</tag>
        <tag>raft</tag>
        <tag>metadata</tag>
        <tag>controller</tag>
      </tags>
  </entry>
  <entry>
    <title>09 · 工程定位：Kafka 在真实系统中的角色与取舍</title>
    <url>/2026/04/01/kafka/notes/09-engineering/</url>
    <content><![CDATA[<h1 id="09-·-工程定位：Kafka-在真实系统中的角色与取舍"><a href="#09-·-工程定位：Kafka-在真实系统中的角色与取舍" class="headerlink" title="09 · 工程定位：Kafka 在真实系统中的角色与取舍"></a>09 · 工程定位：Kafka 在真实系统中的角色与取舍</h1><blockquote>
<p><strong>目标</strong>：理解 Kafka 适合什么、不适合什么，以及它在真实系统架构中的位置</p>
</blockquote>
<hr>
<h2 id="一、Kafka-的本质定位"><a href="#一、Kafka-的本质定位" class="headerlink" title="一、Kafka 的本质定位"></a>一、Kafka 的本质定位</h2><p><strong>高吞吐、持久化、可重放的异步数据管道。</strong></p>
<p>所有设计都围绕这个定位展开：append-only log 保证高吞吐和可重放，partition 保证水平扩展，Consumer Group 保证并行消费，offset 由消费者自己管理保证解耦。</p>
<hr>
<h2 id="二、典型使用场景"><a href="#二、典型使用场景" class="headerlink" title="二、典型使用场景"></a>二、典型使用场景</h2><table>
<thead>
<tr>
<th>场景</th>
<th>Kafka 的角色</th>
<th>为什么适合</th>
</tr>
</thead>
<tbody><tr>
<td><strong>数据管道 &#x2F; ETL</strong></td>
<td>连接数据源和目的地，解耦上下游</td>
<td>高吞吐、持久化、上下游独立演进</td>
</tr>
<tr>
<td><strong>事件流处理</strong></td>
<td>实时处理用户行为、交易、日志等事件流</td>
<td>有序、可重放、支持多消费者</td>
</tr>
<tr>
<td><strong>日志聚合</strong></td>
<td>多台服务器的日志集中收集</td>
<td>高吞吐写入、持久化存储、可回溯</td>
</tr>
<tr>
<td><strong>事件溯源</strong></td>
<td>用不可变日志记录所有状态变更</td>
<td>append-only 天然契合事件溯源模型</td>
</tr>
</tbody></table>
<hr>
<h2 id="三、Kafka-不适合什么"><a href="#三、Kafka-不适合什么" class="headerlink" title="三、Kafka 不适合什么"></a>三、Kafka 不适合什么</h2><h3 id="1-任务队列"><a href="#1-任务队列" class="headerlink" title="1. 任务队列"></a>1. 任务队列</h3><p>传统任务队列（如 RabbitMQ）的工作方式：</p>
<ul>
<li>消费者取一条消息 → 处理 → ack → broker 删除该消息</li>
<li>处理失败可以 nack，broker 自动把消息重新投递给其他消费者</li>
<li>支持”把任务分发给多个 worker，失败自动重试给别人”</li>
</ul>
<p>Kafka 做不到这些：</p>
<ul>
<li>消费后不删除消息，broker 不追踪单条消息的消费状态</li>
<li>没有单条消息级别的 ack&#x2F;nack 机制</li>
<li>没有”这条失败了请重新分配给别人”的内置能力</li>
<li>Consumer 按 offset 顺序读取，粒度是 partition 级别而不是消息级别</li>
</ul>
<h3 id="2-请求-响应模式"><a href="#2-请求-响应模式" class="headerlink" title="2. 请求-响应模式"></a>2. 请求-响应模式</h3><p>请求-响应需要低延迟的点对点通信：A 发请求，B 快速回应给 A。</p>
<p>Kafka 是异步解耦的：</p>
<ul>
<li>Producer 写完就走，不等回复</li>
<li>Consumer 什么时候读、读多快，Producer 完全不知道</li>
<li>没有”把响应路由回原始请求者”的内置机制</li>
<li>虽然可以用 correlation ID + 回复 topic 模拟，但比直接 HTTP&#x2F;gRPC 复杂得多，延迟也高得多</li>
</ul>
<hr>
<h2 id="四、并行消费与顺序保证"><a href="#四、并行消费与顺序保证" class="headerlink" title="四、并行消费与顺序保证"></a>四、并行消费与顺序保证</h2><p>Kafka 只保证 <strong>单个 partition 内消息有序</strong>，跨 partition 没有顺序保证。</p>
<p>并行消费意味着多个消费者同时从不同 partition 读取，哪个先处理完是不确定的。这就是 key 路由的核心价值：</p>
<ul>
<li>有因果关系的消息：用相同 key 路由到同一个 partition，在 partition 内保序</li>
<li>无关消息：分散到不同 partition，并行处理提高吞吐</li>
</ul>
<p>这是 Kafka 在<strong>吞吐量</strong>和<strong>顺序保证</strong>之间的根本取舍：想要更高吞吐就增加 partition 数量和消费者数量，但跨 partition 的全局顺序就无法保证。</p>
<hr>
<h2 id="五、关键工程取舍总结"><a href="#五、关键工程取舍总结" class="headerlink" title="五、关键工程取舍总结"></a>五、关键工程取舍总结</h2><table>
<thead>
<tr>
<th>取舍维度</th>
<th>Kafka 的选择</th>
<th>代价</th>
</tr>
</thead>
<tbody><tr>
<td>吞吐 vs 延迟</td>
<td>批量发送、批量压缩换高吞吐</td>
<td>引入少量延迟（linger.ms）</td>
</tr>
<tr>
<td>可靠性 vs 可用性</td>
<td>acks&#x3D;all + min.insync.replicas 保数据不丢</td>
<td>副本不足时拒绝写入</td>
</tr>
<tr>
<td>顺序 vs 并行</td>
<td>单 partition 内有序，跨 partition 无序</td>
<td>全局有序需要单 partition，牺牲并行度</td>
</tr>
<tr>
<td>解耦 vs 实时</td>
<td>异步解耦，消费者自主控制进度</td>
<td>不适合低延迟请求-响应</td>
</tr>
<tr>
<td>简单性 vs 灵活性</td>
<td>offset 由消费者管理</td>
<td>不支持单条消息级别的 ack&#x2F;nack</td>
</tr>
<tr>
<td>存储效率 vs 随机访问</td>
<td>顺序写入 + Page Cache + 零拷贝</td>
<td>不支持高效随机读写</td>
</tr>
</tbody></table>
<hr>
<h2 id="参考材料"><a href="#参考材料" class="headerlink" title="参考材料"></a>参考材料</h2><ol>
<li><strong>Kafka 设计文档 — Use Cases</strong>：<a href="https://kafka.apache.org/42/getting-started/use-cases/">https://kafka.apache.org/42/getting-started/use-cases/</a></li>
<li><strong>Kafka 设计文档 — Design</strong>：<a href="https://kafka.apache.org/42/design/design/">https://kafka.apache.org/42/design/design/</a></li>
</ol>
]]></content>
      <tags>
        <tag>kafka</tag>
        <tag>engineering</tag>
        <tag>positioning</tag>
        <tag>tradeoffs</tag>
        <tag>use-cases</tag>
      </tags>
  </entry>
  <entry>
    <title>01 · Transformer 结构与 Attention 计算</title>
    <url>/2026/04/01/llm-inference/notes/01-transformer-basics/</url>
    <content><![CDATA[<h1 id="01-·-Transformer-结构与-Attention-计算"><a href="#01-·-Transformer-结构与-Attention-计算" class="headerlink" title="01 · Transformer 结构与 Attention 计算"></a>01 · Transformer 结构与 Attention 计算</h1><blockquote>
<p><strong>目标</strong>：理解 LLM 推理时到底在算什么——从 token 输入到新语义向量输出的完整计算流程</p>
</blockquote>
<hr>
<h2 id="一、推理的本质：一切都是矩阵乘法"><a href="#一、推理的本质：一切都是矩阵乘法" class="headerlink" title="一、推理的本质：一切都是矩阵乘法"></a>一、推理的本质：一切都是矩阵乘法</h2><p>神经网络是一个函数：输入一组数字，输出一组数字。推理（Inference）就是执行这个函数，不涉及任何参数更新。</p>
<p>LLM 推理：</p>
<ul>
<li>输入：一串 token（文字被切分并编码成数字序列）</li>
<li>输出：下一个 token 的概率分布</li>
</ul>
<p>整个推理过程中，绝大部分计算量只有一种运算：<strong>矩阵乘法（GEMM）</strong>。这是 GPU 适合做推理的根本原因——GPU 专为大规模并行矩阵乘法设计。</p>
<hr>
<h2 id="二、Attention-出现之前：RNN-的问题"><a href="#二、Attention-出现之前：RNN-的问题" class="headerlink" title="二、Attention 出现之前：RNN 的问题"></a>二、Attention 出现之前：RNN 的问题</h2><p>RNN 处理语言序列的方式是<strong>逐词传递隐状态</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&quot;猫&quot; → [RNN] → h₁</span><br><span class="line">&quot;喜欢&quot; → [RNN, 读入 h₁] → h₂</span><br><span class="line">&quot;鱼&quot; → [RNN, 读入 h₂] → h₃</span><br></pre></td></tr></table></figure>

<p>问题：隐状态每传一步就被压缩一次，<strong>越早的词信息越难保留</strong>。处理”鱼”时，”猫”的信息已经被稀释了两次。这叫<strong>长程依赖问题</strong>。</p>
<p>另一个问题：必须严格按顺序处理，无法并行，训练极慢。</p>
<hr>
<h2 id="三、Attention-的核心思想"><a href="#三、Attention-的核心思想" class="headerlink" title="三、Attention 的核心思想"></a>三、Attention 的核心思想</h2><p>Attention 不依赖隐状态传递，而是让每个词<strong>直接访问序列中任意其他词的信息</strong>：</p>
<blockquote>
<p>处理”鱼”时，不靠传来的隐状态，而是直接问：整个序列里哪个词和我最相关？→ 直接看到”猫”，借来它的信息。</p>
</blockquote>
<table>
<thead>
<tr>
<th>维度</th>
<th>RNN</th>
<th>Transformer</th>
</tr>
</thead>
<tbody><tr>
<td>信息传递</td>
<td>逐步压缩传递</td>
<td>任意两词直接交互</td>
</tr>
<tr>
<td>长距离依赖</td>
<td>难，信息随距离衰减</td>
<td>容易，距离不影响</td>
</tr>
<tr>
<td>并行计算</td>
<td>不能（必须顺序）</td>
<td>能（所有位置同时算）</td>
</tr>
<tr>
<td>计算复杂度</td>
<td>O(n)</td>
<td>O(n²)（两两交互）</td>
</tr>
</tbody></table>
<p>Attention 用 O(n²) 的计算代价，换来了并行性和全局依赖能力。</p>
<hr>
<h2 id="四、Self-Attention-计算流程"><a href="#四、Self-Attention-计算流程" class="headerlink" title="四、Self-Attention 计算流程"></a>四、Self-Attention 计算流程</h2><p>以”猫 喜欢 鱼”三个词为例，计算”鱼”的新向量。</p>
<p><strong>第一步：生成 Q、K、V</strong></p>
<p>每个词的原始向量 x，分别乘以三个权重矩阵，得到三个角色：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Q (Query)  = x × W_Q   → &quot;我在找什么信息？&quot;</span><br><span class="line">K (Key)    = x × W_K   → &quot;我能提供什么信息的标签？&quot;</span><br><span class="line">V (Value)  = x × W_V   → &quot;我实际携带的内容&quot;</span><br></pre></td></tr></table></figure>

<p><strong>注意：K 和 V 的职责不同。</strong></p>
<ul>
<li>K 是”门牌号”——用来和其他词的 Q 做匹配，决定相关度</li>
<li>V 是”房间里的东西”——被关注之后，真正流进其他词的内容</li>
</ul>
<p>两者用不同权重矩阵学出，可以独立优化：K 学”怎么让别人找到我”，V 学”被找到后给出什么”。</p>
<p><strong>第二步：Q 和所有 K 做点积，得到相关度分数</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">score(鱼, 猫)   = Q_鱼 · K_猫   = 0.74</span><br><span class="line">score(鱼, 喜欢) = Q_鱼 · K_喜欢 = 0.24</span><br><span class="line">score(鱼, 鱼)   = Q_鱼 · K_鱼   = 0.62</span><br></pre></td></tr></table></figure>

<p>点积越大 &#x3D; “鱼”认为这个词越相关。</p>
<p><strong>第三步：除以 √d_k，再 Softmax</strong></p>
<p>除以 √d_k 的原因：d_k 维的向量点积是 d_k 个乘积的求和，维度越高数值越大。不除的话，Softmax 输入过大，输出极端（最高分接近 1，其余接近 0），导致训练时梯度消失。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[0.74, 0.24, 0.62] ÷ √2 → Softmax → [0.42, 0.26, 0.32]</span><br></pre></td></tr></table></figure>

<p>Softmax 把任意数字变成加起来等于 1 的概率分布：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Softmax(xᵢ) = e^xᵢ / Σ e^xⱼ</span><br></pre></td></tr></table></figure>

<p>使用 Softmax 而非直接归一化的原因：Softmax 是指数函数，会放大差距——相关度高的词拿到更多注意力，低的被进一步压制。这让模型能学到”强焦点”。</p>
<p><strong>第四步：用注意力权重对所有 V 做加权求和</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输出 = 0.42 × V_猫 + 0.26 × V_喜欢 + 0.32 × V_鱼</span><br></pre></td></tr></table></figure>

<p>这是<strong>线性组合</strong>，不是点积。结果是”鱼”融合了整个序列语义后的新向量。</p>
<p><strong>完整公式：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Attention(Q, K, V) = Softmax(Q Kᵀ / √d_k) × V</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="五、Multi-Head-Attention"><a href="#五、Multi-Head-Attention" class="headerlink" title="五、Multi-Head Attention"></a>五、Multi-Head Attention</h2><p>单组 Q&#x2F;K&#x2F;V 只能捕捉一种”关注视角”。Multi-Head Attention 并行跑 h 组独立的 Attention，每组用不同权重矩阵，最后拼接投影：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Head_i = Attention(Q_i, K_i, V_i)</span><br><span class="line">输出 = Concat(Head_1, ..., Head_h) × W_O</span><br></pre></td></tr></table></figure>

<p>W_O 是拼接后的投影矩阵，把 h 倍维度压回原始维度。</p>
<p>不同的 head 会学到不同的关注模式（语法关系、语义关系、指代关系等）。</p>
<hr>
<h2 id="六、Transformer-Block-完整结构"><a href="#六、Transformer-Block-完整结构" class="headerlink" title="六、Transformer Block 完整结构"></a>六、Transformer Block 完整结构</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入 x</span><br><span class="line">  ↓</span><br><span class="line">Self-Attention（W_Q / W_K / W_V / W_O）</span><br><span class="line">  ↓ + 残差连接（加上原始 x，防止梯度消失）</span><br><span class="line">  ↓</span><br><span class="line">Layer Norm（归一化，稳定训练）</span><br><span class="line">  ↓</span><br><span class="line">FFN：x × W₁ → ReLU → × W₂</span><br><span class="line">  ↓ + 残差连接</span><br><span class="line">  ↓</span><br><span class="line">Layer Norm</span><br><span class="line">  ↓</span><br><span class="line">输出（进入下一个 Block）</span><br></pre></td></tr></table></figure>

<p><strong>FFN 的作用：</strong></p>
<p>Self-Attention 做的是<strong>词间信息路由</strong>——决定从哪些词借多少信息，结果是线性组合。</p>
<p>FFN 做的是<strong>每个词独立的非线性加工</strong>——对混合后的向量做深度语义变换，互不影响。</p>
<table>
<thead>
<tr>
<th></th>
<th>Self-Attention</th>
<th>FFN</th>
</tr>
</thead>
<tbody><tr>
<td>交互范围</td>
<td>整个序列（词间交互）</td>
<td>单个位置（独立处理）</td>
</tr>
<tr>
<td>变换类型</td>
<td>线性组合</td>
<td>非线性变换（含 ReLU）</td>
</tr>
<tr>
<td>作用</td>
<td>收集上下文信息</td>
<td>消化加工语义</td>
</tr>
</tbody></table>
<p><strong>为什么需要非线性（ReLU）：</strong></p>
<p>纯矩阵乘法是线性变换，多层叠加等价于一层，深度无意义。ReLU（负数清零，正数保留）引入非线性，让网络能表达条件逻辑，无法被合并压缩。</p>
<p>FFN 的 W₁&#x2F;W₂ 矩阵乘法（GEMM）是推理计算量的大头之一，是后续 kernel 优化的主要对象。</p>
<hr>
<h2 id="七、完整模型结构"><a href="#七、完整模型结构" class="headerlink" title="七、完整模型结构"></a>七、完整模型结构</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入 tokens</span><br><span class="line">  ↓</span><br><span class="line">Embedding 层（token → 向量）+ 位置编码</span><br><span class="line">  ↓</span><br><span class="line">Transformer Block × N 层（每层结构相同，参数不同）</span><br><span class="line">  ↓</span><br><span class="line">输出层（向量 → 词表概率分布）</span><br><span class="line">  ↓</span><br><span class="line">下一个 token</span><br></pre></td></tr></table></figure>

<p>GPT-3：96 层，模型维度 12288，96 个 attention head，175B 参数。</p>
<hr>
<h2 id="参考材料"><a href="#参考材料" class="headerlink" title="参考材料"></a>参考材料</h2><ol>
<li><strong>The Illustrated Transformer</strong>：<a href="https://jalammar.github.io/illustrated-transformer/">https://jalammar.github.io/illustrated-transformer/</a></li>
<li><strong>Attention Is All You Need</strong>：<a href="https://arxiv.org/abs/1706.03762">https://arxiv.org/abs/1706.03762</a></li>
</ol>
]]></content>
      <tags>
        <tag>transformer</tag>
        <tag>llm-inference</tag>
        <tag>attention</tag>
        <tag>qkv</tag>
        <tag>ffn</tag>
        <tag>embedding</tag>
      </tags>
  </entry>
  <entry>
    <title>02 · C++ 必要基础</title>
    <url>/2026/04/01/llm-inference/notes/02-cpp-essentials/</url>
    <content><![CDATA[<h1 id="02-·-C-必要基础"><a href="#02-·-C-必要基础" class="headerlink" title="02 · C++ 必要基础"></a>02 · C++ 必要基础</h1><blockquote>
<p><strong>目标</strong>：理解 C++ 的内存模型和对象生命周期管理，能看懂推理框架 host 代码里的指针、智能指针、模板用法</p>
</blockquote>
<hr>
<h2 id="一、指针与引用"><a href="#一、指针与引用" class="headerlink" title="一、指针与引用"></a>一、指针与引用</h2><p>C++ 在 C 的指针基础上增加了引用：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> x = <span class="number">42</span>;</span><br><span class="line"><span class="type">int</span>* p = &amp;x;   <span class="comment">// 指针：存储 x 的地址</span></span><br><span class="line"><span class="type">int</span>&amp; r = x;    <span class="comment">// 引用：x 的别名，本质是语法糖，编译器实现上通常也是地址</span></span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th></th>
<th>指针</th>
<th>引用</th>
</tr>
</thead>
<tbody><tr>
<td>是否可以为 null</td>
<td>可以</td>
<td>不行，必须绑定到对象</td>
</tr>
<tr>
<td>是否可以改变指向</td>
<td>可以</td>
<td>不行，绑定后不能换</td>
</tr>
<tr>
<td>语法</td>
<td><code>*p</code> 解引用</td>
<td>直接用 <code>r</code>，和用原变量一样</td>
</tr>
</tbody></table>
<p><strong>函数传参的三种方式：</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo_value</span><span class="params">(<span class="type">int</span> x)</span>   </span>&#123; x = <span class="number">99</span>; &#125;   <span class="comment">// 复制，不影响外部</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo_ptr</span><span class="params">(<span class="type">int</span>* x)</span>    </span>&#123; *x = <span class="number">99</span>; &#125;  <span class="comment">// 传地址，可修改外部</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo_ref</span><span class="params">(<span class="type">int</span>&amp; x)</span>    </span>&#123; x = <span class="number">99</span>; &#125;   <span class="comment">// 传引用，可修改外部，语法更干净</span></span><br></pre></td></tr></table></figure>

<p>推理框架里大量用 <code>const</code> 引用传大对象，避免复制开销：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">process</span><span class="params">(<span class="type">const</span> std::vector&lt;<span class="type">float</span>&gt;&amp; tensor)</span> </span>&#123; ... &#125;</span><br><span class="line"><span class="comment">// 不复制 tensor，只传引用；const 保证函数内不能修改</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="二、构造函数与析构函数"><a href="#二、构造函数与析构函数" class="headerlink" title="二、构造函数与析构函数"></a>二、构造函数与析构函数</h2><p>对象创建时调用构造函数，销毁时调用析构函数：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Buffer</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Buffer</span>(<span class="type">int</span> size) &#123;   <span class="comment">// 构造函数：对象创建时调用</span></span><br><span class="line">        data = <span class="keyword">new</span> <span class="type">float</span>[size];</span><br><span class="line">    &#125;</span><br><span class="line">    ~<span class="built_in">Buffer</span>() &#123;          <span class="comment">// 析构函数：对象销毁时调用</span></span><br><span class="line">        <span class="keyword">delete</span>[] data;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">float</span>* data;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>析构函数的调用时机：</p>
<ul>
<li>栈上对象：离开作用域时（<code>&#125;</code> 处）自动调用，包括提前 <code>return</code> 的情况</li>
<li>堆上对象：<code>delete</code> 时调用，或智能指针析构时调用</li>
</ul>
<hr>
<h2 id="三、RAII-与智能指针"><a href="#三、RAII-与智能指针" class="headerlink" title="三、RAII 与智能指针"></a>三、RAII 与智能指针</h2><p><strong>RAII（Resource Acquisition Is Initialization）</strong>：把资源的生命周期绑定到对象的生命周期。构造时获取资源，析构时释放资源，编译器保证析构一定被调用。</p>
<p>C 的问题：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span>* p = (<span class="type">int</span>*)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="type">int</span>));</span><br><span class="line"><span class="comment">// 中间可能提前 return → free 被跳过 → 内存泄漏</span></span><br><span class="line"><span class="built_in">free</span>(p);</span><br></pre></td></tr></table></figure>

<p>C++ 用智能指针解决堆上对象的生命周期管理：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> p = std::<span class="built_in">make_unique</span>&lt;<span class="type">int</span>&gt;(<span class="number">42</span>);</span><br><span class="line"><span class="comment">// 不管怎么 return，离开作用域时析构函数自动调用 delete</span></span><br></pre></td></tr></table></figure>

<p><strong>什么时候用堆（智能指针），什么时候用栈（局部变量）：</strong></p>
<table>
<thead>
<tr>
<th>情况</th>
<th>选择</th>
</tr>
</thead>
<tbody><tr>
<td>小对象，生命周期在当前作用域内</td>
<td>栈，直接声明局部变量</td>
</tr>
<tr>
<td>对象太大（大 tensor buffer、模型权重）</td>
<td>堆 + 智能指针</td>
</tr>
<tr>
<td>生命周期需要跨函数边界传递</td>
<td>堆 + 智能指针</td>
</tr>
<tr>
<td>运行时才知道大小或数量</td>
<td>堆 + 智能指针</td>
</tr>
</tbody></table>
<p><strong>栈比堆快</strong>：栈分配只是移动 stack pointer，几乎零开销；堆分配需要内存分配器查找空闲块。能用栈就用栈。</p>
<p><strong>三种智能指针：</strong></p>
<table>
<thead>
<tr>
<th>类型</th>
<th>所有权语义</th>
<th>释放时机</th>
</tr>
</thead>
<tbody><tr>
<td><code>unique_ptr</code></td>
<td>独占，不可复制</td>
<td>离开作用域时析构</td>
</tr>
<tr>
<td><code>shared_ptr</code></td>
<td>共享，引用计数</td>
<td>引用计数归零时析构</td>
</tr>
<tr>
<td><code>weak_ptr</code></td>
<td>不持有所有权</td>
<td>不触发释放，配合 shared_ptr 打破循环引用</td>
</tr>
</tbody></table>
<hr>
<h2 id="四、移动语义（std-move）"><a href="#四、移动语义（std-move）" class="headerlink" title="四、移动语义（std::move）"></a>四、移动语义（std::move）</h2><p><code>unique_ptr</code> 不能复制——复制会导致两个指针都认为自己拥有内存，析构时 double free：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> p1 = std::<span class="built_in">make_unique</span>&lt;<span class="type">int</span>&gt;(<span class="number">42</span>);</span><br><span class="line"><span class="keyword">auto</span> p2 = p1;             <span class="comment">// 编译错误！</span></span><br><span class="line"><span class="keyword">auto</span> p2 = std::<span class="built_in">move</span>(p1);  <span class="comment">// 正确：转移所有权，p1 变为 nullptr</span></span><br></pre></td></tr></table></figure>

<p><code>std::move</code> 不移动任何内存，只是告诉编译器”允许从 p1 窃取资源”。移动后 p1 为空，p2 接管所有权。</p>
<p>推理框架里传递大 tensor buffer 时用移动语义，避免复制：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">enqueue</span><span class="params">(std::unique_ptr&lt;Tensor&gt; tensor)</span> </span>&#123; ... &#125;</span><br><span class="line"><span class="built_in">enqueue</span>(std::<span class="built_in">move</span>(my_tensor));  <span class="comment">// 转移所有权，不复制数据</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="五、模板基础"><a href="#五、模板基础" class="headerlink" title="五、模板基础"></a>五、模板基础</h2><p>模板让一份代码支持多种类型，在编译期展开，运行时无额外开销：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span></span><br><span class="line"><span class="function">T <span class="title">add</span><span class="params">(T a, T b)</span> </span>&#123; <span class="keyword">return</span> a + b; &#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">add</span>&lt;<span class="type">int</span>&gt;(<span class="number">1</span>, <span class="number">2</span>);        <span class="comment">// 编译器生成 int 版本</span></span><br><span class="line"><span class="built_in">add</span>&lt;<span class="type">float</span>&gt;(<span class="number">1.0f</span>, <span class="number">2.0f</span>); <span class="comment">// 编译器生成 float 版本</span></span><br></pre></td></tr></table></figure>

<p>CUDA kernel 大量用模板支持 FP16&#x2F;BF16&#x2F;FP32 等不同精度：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span></span><br><span class="line"><span class="function">__global__ <span class="type">void</span> <span class="title">gemm_kernel</span><span class="params">(T* A, T* B, T* C, <span class="type">int</span> N)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 同一份 kernel，支持不同数据类型</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>模板在编译期展开，每个具体类型生成一份独立机器码，没有运行时类型判断的开销。</p>
<hr>
<h2 id="六、C-vs-Java-内存模型对比"><a href="#六、C-vs-Java-内存模型对比" class="headerlink" title="六、C++ vs Java 内存模型对比"></a>六、C++ vs Java 内存模型对比</h2><table>
<thead>
<tr>
<th></th>
<th>C++</th>
<th>Java</th>
</tr>
</thead>
<tbody><tr>
<td>对象可以在栈上吗</td>
<td>可以</td>
<td>不行，所有对象在堆上</td>
</tr>
<tr>
<td>赋值语义</td>
<td>值拷贝（默认复制对象）</td>
<td>引用传递（复制的是引用）</td>
</tr>
<tr>
<td>内存释放</td>
<td>析构函数 &#x2F; 智能指针</td>
<td>GC 自动回收</td>
</tr>
<tr>
<td>需要关心对象在哪里</td>
<td>需要，影响性能和生命周期</td>
<td>不需要</td>
</tr>
</tbody></table>
<p>C++ 最容易踩的坑：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">MyObject obj1;</span><br><span class="line">MyObject obj2 = obj1;  <span class="comment">// 值拷贝！obj2 是独立副本，不是引用</span></span><br><span class="line">MyObject* ptr = &amp;obj1; <span class="comment">// 这才是类似 Java 引用的用法</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="参考材料"><a href="#参考材料" class="headerlink" title="参考材料"></a>参考材料</h2><ol>
<li><strong>learncpp.com（指针&#x2F;引用）</strong>：<a href="https://www.learncpp.com/cpp-tutorial/introduction-to-pointers/">https://www.learncpp.com/cpp-tutorial/introduction-to-pointers/</a></li>
<li><strong>learncpp.com（智能指针&#x2F;RAII）</strong>：<a href="https://www.learncpp.com/cpp-tutorial/introduction-to-smart-pointers-move-semantics/">https://www.learncpp.com/cpp-tutorial/introduction-to-smart-pointers-move-semantics/</a></li>
<li><strong>learncpp.com（模板）</strong>：<a href="https://www.learncpp.com/cpp-tutorial/function-templates/">https://www.learncpp.com/cpp-tutorial/function-templates/</a></li>
</ol>
]]></content>
      <tags>
        <tag>cpp</tag>
        <tag>llm-inference</tag>
        <tag>memory</tag>
        <tag>raii</tag>
        <tag>smart-pointer</tag>
        <tag>pointer</tag>
        <tag>reference</tag>
        <tag>template</tag>
        <tag>move-semantics</tag>
      </tags>
  </entry>
  <entry>
    <title>03 · C++ 编译模型与 CMake</title>
    <url>/2026/04/01/llm-inference/notes/03-cpp-build-system/</url>
    <content><![CDATA[<h1 id="03-·-C-编译模型与-CMake"><a href="#03-·-C-编译模型与-CMake" class="headerlink" title="03 · C++ 编译模型与 CMake"></a>03 · C++ 编译模型与 CMake</h1><blockquote>
<p><strong>目标</strong>：理解 C++ 从源文件到可执行文件的完整流程，以及 .h&#x2F;.cpp 分离的原因，能独立用 CMake 构建多文件项目</p>
</blockquote>
<hr>
<h2 id="一、编译流程总览"><a href="#一、编译流程总览" class="headerlink" title="一、编译流程总览"></a>一、编译流程总览</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">源文件 (.cpp)</span><br><span class="line">    ↓ 预处理器</span><br><span class="line">翻译单元（纯文本）</span><br><span class="line">    ↓ 编译器</span><br><span class="line">目标文件 (.o)</span><br><span class="line">    ↓ 链接器</span><br><span class="line">可执行文件</span><br></pre></td></tr></table></figure>

<p>每个 <code>.cpp</code> 文件独立编译成一个 <code>.o</code>，链接器最后把所有 <code>.o</code> 合并。</p>
<hr>
<h2 id="二、第一步：预处理器"><a href="#二、第一步：预处理器" class="headerlink" title="二、第一步：预处理器"></a>二、第一步：预处理器</h2><p><strong>输入</strong>：原始 <code>.cpp</code> 源文件（含 <code>#include</code>、<code>#define</code> 等指令）</p>
<p><strong>做了什么</strong>：</p>
<ol>
<li><strong>展开 <code>#include</code></strong>：把头文件内容直接复制粘贴到 <code>#include</code> 的位置</li>
</ol>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 原始 main.cpp</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;add.h&quot;</span></span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123; ... &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 展开后</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span></span>;   <span class="comment">// add.h 的内容被复制进来</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123; ... &#125;</span><br></pre></td></tr></table></figure>

<p><code>#include &lt;iostream&gt;</code> 同理，展开后有几千行。</p>
<ol start="2">
<li><strong>展开 <code>#define</code></strong>：文本替换</li>
</ol>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> MAX_SIZE 1024</span></span><br><span class="line"><span class="type">int</span> arr[MAX_SIZE];</span><br><span class="line"><span class="comment">// 展开后</span></span><br><span class="line"><span class="type">int</span> arr[<span class="number">1024</span>];</span><br></pre></td></tr></table></figure>

<ol start="3">
<li><strong>处理条件编译</strong>：<code>#ifdef</code> &#x2F; <code>#ifndef</code> 等，不满足条件的代码块直接删掉</li>
</ol>
<p><strong>输出</strong>：<strong>翻译单元</strong>——所有 <code>#</code> 指令处理完之后的纯 C++ 文本，没有任何 <code>#</code> 开头的指令。可以用 <code>g++ -E main.cpp</code> 查看。</p>
<hr>
<h2 id="三、第二步：编译器"><a href="#三、第二步：编译器" class="headerlink" title="三、第二步：编译器"></a>三、第二步：编译器</h2><p><strong>输入</strong>：翻译单元（纯 C++ 文本）</p>
<p><strong>做了什么</strong>：词法分析 → 语法分析（生成 AST）→ 语义分析（类型检查）→ 生成机器码</p>
<p>关键点：编译器只看当前翻译单元，遇到 <code>add(1, 2)</code> 的调用时：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// main.cpp 展开后</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span></span>;   <span class="comment">// 只有声明，知道函数签名</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">add</span>(<span class="number">1</span>, <span class="number">2</span>);           <span class="comment">// 生成调用代码，但地址不知道</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>编译器从声明知道参数类型和返回类型，可以生成正确的调用代码，但 <code>add</code> 的实现在哪里它不知道——在 <code>.o</code> 文件里留一个<strong>未解析符号</strong>，标记为 <code>U</code>（undefined）。</p>
<p><strong>输出</strong>：<code>.o</code> 目标文件，包含：</p>
<ul>
<li>机器码</li>
<li><strong>符号表</strong>：已定义的符号（<code>T</code>，有实现）和未解析的符号（<code>U</code>，地址待定）</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nm main.o</span><br><span class="line"><span class="comment"># U add    ← undefined，地址待定</span></span><br><span class="line"><span class="comment"># T main   ← defined，地址已知</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="四、第三步：链接器"><a href="#四、第三步：链接器" class="headerlink" title="四、第三步：链接器"></a>四、第三步：链接器</h2><p><strong>输入</strong>：所有 <code>.o</code> 文件</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">main.o  →  机器码 + 未解析符号：add（U）</span><br><span class="line">add.o   →  机器码 + 已定义符号：add（T）</span><br></pre></td></tr></table></figure>

<p><strong>做了什么</strong>：</p>
<ol>
<li>合并所有 <code>.o</code> 的机器码，分配最终地址</li>
<li>解析未解析符号：<code>main.o</code> 里 <code>add</code> 地址待定 → 在 <code>add.o</code> 里找到实现 → 填入真实地址</li>
</ol>
<p><strong>两种链接错误：</strong></p>
<ul>
<li><code>undefined reference to &#39;add&#39;</code>：有声明和调用，但所有 <code>.o</code> 里都找不到定义</li>
<li><code>multiple definition of &#39;add&#39;</code>：多个 <code>.o</code> 里都有同名定义，不知道用哪个</li>
</ul>
<p><strong>输出</strong>：可执行文件，所有地址填好，可以直接运行</p>
<hr>
<h2 id="五、-h-和-cpp-分离的原因"><a href="#五、-h-和-cpp-分离的原因" class="headerlink" title="五、.h 和 .cpp 分离的原因"></a>五、.h 和 .cpp 分离的原因</h2><p><strong>声明 vs 定义：</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span></span>;                   <span class="comment">// 声明：描述函数签名，不含实现，可出现多次</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span>&#123; <span class="keyword">return</span> a + b; &#125;  <span class="comment">// 定义：包含实现，只能出现一次</span></span><br></pre></td></tr></table></figure>

<p>分离的做法：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// add.h：只放声明，被多个文件 #include，每次预处理展开一次，没有重复定义问题</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// add.cpp：只放定义，只编译一次，生成唯一的 add.o</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;add.h&quot;</span></span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span>&#123; <span class="keyword">return</span> a + b; &#125;</span><br></pre></td></tr></table></figure>

<p>数据流：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">add.h ──────────────────────────────────────┐</span><br><span class="line">                                             ↓ #include 展开</span><br><span class="line">main.cpp ──→ 预处理器 ──→ 翻译单元 ──→ 编译器 ──→ main.o（add: U）</span><br><span class="line">                                                        ↓</span><br><span class="line">add.cpp ──→ 预处理器 ──→ 翻译单元 ──→ 编译器 ──→ add.o（add: T）──→ 链接器 ──→ my_program</span><br></pre></td></tr></table></figure>

<p><code>#include &quot;add.h&quot;</code> vs <code>#include &lt;iostream&gt;</code>：</p>
<ul>
<li>双引号：先在当前目录找，找不到再找系统路径</li>
<li>尖括号：只找系统路径（标准库、已安装的第三方库）</li>
</ul>
<hr>
<h2 id="六、CMake"><a href="#六、CMake" class="headerlink" title="六、CMake"></a>六、CMake</h2><p>手动编译多文件项目繁琐，CMake 通过 <code>CMakeLists.txt</code> 描述项目结构，自动生成编译命令。</p>
<p><strong>最小项目结构：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">my_project/</span><br><span class="line">├── CMakeLists.txt</span><br><span class="line">├── add.h</span><br><span class="line">├── add.cpp</span><br><span class="line">└── main.cpp</span><br></pre></td></tr></table></figure>

<p><strong>CMakeLists.txt：</strong></p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">cmake_minimum_required</span>(VERSION <span class="number">3.10</span>)</span><br><span class="line"><span class="keyword">project</span>(my_project)</span><br><span class="line"><span class="keyword">set</span>(CMAKE_CXX_STANDARD <span class="number">17</span>)</span><br><span class="line"><span class="keyword">add_executable</span>(my_program main.cpp add.cpp)</span><br></pre></td></tr></table></figure>

<p><code>add_executable(my_program main.cpp add.cpp)</code> 等价于：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">g++ -c main.cpp -o main.o</span><br><span class="line">g++ -c add.cpp  -o add.o</span><br><span class="line">g++ main.o add.o -o my_program</span><br></pre></td></tr></table></figure>

<p>漏掉 <code>add.cpp</code>：链接器报 <code>undefined reference to &#39;add&#39;</code>。</p>
<p><strong>构建命令：</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> build &amp;&amp; <span class="built_in">cd</span> build</span><br><span class="line">cmake ..     <span class="comment"># 读取 CMakeLists.txt，生成 Makefile</span></span><br><span class="line">make         <span class="comment"># 执行编译和链接</span></span><br><span class="line">./my_program</span><br></pre></td></tr></table></figure>

<p><strong>工业项目的组织方式：</strong> 不把所有文件列入 <code>add_executable</code>，而是用库来组织：</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">add_library</span>(my_lib add.cpp utils.cpp)</span><br><span class="line"><span class="keyword">add_executable</span>(my_program main.cpp)</span><br><span class="line"><span class="keyword">target_link_libraries</span>(my_program my_lib)</span><br></pre></td></tr></table></figure>

<p>大项目每个子目录有自己的 <code>CMakeLists.txt</code>，各自打包成库，顶层用 <code>add_subdirectory</code> 组合。</p>
<hr>
<h2 id="参考材料"><a href="#参考材料" class="headerlink" title="参考材料"></a>参考材料</h2><ol>
<li><strong>learncpp.com（编译流程）</strong>：<a href="https://www.learncpp.com/cpp-tutorial/introduction-to-the-preprocessor/">https://www.learncpp.com/cpp-tutorial/introduction-to-the-preprocessor/</a></li>
<li><strong>CMake Tutorial</strong>：<a href="https://cmake.org/cmake/help/latest/guide/tutorial/">https://cmake.org/cmake/help/latest/guide/tutorial/</a></li>
</ol>
]]></content>
      <tags>
        <tag>cpp</tag>
        <tag>llm-inference</tag>
        <tag>cmake</tag>
        <tag>compilation</tag>
        <tag>linking</tag>
        <tag>header</tag>
        <tag>preprocessor</tag>
      </tags>
  </entry>
  <entry>
    <title>04 · CUDA 执行模型</title>
    <url>/2026/04/01/llm-inference/notes/04-cuda-execution-model/</url>
    <content><![CDATA[<h1 id="04-·-CUDA-执行模型"><a href="#04-·-CUDA-执行模型" class="headerlink" title="04 · CUDA 执行模型"></a>04 · CUDA 执行模型</h1><blockquote>
<p><strong>目标</strong>：理解 GPU 和 CPU 的执行模型根本差异，掌握 thread&#x2F;warp&#x2F;block&#x2F;grid 四层层级，理解 warp divergence 和延迟隐藏机制</p>
</blockquote>
<hr>
<h2 id="一、GPU-和-CPU-的根本差异"><a href="#一、GPU-和-CPU-的根本差异" class="headerlink" title="一、GPU 和 CPU 的根本差异"></a>一、GPU 和 CPU 的根本差异</h2><p><strong>高层抽象：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CPU：少量强大的核心（8-32），每个核心有复杂的分支预测、乱序执行、大缓存</span><br><span class="line">     → 设计目标：让单个任务跑得尽可能快</span><br><span class="line"></span><br><span class="line">GPU：大量简单的核心（3080 Ti 有 10240 个 CUDA core），每个核心只做简单的乘加</span><br><span class="line">     → 设计目标：让海量任务同时跑</span><br></pre></td></tr></table></figure>

<p>矩阵乘法天然适合 GPU——拆成几万个独立的乘加操作，同时计算。</p>
<p><strong>为什么 GPU 核心可以这么多：</strong></p>
<p>每个 GPU 核心没有复杂的控制逻辑（分支预测、乱序执行），省下来的晶体管全部用来堆计算单元。代价是编程模型受限——程序员必须显式组织并行。</p>
<hr>
<h2 id="二、四层执行层级"><a href="#二、四层执行层级" class="headerlink" title="二、四层执行层级"></a>二、四层执行层级</h2><h3 id="Thread（线程）"><a href="#Thread（线程）" class="headerlink" title="Thread（线程）"></a>Thread（线程）</h3><p>最小执行单元，执行一次 kernel 函数调用。每个 thread 有独立的寄存器和局部变量。</p>
<p>例：矩阵加法 <code>C[i] = A[i] + B[i]</code>，每个 thread 负责计算一个元素。</p>
<h3 id="Warp（线程束）"><a href="#Warp（线程束）" class="headerlink" title="Warp（线程束）"></a>Warp（线程束）</h3><p><strong>32 个 thread 组成一个 warp，是 GPU 硬件的调度单位。</strong></p>
<p>GPU 不单独调度每个 thread，而是以 warp 为单位，32 个 thread 同时执行同一条指令。这叫 <strong>SIMT（Single Instruction Multiple Threads）</strong>。</p>
<p><strong>为什么以 warp 为单位而不是单个 thread：</strong></p>
<p>硬件成本问题。给每个 thread 配独立的指令译码器和调度逻辑代价极高。warp 设计让 32 个 thread 共享一套控制硬件，控制硬件成本降低 32 倍，省下的晶体管用来堆更多 CUDA core。代价是 32 个 thread 必须走同一条指令流。</p>
<h3 id="Warp-Divergence（线程束分歧）"><a href="#Warp-Divergence（线程束分歧）" class="headerlink" title="Warp Divergence（线程束分歧）"></a>Warp Divergence（线程束分歧）</h3><p>同一个 warp 里的 thread 遇到 <code>if/else</code> 走不同分支时发生：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 同一 warp 里一半走 if，一半走 else → divergence</span></span><br><span class="line"><span class="keyword">if</span> (threadIdx.x % <span class="number">2</span> == <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="comment">// 偶数 thread 的计算</span></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// 奇数 thread 的计算</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>硬件实现机制：active mask（活跃掩码）</strong></p>
<p>每个 warp 有一个 32 位的 active mask，记录哪些 thread 当前活跃：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">第一轮：active mask = 1111...0000（前16个thread执行if，后16个被屏蔽）</span><br><span class="line">        被屏蔽的 thread 照样走指令流，但写入结果被丢弃</span><br><span class="line">第二轮：active mask = 0000...1111（后16个thread执行else，前16个被屏蔽）</span><br></pre></td></tr></table></figure>

<p>实际执行时间 &#x3D; if 分支时间 + else 分支时间，并行度减半。这是硬件层面的串行，不是软件层面的。</p>
<h3 id="Block（线程块）"><a href="#Block（线程块）" class="headerlink" title="Block（线程块）"></a>Block（线程块）</h3><p>多个 thread 组成一个 block。block 内的 thread 可以：</p>
<ul>
<li>通过 <strong>shared memory</strong> 共享数据</li>
<li>通过 <code>__syncthreads()</code> 同步</li>
</ul>
<p>block 内最多 1024 个 thread。blockSize 应该是 32 的倍数——如果写 <code>blockSize = 100</code>，GPU 会分配 4 个 warp（128 个 thread slot），其中 28 个 slot 是空的，浪费了硬件资源。</p>
<p><strong>Block 和 Warp 的关系：</strong></p>
<p>block 是编程模型上的概念，warp 是硬件调度单位。你定义 <code>blockSize = 256</code>，GPU 把这 256 个 thread 自动分成 8 个 warp（256 &#x2F; 32 &#x3D; 8），以 warp 为单位调度执行：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Block（程序员定义）</span><br><span class="line">├── Warp 0（thread 0-31）   ← 硬件实际调度</span><br><span class="line">├── Warp 1（thread 32-63）</span><br><span class="line">├── Warp 2（thread 64-95）</span><br><span class="line">└── ...（共 8 个 warp）</span><br></pre></td></tr></table></figure>

<p><strong>Block 的生命周期：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">kernel 启动 → 所有 block 进入等待队列</span><br><span class="line">    ↓</span><br><span class="line">SM 空闲时，从队列取一个 block 开始执行</span><br><span class="line">    ↓</span><br><span class="line">block 内所有 thread 执行完 kernel 函数的最后一行</span><br><span class="line">    ↓</span><br><span class="line">block 结束，SM 释放，Shared Memory 清空</span><br><span class="line">    ↓</span><br><span class="line">SM 取下一个 block</span><br></pre></td></tr></table></figure>

<p><code>__shared__</code> 变量的生命周期 &#x3D; block 的生命周期。block 开始时分配，block 结束时释放。不同 block 的 Shared Memory 互相独立，互不可见。</p>
<h3 id="Grid（网格）"><a href="#Grid（网格）" class="headerlink" title="Grid（网格）"></a>Grid（网格）</h3><p>多个 block 组成一个 grid，是一次 kernel 启动的全部 thread。block 之间不能直接通信，不能保证执行顺序。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Grid</span><br><span class="line">├── Block(0,0)  Block(0,1)  Block(0,2)</span><br><span class="line">├── Block(1,0)  Block(1,1)  Block(1,2)</span><br><span class="line">└── Block(2,0)  Block(2,1)  Block(2,2)</span><br><span class="line">        每个 Block 内有多个 Thread</span><br><span class="line">        每 32 个 Thread 组成一个 Warp</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="三、内置变量与坐标系"><a href="#三、内置变量与坐标系" class="headerlink" title="三、内置变量与坐标系"></a>三、内置变量与坐标系</h2><p>CUDA 提供四个内置变量，每个 thread 启动时由 GPU 硬件自动注入：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">threadIdx：当前 thread 在 block 内的坐标（x, y, z）</span><br><span class="line">blockIdx： 当前 block 在 grid 内的坐标（x, y, z）</span><br><span class="line">blockDim： 每个 block 的尺寸，即有多少个 thread（x, y, z）</span><br><span class="line">gridDim：  grid 的尺寸，即有多少个 block（x, y, z）</span><br></pre></td></tr></table></figure>

<p>x&#x2F;y&#x2F;z 是三个维度的坐标轴名称，没有特殊含义：</p>
<ul>
<li>一维：处理数组 → 只用 x</li>
<li>二维：处理矩阵 → 用 x 和 y</li>
<li>三维：处理三维数据 → 用 x、y、z</li>
</ul>
<p><strong>坐标与 block 是一一对应的关系。</strong> grid 的形状在 kernel 启动时固定，<code>(0,0)</code> 和 <code>(1,1)</code> 在任何情况下都是两个不同的 block，不存在同一个 block 有多种坐标的情况。</p>
<h2 id="四、Thread-下标计算"><a href="#四、Thread-下标计算" class="headerlink" title="四、Thread 下标计算"></a>四、Thread 下标计算</h2><p><strong>一维（vector add）：</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line"><span class="comment">// 本质：block 的起始位置 + thread 在 block 内的偏移</span></span><br></pre></td></tr></table></figure>

<p>具体数字（blockSize&#x3D;256）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Block 0 的 thread 0：  i = 0 * 256 + 0   = 0</span><br><span class="line">Block 0 的 thread 255：i = 0 * 256 + 255  = 255</span><br><span class="line">Block 1 的 thread 0：  i = 1 * 256 + 0   = 256</span><br><span class="line">Block 1 的 thread 1：  i = 1 * 256 + 1   = 257</span><br></pre></td></tr></table></figure>

<p><strong>二维（GEMM）：</strong></p>
<p>矩阵是二维的，自然地用二维坐标映射。x 对应列，y 对应行：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> row = blockIdx.y * blockDim.y + threadIdx.y;  <span class="comment">// 负责 C 的第几行</span></span><br><span class="line"><span class="type">int</span> col = blockIdx.x * blockDim.x + threadIdx.x;  <span class="comment">// 负责 C 的第几列</span></span><br></pre></td></tr></table></figure>

<p>假设矩阵 4×4，TILE_SIZE&#x3D;2，block(1,1) 里的 thread(0,1)：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">row = blockIdx.y * TILE_SIZE + threadIdx.y = 1 * 2 + 1 = 3</span><br><span class="line">col = blockIdx.x * TILE_SIZE + threadIdx.x = 1 * 2 + 0 = 2</span><br><span class="line">→ 负责计算 C[3][2]</span><br></pre></td></tr></table></figure>

<p>和一维完全相同的逻辑，x 和 y 两个方向独立计算。</p>
<hr>
<h2 id="四、CPU-GPU-内存模型与数据传输"><a href="#四、CPU-GPU-内存模型与数据传输" class="headerlink" title="四、CPU &#x2F; GPU 内存模型与数据传输"></a>四、CPU &#x2F; GPU 内存模型与数据传输</h2><p>CPU 和 GPU 是两块独立硬件，各自有独立的内存：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CPU ←→ RAM（几十 GB）</span><br><span class="line">GPU ←→ VRAM/显存（12 GB）</span><br><span class="line">两者通过 PCIe 总线连接，带宽约 16 GB/s</span><br></pre></td></tr></table></figure>

<p>GPU 不能直接读 CPU 的内存，必须先用 <code>cudaMemcpy</code> 复制到显存：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">h_A（CPU内存）─── cudaMemcpy HostToDevice ──→ d_A（GPU显存）</span><br><span class="line">                        kernel 计算</span><br><span class="line">h_C（CPU内存）←── cudaMemcpy DeviceToHost ─── d_C（GPU显存）</span><br></pre></td></tr></table></figure>

<p><code>h_</code> 前缀 &#x3D; host（CPU），<code>d_</code> 前缀 &#x3D; device（GPU）——CUDA 代码的命名惯例。</p>
<p>PCIe 传输是推理延迟的重要来源，推理优化要尽量减少 CPU&#x2F;GPU 之间的数据搬运。</p>
<hr>
<h2 id="五、Occupancy-与延迟隐藏"><a href="#五、Occupancy-与延迟隐藏" class="headerlink" title="五、Occupancy 与延迟隐藏"></a>五、Occupancy 与延迟隐藏</h2><p><strong>SM（Streaming Multiprocessor）</strong> 是 GPU 的计算单元，3080 Ti 有 80 个 SM。每个 SM 可以同时持有多个 warp。</p>
<p><strong>延迟隐藏机制：</strong></p>
<p>显存访问延迟约 200-800 个时钟周期。单个 warp 等待内存时，SM 切换到其他 warp 继续执行，让多个 warp 的内存请求同时在飞：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">时间轴：</span><br><span class="line">Warp A：发出内存请求 ──等待──→ 拿到数据，计算</span><br><span class="line">Warp B：        发出内存请求 ──等待──→ 拿到数据，计算</span><br><span class="line">Warp C：                发出内存请求 ──等待──→ 拿到数据，计算</span><br></pre></td></tr></table></figure>

<p>三个 warp 的等待时间重叠，内存控制器并行处理所有请求。只有一个 warp 时，等待时间完全浪费。</p>
<p><strong>Occupancy（占用率）</strong>：SM 实际运行的 warp 数 &#x2F; SM 最大支持的 warp 数。Occupancy 越高，延迟隐藏效果越好。</p>
<p><strong>实验结果（16M 元素 vector add，3080 Ti）：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">blockSize=  32  time=0.378 ms   ← occupancy 低，延迟隐藏差</span><br><span class="line">blockSize=  64  time=0.243 ms   ← occupancy 提升，延迟被充分隐藏</span><br><span class="line">blockSize= 128  time=0.243 ms</span><br><span class="line">blockSize= 256  time=0.244 ms   ← 64 之后持平：瓶颈转移到显存带宽</span><br><span class="line">blockSize= 512  time=0.244 ms</span><br><span class="line">blockSize=1024  time=0.245 ms</span><br></pre></td></tr></table></figure>

<p>64 之后持平的原因：这是 memory-bound kernel（只做一次加法，主要时间在读写显存）。occupancy 够了之后，瓶颈是显存带宽上限，再增大 blockSize 不会让显存更快。</p>
<hr>
<h2 id="六、第一个-Kernel：Vector-Add"><a href="#六、第一个-Kernel：Vector-Add" class="headerlink" title="六、第一个 Kernel：Vector Add"></a>六、第一个 Kernel：Vector Add</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">__global__ void vector_add(float* A, float* B, float* C, int N) &#123;</span><br><span class="line">    int i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    if (i &lt; N) C[i] = A[i] + B[i];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 启动方式</span><br><span class="line">int blockSize = 256;</span><br><span class="line">int gridSize = (N + blockSize - 1) / blockSize;  // ceil(N / blockSize)</span><br><span class="line">vector_add&lt;&lt;&lt;gridSize, blockSize&gt;&gt;&gt;(d_A, d_B, d_C, N);</span><br></pre></td></tr></table></figure>

<p><code>__global__</code>：在 GPU 上执行，从 CPU 调用。</p>
<p><code>if (i &lt; N)</code>：防止最后一个 block 的部分 thread 越界（N 不一定是 blockSize 的整数倍）。</p>
<hr>
<h2 id="参考材料"><a href="#参考材料" class="headerlink" title="参考材料"></a>参考材料</h2><ol>
<li><strong>CUDA C Programming Guide Ch1-3</strong>：<a href="https://docs.nvidia.com/cuda/cuda-c-programming-guide/">https://docs.nvidia.com/cuda/cuda-c-programming-guide/</a></li>
</ol>
]]></content>
      <tags>
        <tag>cuda</tag>
        <tag>llm-inference</tag>
        <tag>gpu</tag>
        <tag>thread</tag>
        <tag>warp</tag>
        <tag>block</tag>
        <tag>grid</tag>
        <tag>occupancy</tag>
        <tag>memory-latency</tag>
      </tags>
  </entry>
  <entry>
    <title>05 · CUDA 内存优化</title>
    <url>/2026/04/01/llm-inference/notes/05-cuda-memory-optimization/</url>
    <content><![CDATA[<h1 id="05-·-CUDA-内存优化"><a href="#05-·-CUDA-内存优化" class="headerlink" title="05 · CUDA 内存优化"></a>05 · CUDA 内存优化</h1><blockquote>
<p><strong>目标</strong>：理解 GPU 内存层级，掌握 Shared Memory Tiling 优化 GEMM 的原理，理解 bank conflict 和延迟隐藏机制</p>
</blockquote>
<hr>
<h2 id="一、GPU-内存层级"><a href="#一、GPU-内存层级" class="headerlink" title="一、GPU 内存层级"></a>一、GPU 内存层级</h2><p><strong>高层抽象：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">寄存器（Register）  →  Shared Memory  →  L2 Cache  →  HBM（显存）</span><br><span class="line">     最快                                                  最慢</span><br><span class="line">     最小                                                  最大</span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th>层级</th>
<th>作用域</th>
<th>延迟</th>
<th>容量（3080 Ti）</th>
</tr>
</thead>
<tbody><tr>
<td>寄存器</td>
<td>每个 thread 私有</td>
<td>~0</td>
<td>极小</td>
</tr>
<tr>
<td>Shared Memory</td>
<td>block 内共享</td>
<td>~5ns</td>
<td>每个 SM 约 100KB</td>
</tr>
<tr>
<td>L2 Cache</td>
<td>所有 SM 共享</td>
<td>中等</td>
<td>6MB（4090D 72MB）</td>
</tr>
<tr>
<td>HBM</td>
<td>所有 SM 共享</td>
<td>200-800 时钟周期</td>
<td>12GB</td>
</tr>
</tbody></table>
<p><strong>核心推论：</strong> 同一时刻需要被多次读取的数据，应该先从 HBM 搬到 Shared Memory，再从 Shared Memory 读取计算——减少 HBM IO 次数。</p>
<hr>
<h2 id="二、CUDA-编程的固定骨架"><a href="#二、CUDA-编程的固定骨架" class="headerlink" title="二、CUDA 编程的固定骨架"></a>二、CUDA 编程的固定骨架</h2><p>所有 CUDA 程序围绕一个核心问题：CPU 和 GPU 是两块独立硬件，内存独立，不能直接互访。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1. 在 GPU 上分配内存（cudaMalloc）</span><br><span class="line">2. 把数据从 CPU 内存复制到 GPU 内存（cudaMemcpy HostToDevice）</span><br><span class="line">3. 启动 kernel，在 GPU 上并行计算（&lt;&lt;&lt;grid, block&gt;&gt;&gt;）</span><br><span class="line">4. 把结果从 GPU 内存复制回 CPU 内存（cudaMemcpy DeviceToHost）</span><br><span class="line">5. 释放 GPU 内存（cudaFree）</span><br></pre></td></tr></table></figure>

<p>关键语法逐行说明：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">float *d_A;</span><br><span class="line">cudaMalloc(&amp;d_A, size);</span><br><span class="line">// cudaMalloc：在 GPU 显存分配内存，返回的指针只能在 GPU 上用</span><br><span class="line">// &amp;d_A：传指针的地址，cudaMalloc 把分配好的显存地址写入 d_A</span><br><span class="line"></span><br><span class="line">cudaMemcpy(d_A, h_A, size, cudaMemcpyHostToDevice);</span><br><span class="line">// 把数据从 CPU 内存复制到 GPU 显存</span><br><span class="line">// 方向：cudaMemcpyHostToDevice（CPU→GPU）或 cudaMemcpyDeviceToHost（GPU→CPU）</span><br><span class="line"></span><br><span class="line">vector_add&lt;&lt;&lt;gridSize, blockSize&gt;&gt;&gt;(d_A, d_B, d_C, N);</span><br><span class="line">// &lt;&lt;&lt;gridSize, blockSize&gt;&gt;&gt;：kernel 启动语法</span><br><span class="line">// kernel 是异步的，CPU 调用后不等 GPU 算完就继续执行</span><br><span class="line"></span><br><span class="line">cudaMemcpy(h_C, d_C, size, cudaMemcpyDeviceToHost);</span><br><span class="line">// 隐式等待 GPU 完成，再把结果复制回 CPU</span><br><span class="line"></span><br><span class="line">cudaFree(d_A);</span><br><span class="line">// 释放 GPU 显存，对应 cudaMalloc，不释放会泄漏显存</span><br></pre></td></tr></table></figure>

<p><code>__global__</code>：kernel 函数的标志，在 GPU 上执行，从 CPU 调用。函数参数里的指针必须是 GPU 显存指针，传 CPU 内存指针会崩溃。</p>
<p><code>__shared__</code>：声明 Shared Memory 变量，block 内所有 thread 共享，生命周期等于 block 的生命周期。</p>
<h2 id="三、GEMM-是什么"><a href="#三、GEMM-是什么" class="headerlink" title="三、GEMM 是什么"></a>三、GEMM 是什么</h2><p>GEMM（General Matrix Multiply）：<code>C = A × B</code>，A 是 M×K，B 是 K×N，C 是 M×N。</p>
<p>推理计算量最大的操作全是 GEMM：</p>
<ul>
<li>FFN 的 W₁、W₂ 矩阵乘法</li>
<li>Attention 的 QKᵀ 点积</li>
<li>Attention 的 score × V</li>
</ul>
<hr>
<h2 id="三、Naive-GEMM-的问题"><a href="#三、Naive-GEMM-的问题" class="headerlink" title="三、Naive GEMM 的问题"></a>三、Naive GEMM 的问题</h2><p>每个 thread 计算 C 的一个元素，直接从 HBM 读数据：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">__global__ void gemm_naive(float* A, float* B, float* C, int n) &#123;</span><br><span class="line">    int row = blockIdx.y * blockDim.y + threadIdx.y;</span><br><span class="line">    int col = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    if (row &lt; n &amp;&amp; col &lt; n) &#123;</span><br><span class="line">        float sum = 0.0f;</span><br><span class="line">        for (int k = 0; k &lt; n; k++)</span><br><span class="line">            sum += A[row * n + k] * B[k * n + col];</span><br><span class="line">        C[row * n + col] = sum;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>问题：A 的同一行被 N 个 thread 各从 HBM 读一次，B 的同一列被 M 个 thread 各从 HBM 读一次。大量重复的 HBM 读取。</p>
<hr>
<h2 id="四、Shared-Memory-Tiling"><a href="#四、Shared-Memory-Tiling" class="headerlink" title="四、Shared Memory Tiling"></a>四、Shared Memory Tiling</h2><p><strong>核心思路：</strong> 把 A 和 B 分成 TILE_SIZE × TILE_SIZE 的小块，每次协作把一个 tile 加载到 Shared Memory，block 内所有 thread 复用这份数据。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">HBM → Shared Memory（第一个 __syncthreads__ 等待这步完成）</span><br><span class="line">Shared Memory → 寄存器 → 计算（第二个 __syncthreads__ 等待这步完成）</span><br></pre></td></tr></table></figure>

<p><code>__syncthreads()</code> 的作用：</p>
<ul>
<li><strong>第一个</strong>：等所有 thread 把数据从 HBM 搬到 Shared Memory。如果去掉，某些 thread 还没写完，其他 thread 就开始读，得到垃圾数据</li>
<li><strong>第二个</strong>：等所有 thread 计算完毕，再加载下一个 tile，避免覆盖还在使用中的数据</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">__global__ void gemm_tiled(float* A, float* B, float* C, int n) &#123;</span><br><span class="line">    __shared__ float tileA[TILE_SIZE][TILE_SIZE];</span><br><span class="line">    __shared__ float tileB[TILE_SIZE][TILE_SIZE];</span><br><span class="line"></span><br><span class="line">    int row = blockIdx.y * TILE_SIZE + threadIdx.y;</span><br><span class="line">    int col = blockIdx.x * TILE_SIZE + threadIdx.x;</span><br><span class="line">    float sum = 0.0f;</span><br><span class="line"></span><br><span class="line">    for (int t = 0; t &lt; n / TILE_SIZE; t++) &#123;</span><br><span class="line">        // 协作加载 tile 到 Shared Memory</span><br><span class="line">        tileA[threadIdx.y][threadIdx.x] = A[row * n + (t * TILE_SIZE + threadIdx.x)];</span><br><span class="line">        tileB[threadIdx.y][threadIdx.x] = B[(t * TILE_SIZE + threadIdx.y) * n + col];</span><br><span class="line">        __syncthreads();  // 等所有 thread 加载完</span><br><span class="line"></span><br><span class="line">        // 在 Shared Memory 里计算</span><br><span class="line">        for (int k = 0; k &lt; TILE_SIZE; k++)</span><br><span class="line">            sum += tileA[threadIdx.y][k] * tileB[k][threadIdx.x];</span><br><span class="line">        __syncthreads();  // 等所有 thread 算完，再加载下一个 tile</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    if (row &lt; n &amp;&amp; col &lt; n)</span><br><span class="line">        C[row * n + col] = sum;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>每个 tile 只从 HBM 读一次，被 block 内 TILE_SIZE 个 thread 复用——HBM 访问减少 TILE_SIZE 倍。</p>
<hr>
<h2 id="五、实验结果（4090D，1024×1024）"><a href="#五、实验结果（4090D，1024×1024）" class="headerlink" title="五、实验结果（4090D，1024×1024）"></a>五、实验结果（4090D，1024×1024）</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">naive    time=0.477 ms  GFLOPS=4498.9</span><br><span class="line">tiled    time=0.373 ms  GFLOPS=5755.1</span><br><span class="line">Speedup: 1.3x</span><br></pre></td></tr></table></figure>

<p>4090D 的 L2 Cache 是 72MB，几乎把整个 1024×1024 矩阵（4MB）缓存住了。naive kernel 的重复 HBM 读取大部分命中 L2，tiling 的优势被硬件缓存抵消。</p>
<p>两个版本都只达到 4090D 理论峰值（82.6 TFLOPS）的 5-7%——完全是 memory-bound，算力没有被用满。</p>
<p><strong>优化效果取决于硬件：</strong> 在 L2 更小的老 GPU 上，tiling 的提升可以达到 5-10x。理解内存层级和数据复用是写高性能 kernel 的基础，不因为硬件自动缓存了就失去意义。</p>
<hr>
<h2 id="六、Bank-Conflict"><a href="#六、Bank-Conflict" class="headerlink" title="六、Bank Conflict"></a>六、Bank Conflict</h2><p>Shared Memory 被分成 32 个 bank，每个 bank 宽度 4 字节。同一个 warp 里的 thread 访问 Shared Memory 时，如果多个 thread 访问同一个 bank 的不同地址，会被串行化。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">无冲突（每个 thread 访问不同 bank）：</span><br><span class="line">thread 0 → bank 0，thread 1 → bank 1，...  → 一次完成</span><br><span class="line"></span><br><span class="line">有冲突（多个 thread 访问同一 bank）：</span><br><span class="line">thread 0 → bank 0，thread 1 → bank 0  → 串行执行，性能减半</span><br></pre></td></tr></table></figure>

<p><strong>tiled kernel 里的问题：</strong></p>
<p>内循环 <code>tileA[threadIdx.y][k]</code> 中，所有 thread 的 k 相同，threadIdx.y 不同，访问的是同一列（列优先访问），间隔 TILE_SIZE 个元素，导致 bank conflict。</p>
<p><strong>优化方式：加载时转置存储 tileA</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 加载时转置</span><br><span class="line">tileA[threadIdx.x][threadIdx.y] = A[row * n + (t * TILE_SIZE + threadIdx.x)];</span><br><span class="line"></span><br><span class="line">// 内循环变成行访问，连续，无 bank conflict</span><br><span class="line">sum += tileA[k][threadIdx.y] * tileB[k][threadIdx.x];</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="七、Warp-调度与指令执行"><a href="#七、Warp-调度与指令执行" class="headerlink" title="七、Warp 调度与指令执行"></a>七、Warp 调度与指令执行</h2><p>同一个 warp 内的 32 个 thread 在同一时刻执行同一条指令（SIMT）。</p>
<p>不同 warp 之间完全独立，各自执行各自的指令：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Warp 0：执行 load A[i]</span><br><span class="line">Warp 1：执行 multiply</span><br><span class="line">Warp 2：执行 store C[i]</span><br><span class="line">        ← 三个 warp 同时在 SM 上，执行不同指令</span><br></pre></td></tr></table></figure>

<p>SM 的调度器每个时钟周期选一个 ready 的 warp 发射指令，在多个 warp 之间切换，让执行单元始终有活干——这是延迟隐藏的底层机制。</p>
<hr>
<h2 id="参考材料"><a href="#参考材料" class="headerlink" title="参考材料"></a>参考材料</h2><ol>
<li><strong>CUDA C Programming Guide Ch5-6</strong>：<a href="https://docs.nvidia.com/cuda/cuda-c-programming-guide/">https://docs.nvidia.com/cuda/cuda-c-programming-guide/</a></li>
<li><strong>how-to-optimize-gemm</strong>：<a href="https://github.com/yzhaiustc/Optimizing-GEMM-on-NVIDIA-Turing-GPUs">https://github.com/yzhaiustc/Optimizing-GEMM-on-NVIDIA-Turing-GPUs</a></li>
</ol>
]]></content>
      <tags>
        <tag>cuda</tag>
        <tag>llm-inference</tag>
        <tag>shared-memory</tag>
        <tag>tiling</tag>
        <tag>gemm</tag>
        <tag>bank-conflict</tag>
        <tag>memory-hierarchy</tag>
      </tags>
  </entry>
  <entry>
    <title>06 · GPU 性能模型与分析工具</title>
    <url>/2026/04/01/llm-inference/notes/06-gpu-performance-model/</url>
    <content><![CDATA[<h1 id="06-·-GPU-性能模型与分析工具"><a href="#06-·-GPU-性能模型与分析工具" class="headerlink" title="06 · GPU 性能模型与分析工具"></a>06 · GPU 性能模型与分析工具</h1><blockquote>
<p><strong>目标</strong>：理解 Roofline Model，能计算 kernel 的算术强度，判断瓶颈在带宽还是算力，知道该往哪个方向优化</p>
</blockquote>
<hr>
<h2 id="一、两个硬件上限"><a href="#一、两个硬件上限" class="headerlink" title="一、两个硬件上限"></a>一、两个硬件上限</h2><p>GPU 的性能受两个硬件上限约束：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">算力上限：GPU 每秒能做多少次浮点运算（TFLOPS）</span><br><span class="line">带宽上限：GPU 每秒能从显存搬多少数据（TB/s）</span><br></pre></td></tr></table></figure>

<p>4090D 的参数：</p>
<ul>
<li>算力峰值：82.6 TFLOPS（FP32）&#x3D; 82600 GFLOPS</li>
<li>显存带宽：1008 GB&#x2F;s</li>
</ul>
<hr>
<h2 id="二、算术强度"><a href="#二、算术强度" class="headerlink" title="二、算术强度"></a>二、算术强度</h2><p><strong>算术强度（Arithmetic Intensity）</strong>：kernel 的固有属性，由计算量和数据量的比值决定：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">算术强度 = 总浮点运算次数（FLOP） / 总显存访问字节数（Byte）</span><br></pre></td></tr></table></figure>

<p>不需要运行就能算出来，只需数清楚 kernel 做了多少次运算、读写了多少数据。</p>
<p><strong>Vector Add 的算术强度：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">C[i] = A[i] + B[i]，N 个元素</span><br><span class="line"></span><br><span class="line">浮点运算：N 次加法 = N FLOP</span><br><span class="line">显存访问：读 A（4N Byte）+ 读 B（4N Byte）+ 写 C（4N Byte）= 12N Byte</span><br><span class="line"></span><br><span class="line">算术强度 = N / 12N = 1/12 ≈ 0.083 FLOP/Byte</span><br></pre></td></tr></table></figure>

<p><strong>Naive GEMM 的算术强度：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">C = A × B，N×N 矩阵</span><br><span class="line"></span><br><span class="line">浮点运算：2N³ FLOP（每个元素 N 次乘加，共 N² 个元素）</span><br><span class="line">显存访问：每个元素读 A 一行（N 次）+ B 一列（N 次）+ 写 C = (2N+1) 次</span><br><span class="line">          共 N²(2N+1) × 4 Byte ≈ 8N³ Byte</span><br><span class="line"></span><br><span class="line">算术强度 = 2N³ / 8N³ = 0.25 FLOP/Byte</span><br></pre></td></tr></table></figure>

<p><strong>规律：计算越密集、数据复用越多，算术强度越高。</strong></p>
<p>Tiled GEMM 每个 tile 只从 HBM 读一次但被 TILE_SIZE 个 thread 复用，HBM 访问量缩小 TILE_SIZE 倍，算术强度从 0.25 提升到 8.0。</p>
<hr>
<h2 id="三、Ridge-Point"><a href="#三、Ridge-Point" class="headerlink" title="三、Ridge Point"></a>三、Ridge Point</h2><p><strong>Ridge Point</strong> &#x3D; 算力上限 &#x2F; 带宽上限，是硬件的固有属性：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ridge point = 82600 GFLOPS / 1008 GB/s ≈ 82 FLOP/Byte</span><br></pre></td></tr></table></figure>

<p>物理含义：<strong>带宽刚好能喂饱算力时的算术强度</strong>。</p>
<ul>
<li>算术强度 &lt; ridge point → memory-bound（带宽是瓶颈）</li>
<li>算术强度 &gt; ridge point → compute-bound（算力是瓶颈）</li>
</ul>
<p>Ridge point 是分界线，不随 kernel 变化。</p>
<hr>
<h2 id="四、Roofline-Model"><a href="#四、Roofline-Model" class="headerlink" title="四、Roofline Model"></a>四、Roofline Model</h2><p>把两个硬件约束画在同一张图上：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">性能</span><br><span class="line">(GFLOPS)</span><br><span class="line">  |                    ____________  ← 算力上限（水平线）</span><br><span class="line">  |                   /↑</span><br><span class="line">  |                  / ridge point（交点）</span><br><span class="line">  |                 /</span><br><span class="line">  |                /  ← 斜率 = 带宽（GB/s）</span><br><span class="line">  |               /</span><br><span class="line">  |              /</span><br><span class="line">  |             /</span><br><span class="line">  |            /</span><br><span class="line">  |           /</span><br><span class="line">  |          /</span><br><span class="line">  |         /</span><br><span class="line">  |        /</span><br><span class="line">  |       /</span><br><span class="line">  |      /</span><br><span class="line">  |     /</span><br><span class="line">  |    /</span><br><span class="line">  |   /</span><br><span class="line">  |  /</span><br><span class="line">  | /</span><br><span class="line">  |/</span><br><span class="line">  |______________________________</span><br><span class="line">  0                    算术强度（FLOP/Byte）</span><br></pre></td></tr></table></figure>

<ul>
<li><strong>斜线段</strong>（算术强度 &lt; ridge point）：memory-bound。性能 &#x3D; 算术强度 × 带宽，算术强度越高性能越高。优化方向：减少 HBM 访问，提升数据复用（如 tiling）</li>
<li><strong>水平线段</strong>（算术强度 &gt; ridge point）：compute-bound。性能 &#x3D; 算力上限，继续提升算术强度无收益。优化方向：提高算力利用率（如减少 warp divergence、提升 occupancy）</li>
</ul>
<p>Kernel 在图上的位置：</p>
<ul>
<li>x 坐标 &#x3D; 算术强度</li>
<li>y 坐标 &#x3D; 实际性能</li>
<li>落在斜线上 → memory-bound</li>
<li>落在水平线上 → compute-bound</li>
<li>离上限越远，优化空间越大</li>
</ul>
<hr>
<h2 id="五、实验结果分析（4090D，1024×1024-GEMM）"><a href="#五、实验结果分析（4090D，1024×1024-GEMM）" class="headerlink" title="五、实验结果分析（4090D，1024×1024 GEMM）"></a>五、实验结果分析（4090D，1024×1024 GEMM）</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">=== 理论算术强度 ===</span><br><span class="line">Naive：0.25 FLOP/Byte   → 深度 memory-bound（远低于 ridge point 82）</span><br><span class="line">Tiled：8.00 FLOP/Byte   → 仍然 memory-bound</span><br><span class="line"></span><br><span class="line">=== 实测结果 ===</span><br><span class="line">naive：4500 GFLOPS，算力利用率 5.4%</span><br><span class="line">tiled：5763 GFLOPS，算力利用率 7.0%</span><br><span class="line">Speedup：1.3x</span><br></pre></td></tr></table></figure>

<p><strong>为什么 tiled 只快了 1.3x，而不是理论上的 32x：</strong></p>
<p>理论算术强度计算假设所有数据都从 HBM 读取。但 4090D 的 L2 Cache 有 72MB，缓存了 naive kernel 的大量重复读取，真实 HBM 访问量远小于理论值。我们<strong>高估了 naive 的 HBM 访问字节数</strong>，导致算出来的算术强度（0.25）被低估，实际算术强度比 0.25 高得多，naive 的实际性能因此比理论预测高。Tiled 的优势被硬件缓存抵消，只剩 1.3x。</p>
<hr>
<h2 id="六、三个关键-Profile-指标"><a href="#六、三个关键-Profile-指标" class="headerlink" title="六、三个关键 Profile 指标"></a>六、三个关键 Profile 指标</h2><p>在有权限的环境下（非容器），用 <code>ncu</code> 可以直接读取：</p>
<table>
<thead>
<tr>
<th>指标</th>
<th>含义</th>
<th>判断方式</th>
</tr>
</thead>
<tbody><tr>
<td>Compute Throughput</td>
<td>实际 GFLOPS &#x2F; 峰值 GFLOPS</td>
<td>高 → compute-bound</td>
</tr>
<tr>
<td>Memory Throughput</td>
<td>实际带宽 &#x2F; 峰值带宽</td>
<td>高 → memory-bound</td>
</tr>
<tr>
<td>Occupancy</td>
<td>实际 warp 数 &#x2F; 最大 warp 数</td>
<td>低 → 延迟隐藏不足</td>
</tr>
</tbody></table>
<p>两个都低 → occupancy 不足，或存在同步&#x2F;分支等其他瓶颈。</p>
<p>AutoDL 容器环境不支持 <code>ncu</code>（GPU performance counter 被禁用），可用手动计时 + 理论分析替代。</p>
<hr>
<h2 id="七、进一步优化-GEMM-的方向"><a href="#七、进一步优化-GEMM-的方向" class="headerlink" title="七、进一步优化 GEMM 的方向"></a>七、进一步优化 GEMM 的方向</h2><p>当前 tiled GEMM 算术强度 8.0，距离 ridge point 82 还差 10 倍，三个优化方向：</p>
<table>
<thead>
<tr>
<th>方向</th>
<th>原理</th>
<th>收益</th>
</tr>
</thead>
<tbody><tr>
<td>增大 tile 尺寸</td>
<td>更多 thread 复用同一份数据</td>
<td>中，受 Shared Memory 容量限制</td>
</tr>
<tr>
<td>Register Tiling</td>
<td>每个 thread 计算多个输出元素，数据复用在寄存器里</td>
<td>高，cuBLAS 的核心手段</td>
</tr>
<tr>
<td>向量化访存（float4）</td>
<td>一条指令读 16 Byte，减少内存事务数</td>
<td>中</td>
</tr>
</tbody></table>
<p>cuBLAS 综合使用以上手段，能达到算力峰值的 80-90%。</p>
<hr>
<h2 id="参考材料"><a href="#参考材料" class="headerlink" title="参考材料"></a>参考材料</h2><ol>
<li><strong>NVIDIA Nsight Compute 文档</strong>：<a href="https://docs.nvidia.com/nsight-compute/">https://docs.nvidia.com/nsight-compute/</a></li>
<li><strong>Roofline Model 原论文</strong>：<a href="https://crd.lbl.gov/assets/pubs_presos/parlab08-roofline-talk.pdf">https://crd.lbl.gov/assets/pubs_presos/parlab08-roofline-talk.pdf</a></li>
</ol>
]]></content>
      <tags>
        <tag>cuda</tag>
        <tag>llm-inference</tag>
        <tag>roofline</tag>
        <tag>arithmetic-intensity</tag>
        <tag>memory-bound</tag>
        <tag>compute-bound</tag>
        <tag>profiling</tag>
      </tags>
  </entry>
  <entry>
    <title>07 · 自回归生成与 KV Cache</title>
    <url>/2026/04/01/llm-inference/notes/07-autoregressive-generation/</url>
    <content><![CDATA[<h1 id="07-·-自回归生成与-KV-Cache"><a href="#07-·-自回归生成与-KV-Cache" class="headerlink" title="07 · 自回归生成与 KV Cache"></a>07 · 自回归生成与 KV Cache</h1><blockquote>
<p><strong>目标</strong>：理解 LLM 推理的完整时序过程，掌握 KV Cache 的作用和显存代价，理解 Prefill 和 Decode 两个阶段的本质差异</p>
</blockquote>
<hr>
<h2 id="一、Transformer-完整计算图（回顾）"><a href="#一、Transformer-完整计算图（回顾）" class="headerlink" title="一、Transformer 完整计算图（回顾）"></a>一、Transformer 完整计算图（回顾）</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">token 序列</span><br><span class="line">    ↓ Embedding 层</span><br><span class="line">[seq_len, d_model]</span><br><span class="line">    ↓ Transformer Block × N 层（每层结构相同）</span><br><span class="line">        ├── Multi-Head Self-Attention</span><br><span class="line">        │     x → Q,K,V（各乘权重矩阵）</span><br><span class="line">        │     → reshape 成 [num_heads, seq_len, head_dim]</span><br><span class="line">        │     → 每个 head 独立做 Attention</span><br><span class="line">        │     → 拼接 + W_O 投影</span><br><span class="line">        │     → 残差连接 + LayerNorm</span><br><span class="line">        └── FFN</span><br><span class="line">              → W₁ 升维（×4）→ ReLU → W₂ 降维</span><br><span class="line">              → 残差连接 + LayerNorm</span><br><span class="line">[seq_len, d_model]</span><br><span class="line">    ↓ 输出层（W_out，线性变换）</span><br><span class="line">[seq_len, vocab_size]     ← 每个位置预测下一个 token 的概率分布</span><br><span class="line">    ↓ 取最后一个位置（位置 seq_len-1）</span><br><span class="line">[vocab_size]              ← argmax 或采样</span><br><span class="line">下一个 token</span><br></pre></td></tr></table></figure>

<p><strong>为什么取最后一个位置：</strong></p>
<p>位置 i 的输出预测位置 i+1 的 token。序列末尾的位置已经看过所有历史 token，它的输出就是”给定整个输入，下一个 token 是什么”。其他位置的输出在推理时直接丢弃。</p>
<hr>
<h2 id="二、自回归生成"><a href="#二、自回归生成" class="headerlink" title="二、自回归生成"></a>二、自回归生成</h2><p><strong>自回归（Autoregressive）</strong>：用自己过去的输出作为下一步的输入。每一步生成的 token 被拼回输入序列，成为下一步的条件。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入                    → 输出（新 token）</span><br><span class="line">&quot;今天天气&quot;              → &quot;真&quot;</span><br><span class="line">&quot;今天天气真&quot;            → &quot;好&quot;</span><br><span class="line">&quot;今天天气真好&quot;          → &quot;啊&quot;</span><br><span class="line">&quot;今天天气真好啊&quot;        → &lt;EOS&gt;（停止）</span><br></pre></td></tr></table></figure>

<p><strong>为什么必须串行，不能并行输出：</strong></p>
<p>模型学到的是”给定序列，预测下一个 token 的概率分布”。第 i 个 token 的输出会改变第 i+1 个 token 的概率分布——不知道第 i 个 token 是什么，就无法确定第 i+1 个 token。这是严格的串行依赖。</p>
<hr>
<h2 id="三、KV-Cache"><a href="#三、KV-Cache" class="headerlink" title="三、KV Cache"></a>三、KV Cache</h2><p><strong>问题：</strong> 生成第 100 个 token 时，Attention 需要计算这个 token 的 Q 和前 99 个历史 token 的 K&#x2F;V 做点积。这 99 个 K&#x2F;V 在前面的步骤里已经算过了，每次都重新算是浪费。</p>
<p><strong>解决：</strong> 把每个 token 经过 W_K、W_V 变换后的向量缓存起来，下一步直接读，不重新计算。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">第 1 步：计算 K_今、V_今 → 存入 cache</span><br><span class="line">第 2 步：计算 K_天、V_天 → 追加到 cache</span><br><span class="line">         直接读 cache 里的 K_今、V_今</span><br><span class="line">第 3 步：计算 K_真、V_真 → 追加到 cache</span><br><span class="line">         直接读 cache 里的 K_今...K_好</span><br><span class="line">...</span><br></pre></td></tr></table></figure>

<p>每步只需计算<strong>当前新 token</strong> 的 K&#x2F;V，历史 token 的 K&#x2F;V 直接从 cache 读。</p>
<p><strong>代价：</strong> 显存随序列长度持续增长。</p>
<hr>
<h2 id="四、KV-Cache-显存占用"><a href="#四、KV-Cache-显存占用" class="headerlink" title="四、KV Cache 显存占用"></a>四、KV Cache 显存占用</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">KV Cache = 2 × seq_len × num_layers × num_heads × head_dim × bytes_per_element</span><br><span class="line">           ↑</span><br><span class="line">       K 和 V 各一份</span><br></pre></td></tr></table></figure>

<p>以 Qwen-7B 为例（FP16，seq_len&#x3D;2048）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">num_layers = 32</span><br><span class="line">num_heads  = 32</span><br><span class="line">head_dim   = 128</span><br><span class="line">bytes      = 2（FP16）</span><br><span class="line"></span><br><span class="line">每层 KV = 2 × 2048 × 32 × 128 × 2 = 32 MB</span><br><span class="line">全部 KV = 32 MB × 32 层 = 1 GB / 请求</span><br></pre></td></tr></table></figure>

<p>序列长度与并发数的关系（4090D 24GB，50% 显存给 KV Cache）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">seq_len=512   → 0.25 GB/请求 → 最多 48 个并发</span><br><span class="line">seq_len=2048  → 1.00 GB/请求 → 最多 12 个并发</span><br><span class="line">seq_len=4096  → 2.00 GB/请求 → 最多  6 个并发</span><br><span class="line">seq_len=8192  → 4.00 GB/请求 → 最多  3 个并发</span><br></pre></td></tr></table></figure>

<p>序列长度翻倍，KV Cache 翻倍，最大并发数减半。这是推理系统显存管理的核心约束。</p>
<hr>
<h2 id="五、Prefill-和-Decode-两个阶段"><a href="#五、Prefill-和-Decode-两个阶段" class="headerlink" title="五、Prefill 和 Decode 两个阶段"></a>五、Prefill 和 Decode 两个阶段</h2><p><strong>Prefill（预填充）：</strong></p>
<p>输入 prompt 一次性全部处理，计算所有 token 的 K&#x2F;V 填满 cache。</p>
<ul>
<li>输入：N 个 token（整个 prompt）</li>
<li>并行度：高，所有 token 同时计算</li>
<li>算术强度：高（计算量 O(N²)，数据量 O(d_model²)，N 大时 compute-bound）</li>
<li>延迟指标：<strong>TTFT（Time To First Token）</strong></li>
</ul>
<p><strong>Decode（解码）：</strong></p>
<p>每步只生成一个新 token，读取全部历史 KV cache，追加新 K&#x2F;V。</p>
<ul>
<li>输入：1 个 token</li>
<li>并行度：极低，严格串行</li>
<li>算术强度：低（计算量 O(seq_len)，数据量 O(seq_len)，常数比值，memory-bound）</li>
<li>瓶颈：每步都要从显存读取整个 KV cache，GPU 算力大量闲置等数据</li>
<li>延迟指标：<strong>TPOT（Time Per Output Token）</strong></li>
</ul>
<table>
<thead>
<tr>
<th></th>
<th>Prefill</th>
<th>Decode</th>
</tr>
</thead>
<tbody><tr>
<td>输入规模</td>
<td>N 个 token</td>
<td>1 个 token</td>
</tr>
<tr>
<td>并行度</td>
<td>高</td>
<td>极低</td>
</tr>
<tr>
<td>瓶颈</td>
<td>compute-bound</td>
<td>memory-bound</td>
</tr>
<tr>
<td>延迟指标</td>
<td>TTFT</td>
<td>TPOT</td>
</tr>
</tbody></table>
<hr>
<h2 id="六、KV-Cache-是后续优化的核心约束"><a href="#六、KV-Cache-是后续优化的核心约束" class="headerlink" title="六、KV Cache 是后续优化的核心约束"></a>六、KV Cache 是后续优化的核心约束</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">节点 08（Batching）   → 多个请求共享 GPU，提升 Decode 阶段的吞吐</span><br><span class="line">节点 09（PagedAttention）→ 管理 KV Cache 的显存碎片，提升最大并发数</span><br><span class="line">节点 10（FlashAttention）→ 计算 Attention 时减少显存占用</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="七、跨请求-KV-Cache（扩展）"><a href="#七、跨请求-KV-Cache（扩展）" class="headerlink" title="七、跨请求 KV Cache（扩展）"></a>七、跨请求 KV Cache（扩展）</h2><p>节点 07 讲的是单请求内的 KV Cache（避免重复计算历史 token）。</p>
<p>更高层的优化是<strong>跨请求 KV Cache</strong>（如 LMCache）：不同请求如果有相同前缀（同一个 system prompt、同一段 RAG 背景文档），把这部分 K&#x2F;V 缓存起来跨请求复用，下一个请求直接跳过这部分 Prefill 计算。两者是不同层次的概念，建立在单请求 KV Cache 之上。</p>
<hr>
<h2 id="参考材料"><a href="#参考材料" class="headerlink" title="参考材料"></a>参考材料</h2><ol>
<li><strong>vLLM 博客：Efficient Memory Management</strong>：<a href="https://blog.vllm.ai/2023/06/20/vllm.html">https://blog.vllm.ai/2023/06/20/vllm.html</a></li>
<li><strong>Lilian Weng: LLM Inference Optimization</strong>：<a href="https://lilianweng.github.io/posts/2023-01-10-inference-optimization/">https://lilianweng.github.io/posts/2023-01-10-inference-optimization/</a></li>
</ol>
]]></content>
      <tags>
        <tag>transformer</tag>
        <tag>llm-inference</tag>
        <tag>autoregressive</tag>
        <tag>kv-cache</tag>
        <tag>prefill</tag>
        <tag>decode</tag>
      </tags>
  </entry>
  <entry>
    <title>08 · Batching 与调度</title>
    <url>/2026/04/01/llm-inference/notes/08-batching-scheduling/</url>
    <content><![CDATA[<h1 id="08-·-Batching-与调度"><a href="#08-·-Batching-与调度" class="headerlink" title="08 · Batching 与调度"></a>08 · Batching 与调度</h1><blockquote>
<p><strong>目标</strong>：理解 Batching 的本质收益，掌握 Static Batching 和 Continuous Batching 的区别，理解 vLLM scheduler 的核心调度逻辑</p>
</blockquote>
<hr>
<h2 id="一、为什么需要-Batching"><a href="#一、为什么需要-Batching" class="headerlink" title="一、为什么需要 Batching"></a>一、为什么需要 Batching</h2><p>Decode 阶段每步只处理 1 个 token，GPU 有上万个 CUDA core，但只有 1 个 token 的计算量——绝大多数算力在空转。</p>
<p><strong>Batching 的本质收益：让多个请求分摊读取模型权重矩阵的开销，提升算术强度。</strong></p>
<p>用 roofline 语言：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">单请求 decode：</span><br><span class="line">  计算量：1 × d_model²</span><br><span class="line">  数据量：d_model²（读权重矩阵）</span><br><span class="line">  算术强度 ≈ 1 FLOP/Byte   ← 极低，memory-bound</span><br><span class="line"></span><br><span class="line">batch size = N：</span><br><span class="line">  计算量：N × d_model²</span><br><span class="line">  数据量：d_model²（权重矩阵只读一次，N 个请求共用）</span><br><span class="line">  算术强度 ≈ N FLOP/Byte   ← 随 batch size 线性增长</span><br></pre></td></tr></table></figure>

<p>batch size 越大，算术强度越高，在 roofline 图上向右移动，逐渐从 memory-bound 逼近 ridge point。</p>
<hr>
<h2 id="二、Static-Batching-的问题"><a href="#二、Static-Batching-的问题" class="headerlink" title="二、Static Batching 的问题"></a>二、Static Batching 的问题</h2><p>Static Batching：收集一批请求，打包给 GPU，等所有请求都完成再接受下一批。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Batch = [请求A（输出10个token）, 请求B（输出20个token）, 请求C（输出5个token）]</span><br><span class="line"></span><br><span class="line">请求A完成后：████████████████░░░░░░░  （空等请求B）</span><br><span class="line">请求C完成后：█████░░░░░░░░░░░░░░░░░░  （空等请求B）</span><br><span class="line">请求B完成后：████████████████████████  整个 batch 才能结束</span><br></pre></td></tr></table></figure>

<p>问题：LLM 的输出长度在生成完成前完全不可预知，短请求完成后只能空等最长的请求，GPU slot 浪费。</p>
<hr>
<h2 id="三、Continuous-Batching"><a href="#三、Continuous-Batching" class="headerlink" title="三、Continuous Batching"></a>三、Continuous Batching</h2><p>Orca 论文（OSDI 2022）提出：<strong>不以请求为调度单位，以每个 decode step 为调度单位（iteration-level scheduling）</strong>。</p>
<p>每个 decode step 结束后：</p>
<ul>
<li>检查哪些请求输出了 <code>&lt;EOS&gt;</code>，完成的立即出队返回结果</li>
<li>把等待队列里的新请求加入 running batch</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Step 1：[请求A, 请求B, 请求C] → 各生成 1 个 token</span><br><span class="line">Step 3：请求C 输出 &lt;EOS&gt; → 移除，加入请求D</span><br><span class="line">        [请求A, 请求B, 请求D]</span><br><span class="line">Step 4：请求A 输出 &lt;EOS&gt; → 移除，加入请求E</span><br><span class="line">        [请求B, 请求D, 请求E]</span><br><span class="line">...</span><br></pre></td></tr></table></figure>

<p>GPU 始终满载，短请求完成即返回，不被长请求拖累。</p>
<table>
<thead>
<tr>
<th></th>
<th>Static Batching</th>
<th>Continuous Batching</th>
</tr>
</thead>
<tbody><tr>
<td>调度单位</td>
<td>整个请求</td>
<td>每个 decode step</td>
</tr>
<tr>
<td>短请求完成后</td>
<td>等待 batch 里最长的请求</td>
<td>立即换入新请求</td>
</tr>
<tr>
<td>GPU 利用率</td>
<td>低（有空等）</td>
<td>高（持续满载）</td>
</tr>
<tr>
<td>算术强度</td>
<td>随空等下降</td>
<td>始终最大化</td>
</tr>
</tbody></table>
<hr>
<h2 id="四、为什么-2022-年才出现"><a href="#四、为什么-2022-年才出现" class="headerlink" title="四、为什么 2022 年才出现"></a>四、为什么 2022 年才出现</h2><p>不是没人想到，是之前没有这个需求。</p>
<p>传统深度学习推理（图像分类、机器翻译）的输出长度固定或可预估，Static Batching 够用。</p>
<p>LLM 自回归生成的输出长度完全不可预知——“帮我写首诗”可能 50 个 token，”帮我分析报告”可能 2000 个 token，差距 40 倍。这个特性在 ChatGPT 大规模商业化之前没有真实需求驱动。Orca 论文恰好在 ChatGPT 上线前几个月发表，正是 LLM 推理服务化开始被认真对待的时间点。</p>
<p>OS 的调度思想早就有，但需要有人把它迁移到 LLM 推理这个新场景，识别出问题根本是”输出长度不可预知”，然后用 iteration-level scheduling 解决。</p>
<hr>
<h2 id="五、vLLM-Scheduler-的核心逻辑"><a href="#五、vLLM-Scheduler-的核心逻辑" class="headerlink" title="五、vLLM Scheduler 的核心逻辑"></a>五、vLLM Scheduler 的核心逻辑</h2><p>vLLM 实现了 Continuous Batching，调度器每个 step 做三件事：</p>
<p><strong>1. 决定哪些请求进入 running</strong>：受显存容量和 batch size 上限约束</p>
<p><strong>2. Swap out</strong>：显存不足时，把低优先级请求的 KV Cache 换出到 CPU 内存，等显存释放后换回</p>
<p><strong>3. Preempt</strong>：高优先级请求进来时，暂停低优先级请求</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">每个 decode step：</span><br><span class="line">    running 队列 → 执行一步 decode</span><br><span class="line">    等待队列 → 尝试加入 running（显存够才允许）</span><br><span class="line">    running → 完成的请求移出，返回结果</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="六、Continuous-Batching-残留的问题"><a href="#六、Continuous-Batching-残留的问题" class="headerlink" title="六、Continuous Batching 残留的问题"></a>六、Continuous Batching 残留的问题</h2><p>Continuous Batching 解决了调度效率，但没有解决<strong>显存碎片</strong>问题：</p>
<p>传统做法给每个请求预分配连续显存，大小 &#x3D; 最大序列长度 × KV cache per token：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">请求A：预分配 2048 token 的空间（实际只用 50 token）→ 大量浪费</span><br><span class="line">请求B：预分配 2048 token 的空间（实际用 1800 token）→ 勉强够用</span><br></pre></td></tr></table></figure>

<ul>
<li>不知道请求最终输出多少 token，不知道预留多少显存</li>
<li>预留少了不够，预留多了浪费</li>
<li>显存碎片导致实际并发数远低于理论上限</li>
</ul>
<p>这是节点 09（PagedAttention）要解决的核心问题。</p>
<hr>
<h2 id="参考材料"><a href="#参考材料" class="headerlink" title="参考材料"></a>参考材料</h2><ol>
<li><strong>Orca 论文</strong>：<a href="https://www.usenix.org/conference/osdi22/presentation/yu">https://www.usenix.org/conference/osdi22/presentation/yu</a></li>
<li><strong>vLLM 论文 Section 3</strong>：<a href="https://arxiv.org/abs/2309.06180">https://arxiv.org/abs/2309.06180</a></li>
</ol>
]]></content>
      <tags>
        <tag>batching</tag>
        <tag>llm-inference</tag>
        <tag>continuous-batching</tag>
        <tag>scheduling</tag>
        <tag>throughput</tag>
        <tag>vllm</tag>
      </tags>
  </entry>
</search>
